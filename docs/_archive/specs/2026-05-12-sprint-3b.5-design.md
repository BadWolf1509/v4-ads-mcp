# Sprint 3b.5 Design — `stabilization-fixes`

**Date:** 2026-05-12
**Author:** Claude (Sonnet 4.7) em sessão dirigida por wellinton.ribeiro@v4company.com
**Status:** APPROVED — pronto para writing-plans

## 1. Goal

Stabilization sprint que corrige 3 bugs descobertos no Sprint 3b.4 smoke + estabelece test pattern para prevenir A4-class bugs futuros. **Não shippa features novas.** 4 changes paralelas em commits independentes, com smoke runbook abrangente cobrindo todos os fixes.

## 2. Scope decisions (5 perguntas resolvidas via brainstorming 2026-05-12)

| # | Decisão | Resolução | Justificativa |
|---|---|---|---|
| 1 | Composite vs separate sprint | **Composite** | A3+A4 forçados a compartilhar `apply_audience.py`. ProtoFieldCapture é cross-cutting. Histórico de multi-task sprints (3b.1-3b.4) funciona bem com subagent-driven-development |
| 2 | A2 fix mode | **A — schema-restrict** | 8 das 8 dimensões favorecem (audit log clarity, alignment Google Ads UI, V4 playbook PAUSE-not-REMOVE, LOC mínimo, lower risk, SRP, zero consumer backward-compat impact, escalas melhor) |
| 3 | A4 API mechanism | **Pre-flight rejection** (Option 3 empírico confirmado) | Empirically tested: `ad_group_criterion` honors `negative=True` for user_list; `campaign_criterion` does not. Fix é pre-flight reject `(campaign, exclusion, user_list)` combo + direcionar gestor para ad_group level |
| 4 | A3 pre-flight mechanism | **A — GAQL batch lookup** | Empirical batch IN clause confirmed (1 read op per call regardless of batch size). Self-updating vs hardcoded ID range; cobre 6 taxonomy_types (5 confirmed + 1 unknown). Custo negligível vs quota |
| 5 | ProtoFieldCapture scope | **C — hybrid (greenfield + retrofit audiences only)** | Greenfield orphan se não exercitado em Sprint 3b.5. Retrofit `test_builder_campaign_user_list_exclusion` encoda lição A4 como regression test. Outros 3 builders empíricamente OK + YAGNI no retrofit speculative |

## 3. Component specs

### 3.1 A2 — Schema-restrict `update_*_status` tools

**Files modified (5):**
- `src/mcp/tools/update_campaign_status.py`
- `src/mcp/tools/update_ad_group_status.py`
- `src/mcp/tools/update_ad_status.py`
- `src/mcp/tools/update_keyword_status.py`
- `src/governance/blast_radius.py`

**Schema change (idêntico nos 4 tools):**

```python
# Before
"new_status": {"type": "string", "enum": ["ENABLED", "PAUSED", "REMOVED"]}

# After
"new_status": {"type": "string", "enum": ["ENABLED", "PAUSED"]}
```

**Tool description PT-BR update (4 tools):** remove menção de "remover" / "REMOVED". Adiciona note: "Para remover entidades, use Google Ads UI. Tool dedicada `remove_*` pode ser adicionada em sprint futura se demanda real surgir."

**`blast_radius.py` simplification:** remove o special-case REMOVED-always-confirm block adicionado no Sprint 3b.2 retrofit (linhas ~89-93). Esse caminho **nunca executou em produção** porque Google rejeita REMOVED em `*.status.update` (descoberto Sprint 3b.3 smoke). Dead code.

```python
# Before
if operation in ("update_campaign_status", "update_ad_group_status",
                  "update_ad_status", "update_keyword_status"):
    if new_status == "REMOVED":  # ← REMOVE THIS BLOCK
        return RiskClassification(
            RiskLevel.CONFIRM,
            f"{operation}: new_status=REMOVED — remove qualquer coisa sempre confirma (spec §7.1)",
        )
    return _bulk_status_classify(operation, target_count)

# After
if operation in ("update_campaign_status", "update_ad_group_status",
                  "update_ad_status", "update_keyword_status"):
    return _bulk_status_classify(operation, target_count)
```

**Test changes:**

- **Cada um dos 4 `test_update_*_status.py`** ganha 1 test novo:
  ```python
  def test_schema_rejects_removed_status():
      from src.mcp.tools.update_keyword_status import _SCHEMA  # ou similar
      bad = _good_payload()
      bad["new_status"] = "REMOVED"
      with pytest.raises(ValidationError):
          validate(bad, _SCHEMA)
  ```

- **`tests/unit/test_blast_radius.py`** — **remove** 4 casos parametrize que testavam REMOVED behavior (agora dead code):
  ```python
  # REMOVE:
  ("update_campaign_status", {"target_count": 1, "new_status": "REMOVED"}, RiskLevel.CONFIRM, "remove"),
  ("update_ad_group_status", {"target_count": 1, "new_status": "REMOVED"}, RiskLevel.CONFIRM, "remove"),
  ("update_keyword_status", {"target_count": 3, "new_status": "REMOVED"}, RiskLevel.CONFIRM, "remove"),
  ("update_ad_status", {"target_count": 1, "new_status": "REMOVED"}, RiskLevel.CONFIRM, "remove"),
  ```

**Net delta:** ~30 LOC source (deletions dominant) + ~10 test LOC additions − ~15 test LOC deletions = ~25 LOC net delta.

**Builder layer:** **inalterado**. Builders nunca dispatcharam para `*_operation.remove`.

### 3.2 A4 — Pre-flight rejection campaign+exclusion+user_list

**File modified:** `src/mcp/tools/apply_audience.py` — extensão de `_preflight_validate`.

**Signature change:** `_preflight_validate` atualmente aceita `(customer_id, mode, attachments)` (Sprint 3b.4). Mudar para `(customer_id, target_type, mode, attachments)` — `target_type` é necessário pra A4 rule. Caller em `apply_audience()` passa `args["target_type"]`.

**New validation rule (sync, added FIRST in `_preflight_validate`):**

```python
# A4 finding (Sprint 3b.4 smoke + Sprint 3b.5 brainstorming empirical):
# Google silently overrides negative=True → false on CampaignCriterion
# when subtype is user_list. AdGroupCriterion honors negative=True
# correctly for user_list. Direct gestor to ad_group level.
if target_type == "campaign" and mode == "exclusion":
    offending = [i for i, a in enumerate(attachments) if a["audience_type"] == "user_list"]
    if offending:
        return (
            f"Customer Match (user_list) exclusion em campaign level nao eh "
            f"suportada pela Google Ads API — negative flag eh silently dropado "
            f"em CampaignCriterion para user_list. Use target_type='ad_group' "
            f"em vez disso (attachments {offending} sao user_list). "
            f"user_interest exclusion em campaign continua funcionando."
        )
```

**Docstring update:** mention this constraint explicitly. User-facing tool description também.

**Tests novos (em `test_apply_audience.py`):**
- `test_preflight_rejects_campaign_user_list_exclusion` — A4 regression test, error mentions "ad_group"
- `test_preflight_allows_campaign_user_interest_exclusion` — sanity: user_interest exclusion em campaign continua funcionando

### 3.3 A3 — Pre-flight GAQL taxonomy validation

**File modified:** `src/mcp/tools/apply_audience.py` — nova função async + integração no flow.

**New constants:**
```python
_COMPATIBLE_TAXONOMIES = ("IN_MARKET", "AFFINITY")  # SEARCH ad_group/campaign compatible
```

**New async helper.** Por que async + `ctx` como primeiro arg: a função precisa fazer GAQL call via `run_report(manager_id=..., session_id=...)`, então recebe `ctx` (`McpRequestContext`) pra propagar credentials. Função fica em-módulo (não exportada) já que é internal pre-flight.

```python
async def _validate_user_interest_taxonomies(
    ctx, customer_id: str, attachments: list[dict[str, Any]]
) -> str | None:
    """Returns error message PT-BR if any user_interest taxonomy incompatible; None if OK.

    Performs 1 GAQL batch lookup if user_interest attachments present.
    Returns None (skip) if no user_interest in batch.
    """
    user_interest_ids = []
    user_interest_indices = []
    for i, att in enumerate(attachments):
        if att["audience_type"] == "user_interest":
            ui_id = att["audience_resource_name"].rsplit("/", 1)[-1]
            user_interest_ids.append(ui_id)
            user_interest_indices.append(i)

    if not user_interest_ids:
        return None  # No user_interest attachments; skip GAQL

    ids_clause = ", ".join(user_interest_ids)
    query = (
        f"SELECT user_interest.user_interest_id, user_interest.taxonomy_type "
        f"FROM user_interest "
        f"WHERE user_interest.user_interest_id IN ({ids_clause})"
    )
    rows = await run_report(
        manager_id=ctx.manager_id,
        session_id=ctx.session_id,
        customer_id=customer_id,
        query=query,
        row_formatter=lambda r: {
            "id": str(r.user_interest.user_interest_id),
            "taxonomy_type": r.user_interest.taxonomy_type.name,
        },
        operation_name="apply_audience_preflight_taxonomy_lookup",
        audit_this_call=False,  # internal read for validation, not user-facing audit
    )

    taxonomy_by_id = {row["id"]: row["taxonomy_type"] for row in rows}

    incompatible = []
    for i, ui_id in zip(user_interest_indices, user_interest_ids, strict=True):
        taxonomy = taxonomy_by_id.get(ui_id, "UNKNOWN")
        if taxonomy not in _COMPATIBLE_TAXONOMIES:
            incompatible.append({"index": i, "id": ui_id, "taxonomy": taxonomy})

    if incompatible:
        details = ", ".join(
            f"attachments[{x['index']}]={x['id']} ({x['taxonomy']})"
            for x in incompatible
        )
        return (
            f"user_interest attachments com taxonomy_type incompativel detectados: "
            f"{details}. Apenas {', '.join(_COMPATIBLE_TAXONOMIES)} sao aceitas pra "
            f"attachment em ad_group/campaign (V4 use case SEARCH). VERTICAL_GEO "
            f"(Display Topics, IDs 1-79999) eh silently dropado pelo Google."
        )

    return None
```

**Integration in `apply_audience()`:**

```python
async def apply_audience(args: dict[str, Any]) -> dict[str, Any]:
    ctx = get_current()
    # ... existing arg extraction ...

    # Sync pre-flight (existing rules + A4)
    preflight_error = _preflight_validate(customer_id, target_type, mode, attachments)
    if preflight_error:
        return {"status": "error", ...}

    # Async pre-flight (A3 — GAQL taxonomy lookup)
    taxonomy_error = await _validate_user_interest_taxonomies(ctx, customer_id, attachments)
    if taxonomy_error:
        return {
            "status": "error",
            "operation": "apply_audience",
            "customer_id": customer_id,
            "error": taxonomy_error,
        }

    # ... existing classification + dispatch ...
```

**Order rationale:** sync checks first (zero quota cost) → async GAQL only if sync passes (saves 1 read op when sync rejects).

**Tests novos (em `test_apply_audience.py`):**
- `test_taxonomy_preflight_rejects_vertical_geo` — mock GAQL returning VERTICAL_GEO, expect rejection
- `test_taxonomy_preflight_allows_in_market` — mock returning IN_MARKET, expect pass-through
- `test_taxonomy_preflight_allows_affinity` — mock returning AFFINITY, expect pass-through
- `test_taxonomy_preflight_skipped_when_no_user_interest` — pure user_list batch, expect GAQL NOT called (perf)
- `test_taxonomy_preflight_batch_lookup_single_read` — 3 user_interest attachments → 1 GAQL call (not 3)

### 3.4 ProtoFieldCapture pattern

**File created:** `tests/unit/fixtures/proto_capture.py`

**Purpose:** replace MagicMock-everywhere pattern (which silently accepts any attribute set, masking proto field assignment bugs) with a fixture that **captures** assignments for test assertions.

**Implementation:**

```python
"""Proto field capture fixture for mutate builder tests.

Replaces MagicMock pattern that masks proto field assignment bugs (Sprint
3b.4 finding A4 — Google override of negative=True passed undetected because
MagicMock accepts any attribute set silently).

Usage:
    client = make_capture_client()
    ops = build_apply_audience(client, "1234567890", payload)
    assert ops[0].field("ad_group_criterion_operation.create.negative") is True
"""

from typing import Any
from unittest.mock import MagicMock


class _SubCapture:
    """Nested capture for `op.foo.bar.baz = X` style assignments."""

    def __init__(self, name: str, parent_dict: dict[str, Any]) -> None:
        self._name = name
        self._parent = parent_dict
        self._captured: dict[str, Any] = {}

    def __setattr__(self, key: str, value: Any) -> None:
        if key.startswith("_"):
            object.__setattr__(self, key, value)
        else:
            self._captured[key] = value

    def __getattr__(self, key: str) -> Any:
        if key.startswith("_"):
            return object.__getattribute__(self, key)
        if key not in self._captured:
            self._captured[key] = _SubCapture(key, self._captured)
        return self._captured[key]


class CapturedOp:
    """Wraps a proto-like op + records every assignment in a nested dict.

    Drop-in replacement for client.get_type('MutateOperation') in builder
    tests. Builder code does e.g. `op.ad_group_criterion_operation.create.negative = True`
    — capture stores it so tests can assert via `op.field("ad_group_criterion_operation.create.negative")`.
    """

    def __init__(self) -> None:
        self._captured: dict[str, Any] = {}

    def __getattr__(self, name: str) -> Any:
        if name.startswith("_"):
            return object.__getattribute__(self, name)
        if name not in self._captured:
            self._captured[name] = _SubCapture(name, self._captured)
        return self._captured[name]

    def field(self, path: str) -> Any:
        """Get captured value at dotted path. Returns None if not set."""
        cur: Any = self._captured
        for part in path.split("."):
            if isinstance(cur, _SubCapture):
                cur = cur._captured
            if isinstance(cur, dict) and part in cur:
                cur = cur[part]
            else:
                return None
        if isinstance(cur, _SubCapture):
            return None  # Sub-message, not a leaf value
        return cur

    def has(self, path: str) -> bool:
        """True if path was explicitly assigned (vs never set)."""
        return self.field(path) is not None


def make_capture_client() -> MagicMock:
    """Mock SDK client whose get_type() returns CapturedOp instances."""
    client = MagicMock()

    ag_service = MagicMock()
    ag_service.ad_group_path = lambda cid, ag_id: f"customers/{cid}/adGroups/{ag_id}"
    camp_service = MagicMock()
    camp_service.campaign_path = lambda cid, c_id: f"customers/{cid}/campaigns/{c_id}"

    def get_service(name: str) -> Any:
        if name == "AdGroupService":
            return ag_service
        if name == "CampaignService":
            return camp_service
        return MagicMock()

    client.get_service = get_service
    client.get_type = lambda _name: CapturedOp()
    client.enums.AdGroupCriterionStatusEnum.ENABLED = "AG_ENABLED"
    client.enums.CampaignCriterionStatusEnum.ENABLED = "CAMP_ENABLED"
    return client
```

**Retrofit `test_apply_audience_builder.py::test_builder_campaign_user_list_exclusion`:**

Replace the existing test (which only checks `len(ops) == 1`) with:

```python
def test_builder_campaign_user_list_exclusion_sets_negative_true():
    """A4 regression test: builder MUST set crit.negative = True for exclusion.

    Google then silently overrides this to false (Sprint 3b.4 A4 finding), but
    our builder must NOT regress on its side. This test verifies the builder's
    contract independently of Google's server-side behavior.
    """
    from tests.unit.fixtures.proto_capture import make_capture_client
    from src.google_ads.mutates.audiences import build_apply_audience

    client = make_capture_client()
    payload = {
        "target_type": "campaign",
        "mode": "exclusion",
        "attachments": [
            {
                "target_id": "22169885957",
                "audience_type": "user_list",
                "audience_resource_name": "customers/1234567890/userLists/9377822529",
            }
        ],
    }
    ops = build_apply_audience(client, "1234567890", payload)
    assert len(ops) == 1
    op = ops[0]

    # Critical regression assertions (would have caught A4):
    assert op.field("campaign_criterion_operation.create.negative") is True
    assert op.field("campaign_criterion_operation.create.campaign") == "customers/1234567890/campaigns/22169885957"
    assert op.field("campaign_criterion_operation.create.user_list.user_list") == "customers/1234567890/userLists/9377822529"
    assert op.field("campaign_criterion_operation.create.status") == "CAMP_ENABLED"
    assert not op.has("campaign_criterion_operation.create.bid_modifier")  # exclusion has no bid_modifier
```

**CLAUDE.md update (conventions section):**

```markdown
### Mutate builder test convention (post-Sprint 3b.5)

New mutate builder tests MUST use `tests/unit/fixtures/proto_capture.py::make_capture_client`
instead of MagicMock when asserting on proto field assignments. MagicMock accepts any
attribute set silently, masking bugs like Sprint 3b.4 A4 (Google override of `negative=True`
on CampaignCriterion user_list — shipped the bug because the test couldn't verify the
field was actually being set).

Retrofitting existing builder tests (negatives, keywords, ad_groups) is optional and
deferred — only the audiences builder has been retrofitted (Sprint 3b.5).
```

## 4. Files affected

```
src/
├── governance/
│   └── blast_radius.py                          # MODIFY: remove REMOVED special-case (dead code)
└── mcp/tools/
    ├── apply_audience.py                        # MODIFY: A4 sync pre-flight + A3 async pre-flight
    ├── update_campaign_status.py                # MODIFY: schema enum + description PT-BR
    ├── update_ad_group_status.py                # MODIFY: schema enum + description PT-BR
    ├── update_ad_status.py                      # MODIFY: schema enum + description PT-BR
    └── update_keyword_status.py                 # MODIFY: schema enum + description PT-BR

tests/
├── unit/
│   ├── fixtures/
│   │   └── proto_capture.py                     # NEW: ProtoFieldCapture fixture
│   ├── test_blast_radius.py                     # MODIFY: remove 4 REMOVED test cases (dead)
│   ├── test_apply_audience.py                   # MODIFY: 7 new tests (A4 x2, A3 x5)
│   ├── test_apply_audience_builder.py           # MODIFY: retrofit T3 with ProtoFieldCapture
│   ├── test_update_campaign_status.py           # MODIFY: 1 new test (rejects REMOVED)
│   ├── test_update_ad_group_status.py           # MODIFY: 1 new test (rejects REMOVED)
│   ├── test_update_ad_status.py                 # MODIFY: 1 new test (rejects REMOVED)
│   └── test_update_keyword_status.py            # MODIFY: 1 new test (rejects REMOVED)

docs/
├── operacao/
│   └── phase-3b-5-bootstrap.md                  # NEW: 10-test smoke runbook
├── superpowers/specs/
│   └── 2026-05-12-sprint-3b.5-design.md         # NEW: this file
└── CLAUDE.md                                    # MODIFY: convention note about ProtoFieldCapture
```

## 5. Error handling

All 4 fixes use the existing pre-flight error response shape:

```json
{
  "status": "error",
  "operation": "<tool_name>",
  "customer_id": "<id>",
  "error": "<PT-BR descriptive message>"
}
```

For A2 (schema-restrict), the rejection happens at jsonschema layer (MCP transport):

```
Input validation error: 'REMOVED' is not one of ['ENABLED', 'PAUSED']
```

This is the standard MCP transport-level error — same as Sprint 3b.3 T4 (REGEX rejection).

For A3 + A4, the rejection happens in `_preflight_validate` or `_validate_user_interest_taxonomies` — returns Python error string captured by the tool's main flow into the standard error shape.

## 6. Risks & mitigations

| Risk | Likelihood | Mitigation |
|---|---|---|
| Gestor confunde rejeição A2 com "tool quebrado" | Medium | Tool description PT-BR explicitamente menciona "use Google Ads UI para remover". Erro do jsonschema é claro |
| A3 GAQL pré-flight falha (Google API down) | Low | Erro de GAQL bubbles up via `run_report` → tool returns `error` status with raw message. Não bloqueia outras ops |
| Test pattern overhead — ProtoFieldCapture adiciona complexidade | Low | Apenas 1 retrofit + greenfield para futuros builders. Pattern documentado em CLAUDE.md |
| `_validate_user_interest_taxonomies` adiciona latência (1 GAQL call) | Low | ~100-300ms extra por call. Skip via early-return quando no user_interest no batch. Aceitável vs valor de prevenir silent bug |
| Gestor não entende por que campaign-level user_list exclusion não funciona | Medium | Erro PT-BR explica explicitamente + sugere ad_group level. Docstring atualizada |
| A2 rejection quebra workflow existente que dependia de REMOVED | Very Low | REMOVED no MCP **já estava quebrado** desde Sprint 3a. Ninguém pode ter dependência funcional. Não há regressão |

## 7. Open questions — resolved

| # | Question | Resolution |
|---|---|---|
| 1 | Composite vs separate sprint | Composite (A) — shared infrastructure + atomicity OK via commits independentes |
| 2 | A2 fix: schema-restrict vs builder branch | Schema-restrict (A) — wins 8/8 dimensions |
| 3 | A4 API mechanism | Pre-flight rejection (Option 3 empirical confirmed) — ad_group works, campaign doesn't for user_list |
| 4 | A3 pre-flight: GAQL lookup vs hardcoded range | GAQL batch lookup (A) — 1 read op per call regardless of batch via `IN (...)` clause |
| 5 | ProtoFieldCapture scope | Hybrid (C) — greenfield + retrofit audiences only. YAGNI no retrofit completo |

## 8. Out of scope (explicit)

- **`remove_*` tools** (`remove_keyword`, `remove_campaign`, etc.) — separate sprint candidato se demanda real surgir
- **Retrofit dos outros 3 builders** (negatives, keywords, ad_groups) com ProtoFieldCapture — empirically OK em prod + YAGNI
- **Audience asset path** (Option 2 A4) — eliminado pela descoberta empírica de Option 3 funciona em ad_group
- **Cleanup do criterion observation `2480650242694`** na Mestre da Obra JP — Wellington decide via UI se quiser detach (Customer Match observation é benigna)
- **Expansion taxonomy whitelist** (MOBILE_APP_INSTALL_USER, NEW_SMART_PHONE_USER) — out of V4 use case (SEARCH). Adicionar se demanda real surgir
- **bulk_pause_by_query** integration com schema-restrict de A2 — separado; aquela tool tem seu próprio target_type discriminator e não usa `new_status` field

## 9. References

- Spec Sprint 3b.4 (gerou A3 + A4): [`2026-05-12-sprint-3b.4-design.md`](2026-05-12-sprint-3b.4-design.md)
- Spec Sprint 3b.3 (gerou A2 via smoke): [`2026-05-11-sprint-3b.3-design.md`](2026-05-11-sprint-3b.3-design.md)
- Spec Sprint 3b.2 (que adicionou o REMOVED retrofit agora removido): [`2026-05-11-sprint-3b.2-design.md`](2026-05-11-sprint-3b.2-design.md)
- Smoke 3b.3 (T2 que descobriu A2): [`../../operacao/phase-3b-3-bootstrap.md`](../../operacao/phase-3b-3-bootstrap.md)
- Smoke 3b.4 (T1 que descobriu A3 + T3 que descobriu A4): [`../../operacao/phase-3b-4-bootstrap.md`](../../operacao/phase-3b-4-bootstrap.md)
- Existing pattern peer: `src/mcp/tools/add_negatives_from_search_terms.py` (partial_failure pattern)
- Existing pattern peer: `src/mcp/tools/apply_audience.py` (will be modified — current pre-flight rules base)
