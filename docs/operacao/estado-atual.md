# Estado atual — V4 Ads MCP

> **Volátil por natureza.** Esta é a seção que muda a cada sessão; ela vivia no
> `CLAUDE.md` e era o motivo de ele reinchar — estado e convenção no mesmo arquivo
> significa que todo trabalho novo empurra bytes para dentro do que é carregado
> sempre. O `CLAUDE.md` mantém um resumo de poucas linhas e aponta para cá.

> **Ao terminar uma sessão, atualize ESTE arquivo**, não o `CLAUDE.md`.

---

**Última atualização:** 2026-09-05. **Tudo mesclado e em produção** — catorze PRs ([#27](https://github.com/BadWolf1509/v4-ads-mcp/pull/27), [#28](https://github.com/BadWolf1509/v4-ads-mcp/pull/28), [#29](https://github.com/BadWolf1509/v4-ads-mcp/pull/29), [#30](https://github.com/BadWolf1509/v4-ads-mcp/pull/30), [#31](https://github.com/BadWolf1509/v4-ads-mcp/pull/31), [#32](https://github.com/BadWolf1509/v4-ads-mcp/pull/32), [#33](https://github.com/BadWolf1509/v4-ads-mcp/pull/33), [#34](https://github.com/BadWolf1509/v4-ads-mcp/pull/34), [#35](https://github.com/BadWolf1509/v4-ads-mcp/pull/35), [#36](https://github.com/BadWolf1509/v4-ads-mcp/pull/36), [#37](https://github.com/BadWolf1509/v4-ads-mcp/pull/37), [#38](https://github.com/BadWolf1509/v4-ads-mcp/pull/38), [#39](https://github.com/BadWolf1509/v4-ads-mcp/pull/39), [#40](https://github.com/BadWolf1509/v4-ads-mcp/pull/40)) mesclados **sem `--admin`**, cada deploy verificado; `/health` 200 com `db: ok`. Mais os **quatro do Dependabot** de 05/09 ([#22](https://github.com/BadWolf1509/v4-ads-mcp/pull/22), [#23](https://github.com/BadWolf1509/v4-ads-mcp/pull/23), [#25](https://github.com/BadWolf1509/v4-ads-mcp/pull/25), [#26](https://github.com/BadWolf1509/v4-ads-mcp/pull/26)) — revisão servindo `v4-ads-mcp-00096-wtw`. Antes: 2026-08-20. **68 MCP tools em produção** (62 Google + 6 Meta), bucket **22 always + 46 defer** — contagem por grep de `bucket="` em `src/mcp/tools/*.py`. As duas mais novas são `get_ad_schedule` e `update_ad_schedule` (#31, sprint 3b.42), e o **smoke delas fechou 10/10 em 04/09** — foi ele que achou o F150 e o F151, os dois em código que três camadas de revisão tinham aprovado. O que segue **inteiro pendente** é o **3b.44** (`bid_modifier` por janela, #40): 7 testes, todos mutantes. A reclassificação de buckets de 04/09 (#32) moveu 13 para cima e 14 para baixo, com `detect_drift` mantido no always **por decisão do Wellington, contra a medição**; método, tabela e a data da próxima remedição (04/10) em [`tool-buckets-2026-09-04.md`](tool-buckets-2026-09-04.md). Smoke autenticado F58 segue dormente. F76/F77 encerrados. **Catálogo em 152 IDs**, com o **F147 aberto** (minor fail-safe: a reconsulta de confirmação do `apply_change` não tem sentinela de truncamento). **Mapa da sessão:** [`session-2026-09-02-03-handoff.md`](session-2026-09-02-03-handoff.md).

**Em 2026-09-02/03 entraram 16 findings (F131–F146), e os 16 estão fechados e em produção.** Vieram de uma sessão de campo (MO-JP) com evidência real, mais duas tools novas e a spec de `ad_schedule`. O que **muda de contrato** para quem consome as tools: (1) `freshness.status` em `get_change_history`/`detect_drift` é `confiavel | ambiguo | nao_coberto | em_curso | indeterminado` — `atrasado` não existe mais, e janela que alcança o dia corrente da conta nunca sai `confiavel`; (2) cada change traz `old_status`/`new_status` (CAMPAIGN/AD_GROUP), e `detect_drift` ganhou a flag `status_change_detected` (medium) — remover campanha no Google é `UPDATE` de status, não `REMOVE`; (3) `hoje` é o **da conta** em 24 tools Google (presets, clamp, freshness, `get_budget_pacing`, `get_negative_keywords_audit`); (4) `import_offline_conversions` usa o fuso da conta, o preview traz `summary.time_zone`/`utc_offset`, e conta sem fuso **recusa**; (5) `auto_applied_count` e `auto_apply_detected` passaram a ver `GOOGLE_ADS_RECOMMENDATIONS_SUBSCRIPTION`. ⚠️ **F140 vale para tudo isso:** schema e description novos só aparecem para sessão MCP reconectada; o comportamento é do servidor. Verificações vivas: Camaçari (`auto_applied_count` 0→1; `nao_coberto`), campanha `23861545627` removida (`structural_change` `PAUSED->REMOVED`, ontem `flags: []`), dry-run offline na `1163862076` (`America/Recife`, `-03:00`, token não aplicado — confirmado também pela sessão MO-JP). **Nota sem ID:** o classificador de blast radius não conhece `import_offline_conversions` e cai no *default seguro: confirmar* — correto, texto feio; entra junto se o F112 for revisitado.

- **F131-F135 fechados** (`38d890c`). `get_change_history` e `detect_drift` passaram a devolver **`freshness`** com duas fronteiras — a da conta (sonda própria em `asyncio.gather`, sem os filtros do usuário, porque herdá-los reproduziria a cegueira que ela mede) e a do recorte (`max` das linhas já devolvidas, custo zero). Status: `confiavel`/`ambiguo`/`atrasado`/`indeterminado`. **O lag do `change_event` não tem contrato** — medido de ~3h a >4 dias na MESMA conta —, então nenhuma description promete magnitude. `audit_competitor_keywords` passou a ler conversão (o `total_cost_wasted_brl` mantém o nome, com `total_conversions` ao lado). E **`_RESOURCE_TYPES` divergia do enum do Google em 13 posições**: faltavam 10, entre elas `AD`, que é o que a API emite ao editar RSA — filtro por anúncio devolvia zero com 20 edições no dia.
- **O gate do F138 foi validado ao vivo em 02/09**, nos três pushes do dia: merge (código) → deploy `success`; commit só de docs → deploy **`skipped`**; fix de código → deploy `success`. Commit de docs deixou de publicar revisão do servidor.
- **F136 e F138 resolvidos.** F136: apagar o membro morto **e** declarar o limite — apagar sozinho pioraria a cobertura em silêncio. F138: o `deploy` passou a exigir `code_changed`; **`paths-ignore` no `on:` foi verificado e seria pior**, porque travaria PR de docs com o `test` sendo required check.
- **F137 fecha a pendência 8** (`84c8720`): eram **4 CVEs reais** em `requirements.txt`, e o `continue-on-error` os tornava invisíveis.
- **Spec de 3 tools novas** ([`2026-09-02-ad-schedule-e-assets-design.md`](../superpowers/specs/2026-09-02-ad-schedule-e-assets-design.md)), aguardando revisão: `get_ad_schedule` + `update_ad_schedule` (semântica de **conjunto**, não incremento; dry-run com CPA e aviso de realocação em orçamento compartilhado), `get_assets` e `remove_asset_link` (só unlink).
- **Duas premissas de campo caíram na verificação, e valem para quem for mexer nisso:** o gasto de fim de semana da MO-JP, tratado como desperdício, tem **CPA R$ 18,59 contra R$ 23,59** de seg–sex; e vínculo de asset em nível de conta **não fica dormente** por existir vínculo de campanha — `primary_status` devolve `ELIGIBLE` nos dois, e o enum de razões não tem nenhum valor de precedência.

**`ad_schedule` saiu de spec para implementado nesta sessão, branch `feat/ad-schedule` (10 tasks, TDD, 8 Rulings em `.superpowers/sdd/2026-09-03-ad-schedule/progress.md`) — atualiza a linha acima, que dizia "aguardando revisão" pras duas tools.** `get_ad_schedule` (leitura da grade dia×hora por campanha — `has_schedule`, `hours_per_week`, `budget_is_shared`) e `update_ad_schedule` (grade **completa** por CONJUNTO — não incremento —, dry-run com CPA dos dois lados e aviso de realocação em orçamento compartilhado, sempre-CONFIRM); `apply_change` ganhou um branch pra essa operação, reconsultando a grade por GAQL pós-apply (§4.6) de forma best-effort (F83/F91 — reconsulta que falha nunca apaga o resultado de uma mutação já aplicada). **Não relacionado à onda de `freshness`/`change_event` (F131-F146) do parágrafo acima** — nenhuma das duas tools toca `get_change_history` nem `detect_drift`; é funcionalidade nova, não fix. Runbook de smoke em [`phase-3b-42-ad-schedule-smoke.md`](phase-3b-42-ad-schedule-smoke.md), **100% `⬜ pending`** — falta reconectar a sessão MCP antes de rodar (F140: catálogo negociado no handshake) e os passos de mutação (T3-T4, T6-T8) precisam do aval do Wellington numa conta real de teste. `ad_schedule` sai de "próximo sprint candidato" no `CLAUDE.md` para **entregue, smoke pendente** — fica só **M.5** como candidato.

**Três entregas em 2026-08-20, todas em produção ou a caminho dela:**

1. **Painel responsivo** (F118-F127) — revisão `00069-xpj`, em produção.
2. **F128** (contador de ausências no resync Meta) — migration `005`, em produção.
3. **Reconciliação da parceria Meta** — PR [#21](https://github.com/BadWolf1509/v4-ads-mcp/pull/21) mesclado (`b48922f`), migration `006`. **Sobe com `META_RECONCILE_APPLY=false`**: observa e conta, não revoga. Ver a pendência 1c.

**Dependências em dia — segunda rodada em 05/09, e ela corrige o registro da primeira.** Os quatro PRs abertos desde 03/09 ([#22](https://github.com/BadWolf1509/v4-ads-mcp/pull/22) `grpcio` + `grpcio-status` 1.83.1, [#23](https://github.com/BadWolf1509/v4-ads-mcp/pull/23) `cffi` 2.1.1, [#25](https://github.com/BadWolf1509/v4-ads-mcp/pull/25) `google-api-core` 2.34.0, [#26](https://github.com/BadWolf1509/v4-ads-mcp/pull/26) `google-cloud-storage` 3.13.1) foram mesclados **sem nenhum bypass**: os quatro estavam `BEHIND`, e `gh pr update-branch` → CI verde → merge resolveu os quatro. É a confirmação POR EXECUÇÃO do que a pendência 7 tinha estabelecido só por leitura da API — a causa nunca foi falta de revisor, é `strict=true` exigindo a branch atualizada com a base.
   **Verificação mais forte que a de agosto, e vale adotar como padrão:** em 20/08 a prova de que os bumps chegaram à imagem foi *ler o log do build*. Desta vez o digest do build (`033ff20e`) e o `spec.containers[0].image` da revisão servindo (`00096-wtw`) são **o mesmo `sha256:bb379600…`** — identidade, não leitura. O log confirma junto: `cffi-2.1.1`, `google-api-core-2.34.0`, `google-cloud-storage-3.13.1`, `grpcio-1.83.1`, `grpcio-status-1.83.1`.
   **Mecânica que custa tempo e é bom saber antes:** com `strict=true`, mesclar um PR deixa os outros `BEHIND` de novo — são N rodadas de `update-branch` + CI, não um lote. E logo após um merge o `mergeStateStatus` dos demais é **`UNKNOWN`**, não `BEHIND`; automatizar isso exige esperar o estado assentar e exigir que o `update-branch` tenha **movido o head**, senão você lê um check verde de um head anterior. Registro anterior (20/08): os 6 PRs parados desde julho — `fastapi` 0.139.2, `google-cloud-storage` 3.13.0, `google-api-core` 2.32.0, `uvicorn` 0.51.0, `cffi` 2.1.0 e `actions/setup-python` 7 — que **saíram por bypass de admin**, hoje sabidamente desnecessário.

**Em 2026-08-20 entrou a reconciliação da parceria Meta** (PR #21, 21 commits). O que muda de premissa, para quem for mexer no Meta depois:

- **`/me/adaccounts` não define mais o inventário.** A fonte autoritativa é a parceria do BM (`client_ad_accounts ∪ owned_ad_accounts`); `/me/adaccounts` alimenta só o sinal `su_reachable`. Isso separa duas coisas que antes eram indistinguíveis — *"saiu da parceria"* e *"ninguém atribuiu o system user"* — e as duas viravam ausência e `#200`.
- **O gate mudou nos dois lados.** `can_manager_access` (Meta, 20/08) exige conta ativa **e** grant não revogado. Sob o token de system user a Meta não nega nada por nós; o gate é a única fronteira. **O lado Google fechou o mesmo buraco em 05/09** (branch `feat/gate-google`, F130 no catálogo) — mesmo par de condições, mesma revogação soft com restauração restrita ao churn. O que falta no Google, e é sprint seguinte, é a reconciliação automática (nenhum job compara o inventário contra o `customer_client` do MCC ainda) — ver pendência 10.
- **Revogação é soft**: `revoked_at` + `revoked_reason`. O painel restaura com um clique quando a parceria volta, e **só** o que foi revogado por churn — acesso que um admin tirou de propósito não volta.
- **A trava `META_RECONCILE_APPLY` governa destruição, não observação.** Upsert, carência e alcance escrevem mesmo com ela desligada; senão o dry-run não observaria nada.
- **`/admin/accounts/meta` tem três filas**, cada uma uma ação diferente: *Aguardando delegação*, *Sem o system user atribuído*, *Saíram da parceria*.
- **Medido em produção antes do fix:** parceria 25, alcance do SU 23, inventário 24 → 1 saída (Petrolina, 4 grants) e 2 entradas, uma delas `CA - V4 Lima Soares`, a conta da própria V4, que nunca entrou no MCP por falta de atribuição do SU.

Detalhe: [spec](../superpowers/specs/2026-08-20-meta-partnership-reconciliation-design.md) · [plano](../superpowers/plans/2026-08-20-meta-partnership-reconciliation.md).

**Em 2026-08-20 houve a revisão de responsividade do painel** (F118-F127, todos fechados — commits `79e67d9` + `9d48437`). As 24 telas foram renderizadas fora do app com dados realistas e medidas em 8 larguras (320→1280px): **9 rolavam na horizontal em 375px** — a pior, `/admin/audit`, com +751px, e ela estourava **até em 1024px** — e o header estourava **todas** elas na faixa **768-799px** (iPad em retrato, só para admin com e-mail longo). Depois do fix: **192/192 medições limpas**. O que muda de premissa no painel:

- **Toda `<table>` vive num contentor de scroll, sem exceção.** As duas exclusões antigas (sticky-head e dropdown) caíram: `v4-table--sticky-head` usa também `.v4-table-wrap--wide`, que desliga o scroller acima de 1200px e devolve o sticky do F97; e o dropdown dentro de scroller **se desancora sozinho** (`v4DesancorarMenu`, `position: fixed` via CSSOM, fecha no scroll). Contentor novo exige `tabindex="0" role="region" aria-label` — sem foco, Firefox e Safari não rolam por teclado (F125).
- **O layout de página é `body > main`**, não `main`. No seletor de elemento a regra caía em `<main>` aninhado e o `margin: 0 auto` dela cancelava o stretch do flex (F121). Só `_base.html` pode abrir `<main>`; guard cobra.
- **`.v4-sparkline` e `<code>` inline** ganharam contenção no design system: o Preflight do Tailwind dá `max-width` a `img`/`video` e **não** a `svg` (F120), e caminho de arquivo em `<code>` é palavra única sem ponto de quebra (F126).
- **O e-mail do gestor trunca no header** (F124). Foi o único dos três fixes medidos que não muda a **altura** do header — os offsets sticky do F79 dependem dela.
- Guard novo em [`test_frontend_responsive_guards.py`](../../tests/unit/test_frontend_responsive_guards.py): 11 testes que **derivam** os alvos do source e mascaram comentário antes de varrer (o `<main>` citado no comentário que explica o fix derrubou o próprio teste — 5ª vez dessa armadilha no repo).

Detalhe e método: seção **Revisão de responsividade 2026-08-20** do [`findings-catalog.md`](findings-catalog.md) (não há handoff separado desta sessão).

**Em 2026-08-19 houve tres investigações**: frontend (F101-F108), backend (F109-F112) e infra/CI (F113-F117), **todas fechadas**. Da infra/CI: o `ci.yml` ensinava a regenerar o lockfile **sem `--universal`**, comando que quebra o build Linux (F113); os 3 Cloud Run Jobs precisam dos 8 campos obrigatórios de `Settings` e o `deploy.yml` passou a declará-los com `--update-*` (merge), com guard (F114); o gate local ganhou o check do Tailwind que só existia no CI (F115); e o rollback ancorou na revisão que **estava servindo**, não na ordem de criação (F116). Detalhe: [`session-2026-08-19-infra-ci-handoff.md`](session-2026-08-19-infra-ci-handoff.md). Do backend, o que muda de premissa: **toda** chamada de SDK que atende request sai do event loop por [`src/blocking.py`](../../src/blocking.py) — o helper saiu de `google_ads/` porque agora serve o Meta também, e ganhou o guard que o F86 não tinha (F109); `get_my_rate_limit_status` devolve `manager` + `account` + `blocking_scope`, não mais chaves planas (F110); o audit Meta grava a conta do kwarg obrigatório (F111); e a política de blast radius, embora consultiva em 17 das 26 tools, está amarrada por teste derivado do source (F112). Detalhe: [`session-2026-08-19-backend-handoff.md`](session-2026-08-19-backend-handoff.md).

Do frontend (8 findings, F101-F108, todos fechados): O que mudou de premissa no painel: o rótulo acessível dos checkboxes da matriz vem por **`aria-labelledby` apontando pra fora do nó trocado** (F101); **toda** referência a `/static` carrega `?v=` (F102); a isenção de CSRF é **por rota**, não por prefixo — `/oauth/meta/revoke` e `/oauth/meta/refresh-accounts` deixaram de ser isentos (F106); `sessions_revoke` sem HTMX devolve **303** (F107). Detalhe: [`session-2026-08-19-frontend-handoff.md`](session-2026-08-19-frontend-handoff.md).

**A sessão 2026-08-14/15 foi uma investigação ampla de bugs** (19 findings catalogados, **todos fechados** em duas ondas). O núcleo mudou de comportamento em pontos que valem saber de cara:

- Bookkeeping em `finally` não derruba mais a operação (`best_effort`, F83); chamada do SDK Google roda **fora do event loop** (`run_blocking`, F86); escape GAQL usa barra invertida (`_gaql.py`, F87).
- Gates de sessão usam `Manager.is_deactivated` (F84); pool caiu pra **5** conexões/instância (F92); jobs auditam crash e inventário parcial (F93).
- Tools Meta paginam e devolvem `truncated` (F88), e **não devolvem mais** `effective_status`/`creative_id`/`daily_budget_brl`/`billing_event` (F89).
- httpx silenciado (as linhas `HTTP Request:` sumiram do Cloud Logging de propósito) **e o token Meta saiu da query string** — vai em `Authorization: Bearer`, inclusive em cada página da paginação (F82).

Detalhe e lições: [`session-2026-08-14-15-handoff.md`](session-2026-08-14-15-handoff.md).

**Frontend pós 2026-08-11** (o que mudou de premissa): **Tailwind não é mais CDN** — CSS gerado offline e commitado, com guard de diff no CI. **A CSP não tem nenhuma diretiva `unsafe-*`**: zero JS e zero CSS inline nas templates; comportamento em `v4-panel.js` via `data-v4-*`, estilo via classe. Assets com gzip + `Cache-Control` imutável versionado por `K_REVISION`. `domContentLoaded` do `/login`: **1128 ms → 261 ms**.

**Sessões recentes** (detalhe canônico nos handoffs — leia só o da sessão relevante):
- **2026-08-20** — três frentes no mesmo dia, sem handoff próprio (o registro está no [`findings-catalog.md`](findings-catalog.md)): **responsividade** do painel medida em 8 larguras (F118-F127, 192/192 limpas depois do fix); **F128**, o contador de ausências; e a **reconciliação da parceria Meta**, que substituiu o contador por comparação contra fonte autoritativa e fechou o offboarding automático (PR #21, 21 commits, revisão `00071-q4v`).
  **Como foi feito, porque a lição é de método:** 8 tarefas com implementador e revisor independentes por tarefa, mais revisão da branch inteira. **Os achados mais graves foram defeitos do plano, não da implementação** — função renomeada sem varrer os callers (quebraria o job de produção com `ImportError`), código de teste que não compila, inventário lido depois do upsert (deixando `added` sempre 0) e duas falhas de costura que só a revisão final viu. Nenhum deles aparece em teste com mock.
- **2026-08-14/15** — investigação ampla de bugs sem escopo prévio: 19 findings catalogados (F82-F100), **todos fechados** em duas ondas (11 + 8). Núcleo, auth, jobs, Meta, pool, painel e backup tocados. [`session-2026-08-14-15-handoff.md`](session-2026-08-14-15-handoff.md).
- **2026-08-19** — três investigações. **Infra/CI**: 5 findings (F113-F117) [`-08-19 infra`](session-2026-08-19-infra-ci-handoff.md). **Frontend**: 8 findings (F101-F108), os três mais graves em pontos cegos de guards **verdes** [`-08-19 front`](session-2026-08-19-frontend-handoff.md). **Backend**: 4 findings (F109-F112) — o F86 tinha sido fechado **sem guard**, e 3 caminhos que servem request seguiam bloqueando o event loop [`-08-19 back`](session-2026-08-19-backend-handoff.md).
- **2026-08-11** — frontend medido no DOM de produção: Play CDN aposentado, CSP sem `unsafe-*`, a11y, +F78-F81. [`-08-11`](session-2026-08-11-frontend-handoff.md).
- **2026-07-22/23** — 500 e 503 intermitentes por conexão asyncpg stale → F76 (`run_with_reconnect`) + F77 (deep health resiliente) + `severity` no Cloud Logging. [`-07-22`](session-2026-07-22-handoff.md) · [`-07-23`](session-2026-07-23-handoff.md).
- **2026-07-04** — 3 ondas de governança/dívida (F73 quota leak + cap por gestor; `_mutate_common`; lockfile; backup) e, na 2ª sessão, o pacote UI/UX do painel (F74/F75). [`-07-04`](session-2026-07-04-handoff.md) · [`-07-04 UI`](session-2026-07-04-ui-ux-handoff.md).
- **2026-07-02** — pós-migração: cutover (F68/F70), jobs CNB (F66), scheduler (F69), gate Meta obrigatório (F72), deploy gated pelo CI. [`-07-02`](session-2026-07-02-handoff.md).
- **2026-06-30** — **migração GCP** pro projeto próprio (chaves regeneradas, token Meta all-targets). [`-06-30`](session-2026-06-30-handoff.md).
- **Anteriores:** 06-20 (observabilidade + M.4 + Fase 2A) · 06-19 (recuperação de conta, F61-F63) · 05-29 (hard-gate + CSP).

**O que existe:** Foundation (Phases 0-1b/3a) + 40 sprints Google (3b.1→3b.40) + **Fase 2A** (`get_performance_breakdown` consolida 8 reports, aditivo) + família Meta (M.1→M.4 breakdowns) + camada de acesso/segurança (Modelo B + hard-gate + CSRF/CSP) + governança (audit sempre em mutates, **incl. Customer Match desde 07-02**) + **deploy gated pelo CI** + **painel web endurecido** (2026-08-11: sem CDN de CSS, sem JS/CSS inline, CSP sem `unsafe-*`, assets comprimidos e cacheados). Detalhe: [`sprint-history.md`](sprint-history.md) + handoffs em `docs/operacao/`.

**Tokens válidos:** v4-ads Bearer (procedure abaixo; **Bearers antigos seguem válidos** pós-migração — validados por hash no DB compartilhado). Meta system-user token **all-targets** no secret `meta-system-user-token` (app *V4 Ads MCP* `1522411803012799`, SU `v4-ads-mcp-integracao` `61590110716028`, não expira). Meta OAuth pessoal do Wellington **reconectado em 11/08/2026** (audit `meta_oauth_connect` 23:35) — válido até **10/10/2026**. Continua dormente/opcional: as tools Meta rodam com o system-user token (Modelo B).

**✅ IAM GCP (resolvido 2026-06-30):** o projeto novo `v4-ads-mcp` tem **Wellington como owner** — lê/grava secrets, Cloud Run, jobs, rollback direto via gcloud (`gcloud ... --project=v4-ads-mcp`, autenticado `wellington.ribeiro@v4company.com`). **Não depende mais de Org Admin V4.** (O antigo `v4-ads-mcp-prod` seguia sem owner humano — motivo da migração; a decomissionar.)

**Próximo sprint candidato: M.5** (Meta `meta_get_audience_performance` + `meta_get_top_creatives`) — alimenta o volume do checkpoint Meta e é o próximo do roadmap ([`specs/2026-05-24-meta-ads-incorporation-design.md`](../superpowers/specs/2026-05-24-meta-ads-incorporation-design.md)). **Fase 2B** (tombstone dos 8 reports — §4.1 do [refactor design](../superpowers/specs/2026-05-25-architecture-refactor-design.md)) fica **bloqueada no soak REAL**: em 07-02 os 8 antigos seguem em uso ativo e `get_performance_breakdown` está parado desde 06-24 — steering feito (nota de deprecação nas 8 descriptions), mas só tombstonar quando os gestores migrarem (re-checar `audit_log`).

**Decision gates** (remedidos em **15/08** direto no `audit_log`: 2890 eventos totais desde 04/05; 1242 em 30d, 667 em 15d, 3 gestores ativos): **checkpoint volume Meta** — **221 chamadas/15d** contra a régua de 500/15d (390/30d, 574/90d). Não bate, mas cresceu: a projeção de 11/08 era ~172/15d. **86% do volume Meta é de um único gestor** (ver abaixo) · **soak Fase 2A→2B** → **NÃO tombstonar**: 82 chamadas aos 8 antigos contra 8 do `get_performance_breakdown` em 15d (10,2:1; 147×9 em 30d). **Achado que muda a estratégia:** `get_campaign_performance` sozinho é **179 dos 258** usos dos antigos em 90d (69%) e `get_hourly_performance` tem **zero** — não são 8 tools pra migrar, é essencialmente **uma**.

**Quem realmente usa (15/08, direto no DB — o registro anterior estava errado):**
- **`pedro.vytor@v4company.com`** é o usuário PRINCIPAL: 1659 eventos totais (mais que o admin), 943 em 30d, 335 das 390 chamadas Meta. Criado em 19/05. **Não constava nesta doc até 15/08** — o Gate 1 é movido por ele.
- **`wellington.ribeiro`** (admin): 1063 eventos totais, 204 em 30d.
- **`anderson.cordeiro`**: **1 evento no total**, 3 sessões MCP ativas. Tem Bearer, praticamente não usa.
- **`lucassoares`**: **0 eventos**, `last_seen_at` vazio — mas tem **1 sessão MCP ativa**. Ou seja, ele ENTROU no painel e emitiu um Bearer (só a UI emite, e só o próprio gestor); o que nunca houve foi uso. Não é "falta onboardar" — é token vivo sem uso, que é decisão diferente (revogar ou confirmar intenção).

**Inventário Google em 04/09: 26 contas ativas** (era 25). A `Hust App` (`948-545-9729`, `America/Araguaina`) entrou no MCC e só apareceu quando o resync foi disparado à mão — o job é diário às 06:00 BRT e ela chegou dentro da janela. **Delegada em 04/09** — hoje tem 4 grants `write` (anderson.cordeiro, lucassoares, pedro.vytor, wellington.ribeiro) e as **26 contas ativas têm pelo menos um grant**, medido. Vale guardar o que ela expôs enquanto esteve sem: sincronizar **não** concede acesso, então conta nova entra alcançável por ninguém. 🔑 **Lacuna de operação que segue aberta:** nada avisa quando entra conta nova sem acesso — foi descoberta por acaso, olhando o seletor de contas do Google. O lado Meta já tem a fila *Aguardando delegação* no painel; o Google não. Com ela, o MCC passa a seis fusos (entrou `America/Araguaina`, UTC−3).

**Smoke 3b.42 em 04/09: 5 de 10.** T1, T2, T2b, T3 e T9 PASS; T4, T7 e T8 mutam a `1163862076` e seguem pendentes de aval, e T5/T6 dependem do T4. O dry-run do T2b provou a §4.2 contra dado real e **mudou a decisão da conta**: na JPA o que sai é melhor (CPA 18,96 contra 19,87), na CAB o que sai é pior (21,06 contra 19,10) — mesma janela, direções opostas, que é o número que o agregado de conta esconde. 🔑 **Lição de planejamento:** o classificador de auto mode recusa smoke de tool mutante sem o gestor autorizando **na sessão dele**; aval relayado por sessão-par não passa, e nem dry-run nem conta de teste isentam.

**F148 (HIGH) — CORRIGIDO e VERIFICADO EM PRODUÇÃO em 04/09.** A auditoria de tentativa de escrita estava invertida: as 24 tools que chamam `create_pending` não gravavam linha própria no dry-run, e o `ensure_account_access` que ele chama com `level="write"` audita **só quando nega** — a trilha guardava os previews **recusados** e perdia todos os que **funcionavam**. Fix em dois PRs. O **#33** pôs a gravação no `create_pending` (sítio único: `generate_token` e o INSERT existem só em `dry_run.py`), na mesma transação da pendência, com migration `007` aditiva e coluna nullable. O **#35** expôs `dry_run` no `list_for_manager`, porque a Parte 1 deixou a **leitura** ambígua: preview e mutação aplicada gravam os mesmos valores em `action_type` e `target_count`, e o único diferenciador acidental (`duration_ms` NULL) **não é exclusivo** — `admin_access_grant` também grava `mutate` com duração nula. 🔑 **Medição na `7862230676`, com aval do Wellington:** antes, os dois dry-runs deixavam só `4065`/`4066`, `read` com `target_count: 0`, enquanto a resposta reportava 10 e 5; depois, `4085` e `4087` com `mutate` e o `target_count` planejado. Perguntado *quantas mutações foram tentadas nesta conta*, o log responde **2** em vez de zero.

**F149 (MEDIUM, EM PRODUÇÃO desde 05/09 — smoke 3b.44 pendente) — `bid_modifier` por chamada, não por janela.** Histórico: `get_ad_schedule` **lia** modificador por janela, mas `update_ad_schedule` só aceitava um por chamada — mudar UMA faixa exigia duas chamadas com um estado intermediário em que a campanha servia ~50 de 168 horas (sem ordenação segura; inundava a irmã em orçamento compartilhado). A metade de **bug** fechou pelo **#34** (preview com `cobertura` e `bid_modifier` antigo ao lado do novo). **A feature por janela fechou** no PR [#40](https://github.com/BadWolf1509/v4-ads-mcp/pull/40) (merge `0162017`, revisão `v4-ads-mcp-00092-sl2`; Tasks 1-6): `windows[].bid_modifier` vence o escalar da chamada, que vira default; `Window.key()` continua com 5 posições (o modificador não entra na identidade, senão mudá-lo recriaria o criterion e queimaria ~14 dias de re-learning). A revisão final da branch achou e corrigiu, no mesmo commit, um Critical que a própria feature expunha: `bid_modifier` é `proto.FLOAT` (32 bits) no SDK v24, e comparar por `==` o pedido (float64) contra o devolvido pelo Google nunca converge (`1.4` volta `1.399999976158142`) — fix via `bid_modifier_diverge` (`math.isclose(rel_tol=1e-6)`, NÃO aplicado em `schedule_fingerprint`). Um Important junto: `matches_requested` pós-apply não conferia o modificador, só a identidade da faixa — fixado com `windows_bid_modifiers`, chave paralela a `windows` no payload pendente. 1621 testes unitários verdes (10 novos, cobrindo os dois sentidos da tolerância e das duas checagens). **Falta:** smoke 3b.44 contra conta real (`docs/operacao/phase-3b-44-bid-modifier-smoke.md`, T1-T7), pendente do Wellington autorizando cada rodada de mutação na própria sessão que executa (mesma restrição medida no 3b.42/3b.43) — a cobertura acima é só unitária, não leia como verificado em produção.

**Partição horária EM PRODUÇÃO** (PR #39, revisão `v4-ads-mcp-00091-xvw`; smoke 3b.43 executado **6/6**, com o T1 fechando ao centavo contra fonte independente). Desenho em [`2026-09-04-particao-horaria-design.md`](../superpowers/specs/2026-09-04-particao-horaria-design.md). Partição como default (3 blocos por campanha), grade crua só sob flag — 168 células por campanha estouram o `default: 100` do `get_performance_breakdown` **antes de terminar uma campanha**. Geo por cidade ficou fora: o mesmo município tem dois `geoTargetConstant` e o canônico não colapsa.

🔴 **F150 (HIGH) — `update_ad_schedule` foi para produção capaz de PREVER e incapaz de APLICAR.** Achado no **T4 do smoke**, em conta real, com a tool no ar desde o PR #31. O `apply_change` devolvia só *"Erro interno"*; a mensagem real (`No mutate builder registered`) vivia apenas no audit log. Falhou em 100 ms sem chegar no Google, então a conta ficou **intacta**. Causa: o `import_all_builders` mantinha uma lista de imports à mão e `mutates/ad_schedule.py` não estava nela — 12 módulos no pacote, 11 na lista. Fix no PR #36: varredura do pacote por `pkgutil` em vez de lista, `suppress(ImportError)` vira `log.exception`, e guard AST que exige que toda chave declarada chegue ao registry. 🔑 **O que isso ensina, e é maior que o bug:** três camadas de revisão (por task, da branch inteira, e escopada) passaram por cima, porque **revisão olha código escrito e o defeito era código ausente**. Nenhum sprint de tool mutante deveria fechar com o passo de apply em `⬜ pending` — o smoke achou em uma tarde o que a revisão não achou, porque ele executa em vez de ler.

**Smoke 3b.42 parado no T4.** T5-T8 dependem do estado que o T4 deixaria e não foram tentados. Quando o #36 subir, a cadeia recomeça do T3 (o token tem TTL de 10 min, então cada rodada minta um novo) — e **cada rodada precisa do aval do Wellington na sessão que executa**: aval relayado por sessão-par não passa no classificador, medido em 04/09.

**Smoke 3b.42 FECHOU 10/10 em 04/09.** T4 confirmou o F150 corrigido em campo com os dois sinais certos: `provider_request_id` presente (a tentativa falhada não tinha) e `duration_ms: 871` contra os 100 ms de quando nem saía da máquina. T5 conferiu por `criterion_id`, T6 deu `no_changes` sem token, T7 provou field mask nos mesmos 5 ids, e T8 restaurou o 24×7 natural. Conta verificada limpa por dois instrumentos.

**F151 (HIGH, corrigido pelo PR #37) — achado no T8.** O `cobertura` que o PR #34 introduziu dizia que `clear_schedule` leva a entrega a **zero**, quando ele a **restaura** para 168 h/semana; a mesma resposta se contradizia, com `resulting_schedule` trazendo 168.0. E a rota de restauração ainda disparava o aviso de realocação de gasto. Inversão de propósito: o bloco existe para ninguém desligar entrega sem ver, e na única operação que religa entrega ele anunciava perda total.

**F152 (LOW, corrigido pelo PR #38)** — o rótulo por item era fixo em `"added"` mesmo em `update`/`remove`. Número certo, nome errado. Passou a ser `"success"`, neutro — a camada genérica não conhece o verbo, que só existe do lado da requisição. 🔴 **A renomeação do campo foi considerada e RECUSADA** (40 referências em 14 arquivos, contrato de toda tool de mutação, ganho de trocar um nome ambíguo por outro); não reabrir sem argumento novo. Junto vai a nota de que `bid_modifier` volta do Google em float32 (`1.100000023841858`), então comparação tem de ser por tolerância.

**Pendente operacional (AÇÕES HUMANAS — runbook detalhado no handoff 2026-07-02):**
1. **`lucassoares`: token vivo sem uso — decidir revogar ou confirmar intenção.** Remedido em 15/08 e o registro anterior ("nunca onboardou") estava errado: ele tem **1 sessão MCP ativa**, logo entrou no painel e emitiu um Bearer. O que não existe é uso — `last_seen_at` vazio e **0 eventos desde 05/05**. Como o Bearer não expira sozinho, a pergunta não é mais "como ajudo ele a entrar", e sim **"ele vai usar?"** — se não, revogue a sessão em `/sessions` (credencial viva sem dono ativo é superfície à toa, com 34 grants Google + 28 Meta atrás dela). `anderson.cordeiro` está no mesmo padrão em menor grau: 3 sessões ativas e **1 evento no total**.
1c. ~~**Soak da reconciliação, e depois virar a chave.**~~ **CHAVE VIRADA em 2026-09-05** (PR [#41](https://github.com/BadWolf1509/v4-ads-mcp/pull/41), merge `66a0e05`, revisão `v4-ads-mcp-00097-m7x`). ⚠️ **A revogação ainda NÃO aconteceu quando esta linha foi escrita** — ela cai na execução das **09:00Z de 05/09**; até lá o estado é "armado", não "aplicado". Confira o `audit_log` depois disso.
   - **O soak respondeu o que tinha de responder.** 16 execuções desde 2026-08-21, todas `success` e `applied: false`, e a **primeira bateu a previsão nos quatro números** (`added=2`, `bumped=1`, `unreachable=2`, `removed=0`). A carência aparece funcionando: `bumped=1` em 08-21 e 08-22, e desde 08-23 são 14 execuções seguidas com **`removed: 1` estável**. Confirmação independente do contador: o `synced_at` da conta candidata está congelado em **08-19** enquanto as outras 25 ativas sincronizaram em 09-04 — ausência real da lista autoritativa, não bug de contador.
   - **Raio medido:** 1 conta e 4 grants — `act_468463369497370` (Petrolina), grants `write` de `anderson.cordeiro`, `lucassoares`, `pedro.vytor` e `wellington.ribeiro`. O aviso original valeu: a carência **já estava cumprida**, então cai na primeira execução seguinte, sem segundo período de graça.
   - 🔑 **NÃO entram `CA - V4 Lima Soares` e `CHUTE 07`**, que estão `su_reachable=false` com **4 grants vivos cada**. As duas têm `missed_syncs=0` e sincronizaram em 09-04 — estão *na* parceria, e o sinal delas é "SU não atribuído", fila distinta de churn. São 8 grants que dependem dessa distinção; se algum dia forem revogados, o bug é aí.
   - 🔴 **Por que a chave foi virada no `deploy.yml` e não por `gcloud`:** `JOB_ENV_VARS` reescreve a chave nos três jobs **a cada deploy**, então `gcloud run jobs update` seria revertido em silêncio no push seguinte (em 05/09 foram sete deploys). O valor no **serviço** é inerte — só `src/jobs/meta_resync.py` lê a flag —, mas acompanha, com o motivo escrito no workflow. **Verificado por leitura do env real dos 3 jobs pós-deploy**, não pelo arquivo: o estado deles não é enumerável a partir do repo (F95).
   - **Para desligar:** volte a chave para `false` no `deploy.yml`. A revogação é soft (`revoked_at` + `revoked_reason`) e o painel restaura com um clique — e só o que foi revogado por churn.

1b. **`Mestre da Obra Petrolina` (`act_468463369497370`): revogar os grants.** ⏳ **ARMADA em 05/09, não mais manual** — com a 1c virada, o reconciliador revoga os 4 grants na execução das 09:00Z de 05/09. **Só marque como resolvida depois de ver a linha no `audit_log`**; até lá isto está agendado, não feito. Se a execução passar e os grants continuarem vivos, o problema é o reconciliador e esta pendência volta a ser manual. A parceria com a V4 Lima Soares caiu e o system user perdeu o acesso — verificado em 20/08 pelo Graph (`#200 ... has NOT grant ads_management or ads_read`). O inventário agora se desativa sozinho em ~3 dias (F128), mas **isso não revoga acesso**: `can_manager_access` lê só a matriz. Enquanto os grants existirem, qualquer gestor que tenha a conta continua "com acesso" — hoje só colhe erro #200, mas se a parceria voltar o acesso se restabelece sem ninguém re-autorizar. Revogue em `/admin/access/meta`.
2. **Decomissionar `v4-ads-mcp-prod`** (só após cutovers — o scheduler antigo ainda cobre o resync até lá, agora redundante) · **revogar** o token BTW temporário do Wellington.
3. **F67 custom domain** `mcpv4.fluxocerto.dev.br` via Load Balancer (southamerica-east1 não permite domain mapping direto; domínio já verificado).
4. ~~**Atribuir o SU** às contas Meta faltantes~~ — **ENCERRADO em 15/08, e não como estava escrito.** A lista de 4 vinha dos erros #200 observados em 05/08, sem os nomes. Ao resolvê-los, nenhuma precisava de atribuição: `Mestre da Obra - Pinda` e `CA - ML Antiguidades` são **ex-clientes** da unidade; `WJX Construções` tem `account_status=101` (**conta fechada** no Meta); e `Wellington Ribeiro` é **conta pessoal sem business** — conta pessoal não pode receber system user, que vive dentro de um BM. A única conta real do problema (`CA - MDO João Pessoa`, `act_27798855556414269`) **não estava na lista** — apareceu só ao buscar os nomes — e foi **atribuída pelo gestor em 15/08**, verificada nas 3 camadas (GET no node, inventário `/me/adaccounts`, leitura de insights). O SU alcança **24 contas**.
   **Limpeza feita (autorizada, 15/08):** as 4 saíram do inventário (`is_active=false`) e **16 grants foram revogados** (4 contas × 4 gestores), com linha `meta_access_cleanup` no `audit_log`. O cache passou a bater com a realidade: **24 ativas, 24 alcançáveis**.
   **Duas lições que valem além deste item.** (a) `can_manager_access` **NÃO consulta `is_active`** — só a tabela de grants. Desativar no inventário sozinho não remove acesso nenhum; quem manda é o grant. (b) ~~O resync **não** limpa isso sozinho~~ — **reescrito duas vezes em 20/08.** Primeiro o F128 pôs um contador de ausências; depois a reconciliação o substituiu por inteiro. Hoje o job **compara o inventário com a lista autoritativa da parceria** (`client_ad_accounts ∪ owned_ad_accounts` do BM da V4) e, ao cruzar a carência de 3 execuções completas, **desativa E revoga os grants (soft)** — não sobra ação manual. **A lição (a) mudou de status:** `can_manager_access` **passou a exigir** conta ativa e grant não revogado, porque sob token de system user o gate é a única fronteira que resta. O que continua manual, de propósito, é o sentido oposto: **conceder**. Conta que entra na parceria aparece em `/admin/accounts/meta` na fila *Aguardando delegação*, sem nenhum acesso, até um humano delegar.
5. **Revisar as 4 skills `v4-trafego-google-ads`** no claude.ai (fora do repo) pra apontarem `get_performance_breakdown` · **decisões:** checkpoint Meta (só 43 calls) + soak da Fase 2B.
6. **Smoke F58 dormente** (gestor smoke excluído 07-14) — deploys OK via fail-well; re-armar recriando gestor smoke sem grants + repor `SMOKE_MCP_BEARER` no GitHub se quiser a proteção ativa (memory `ci-smoke-bearer-dormant-2026-07`).
7. **~~Branch protection exige revisão num repo sem revisor~~ — DIAGNÓSTICO ERRADO, corrigido em 02/09.** A pendência dizia que a `main` exige aprovação, que num repo solo ela nunca vem, e que por isso **todo** merge sai por `--admin`. **Medido pela API em 02/09, é falso:** `required_approving_review_count` é **0**. A regra exige que exista um *pull request* e que o check `test` passe — **não exige aprovação de ninguém**. O [PR #27](https://github.com/BadWolf1509/v4-ads-mcp/pull/27) (32 commits, F131-F138 + as tools de asset) foi mesclado **sem `--admin`**, com `mergeStateStatus: CLEAN`, o que fecha a questão empiricamente. Estado real: `required_approving_review_count=0`, `contexts=['test']`, `strict=true`, `enforce_admins=false`.
   **O que de fato aconteceu nos dois casos que geraram esta pendência, e são causas diferentes:**
   - **19/08, os 6 PRs do Dependabot recusados:** a causa é `strict=true`, que exige a branch **atualizada com a base**. Aquelas estavam atrás da `main`. O remédio ali é `gh pr update-branch`, não bypass — e a pendência propunha desligar o `required reviews`, que já estava efetivamente desligado, então a "correção" não teria mudado nada. **Confirmado por execução em 05/09:** os quatro PRs do Dependabot então abertos estavam `BEHIND` e foram mesclados com `update-branch` + CI, **zero bypass**. O diagnóstico deixou de ser inferência sobre a API e passou a ser resultado.
   - **02/09, o `Bypassed rule violations` nos 4 commits de catálogo:** aquilo **não** foi merge de PR, foi **push direto na `main`**. Push direto viola "changes must be made through a pull request" legitimamente, e passa porque `enforce_admins=false`. É o único bypass real dos dois.
   **A decisão que sobra é outra, e mais estreita:** manter `enforce_admins=false` (push direto na `main` continua possível — conveniente para hotfix, e é o que torna a regra de PR não-vinculante para o dono) ou ligá-lo e aceitar que todo trabalho passe por PR, inclusive emergência. Não há mais nenhuma questão sobre revisão: ela não é exigida. **Lição de método:** esta pendência viveu duas semanas propondo desligar um controle que já estava desligado, porque a leitura de `required_pull_request_reviews` como "ativo" nunca foi acompanhada da leitura do `required_approving_review_count` dentro dele. Presença do bloco ≠ exigência de aprovação.
8. ~~**`pip-audit` é `continue-on-error` — CVE em dependência não bloqueia nem avisa.**~~ **RESOLVIDA em 02/09** (F137, commit `84c8720`, em produção desde 02/09 via PR #27) — e o silêncio previsto aqui já estava acontecendo: havia **4 CVEs reais** (`aiohttp` ×3, `cryptography`) em `requirements.txt`. Escolhida a **2ª das três opções abaixo** — informativo com alerta visível: o passo perdeu o `continue-on-error`, trata os próprios erros (nunca falha, inclusive se a instalação falhar) e escreve o resultado no `$GITHUB_STEP_SUMMARY`, que é onde alguém lê. Segue non-blocking de propósito. Registro original: Os 6 PRs do Dependabot ficaram abertos de 19-26/07 a 19/08 (um mês), todos com CI verde, e **nada sinalizou**. Se algum tivesse sido bump de segurança, o silêncio seria o mesmo. Decidir entre: tornar o `pip-audit` bloqueante (risco de travar o CI por CVE em dep transitiva sem fix), mantê-lo informativo mas com alerta (o canal de e-mail do Monitoring já existe), ou deixar como está e assumir revisão manual periódica dos PRs do Dependabot.
9. ~~**Primeira execução do backup com `google-cloud-storage` 3.13.0 é no domingo seguinte a 19/08.**~~ **VERIFICADA em 03/09, positiva.** Duas execuções na 3.13.0 — `wpppk` (23/08) e `qqfc4` (30/08) — ambas `SUCCEEDED_COUNT 1`, numa sequência de 6 domingos sem falha desde 26/07. Bucket com os dois prefixos, 13 tabelas cada, carimbo 08:00Z (= 05:00 BRT, batendo com o cron). **Não é só "sucedeu":** o `audit_log.csv.gz` cresce monotonicamente (96,5 → 105,8 → 118,1 KB), e o `_migrations.csv.gz` saltou de 167 para 228 bytes entre 16/08 e 23/08 — a migration `006` (PR #21, 20/08) aparecendo no dump. Baixei o de 30/08: **gunzip limpo, header + 6 migrations, `001`…`006`**, todas com `applied_at` correto. 🔴 **O que isto NÃO verifica, e não tem como verificar por observação:** o caminho que o F94 testou na 3.12.0 é o de **falha** — `BlobWriter.__exit__` chamando `terminate()` pra cancelar o upload resumable e não deixar `.gz` truncado. Esse caminho só dispara se o job falhar, e nenhum falhou. O happy path da 3.13.0 está provado; o exception path segue verificado só na 3.12.0. Exercitá-lo exigiria induzir uma falha de propósito — decisão à parte, provavelmente não vale. Registro original: O F94 verificou empiricamente que `BlobWriter.__exit__` chama `terminate()` na exceção — e cancela o upload resumable, evitando `.gz` truncado no bucket. Essa verificação foi feita **na 3.12.0**; o bump do PR #18 subiu pra 3.13.0. O caminho não é exercitado por teste (o job roda semanal), então a próxima execução é a primeira confirmação real. Se o backup falhar, comece por aí.

11. ~~**F136 — `detect_drift` promete vigiar remoção de conversion action e não consegue ver nenhuma.**~~ **RESOLVIDA em 02/09** — decidido e corrigido: membro morto removido, limite declarado na description (nem `change_event` nem `change_status` rastreiam conversion action; `get_conversion_actions` para o estado), e guard que cruza o conjunto com o enum do SDK. Registro original: `_STRUCTURAL_RESOURCE_TYPES` guarda `CONVERSION_ACTION`, que **não existe** em `ChangeEventResourceType` (ausente do enum do SDK e rejeitado pela API em `WHERE`). A flag `structural_change`, severity **high**, só pode disparar em `CAMPAIGN` e `AD_GROUP`, enquanto a `message_pt` entregue ao gestor promete os três. Remover conversion action quebra Smart Bidding e o gestor acredita estar coberto. **Não corrigido de propósito: o fix óbvio é o errado** — tirar o valor do set deixa o código honesto e a cobertura pior, calada. As opções reais (ajustar a mensagem apontando `get_conversion_actions` como leading indicator, ou cruzar estado por GAQL) mudam o comportamento de uma tool de segurança.

12. ~~**F138 — commit que toca só `docs/` publica revisão nova do servidor MCP.**~~ **RESOLVIDA em 02/09**: o `deploy` passou a exigir `code_changed` do job `test`, que roda sempre. Testado contra os commits reais (o push `3e78e12..92bc5dd` devolve `false`) e por sabotagem nas duas direções. Registro original: Aconteceu em 02/09: 4 commits de catálogo rodaram `Route 100% traffic to latest revision` + smoke. Deu certo, mas **este é o servidor por onde o gestor escreve nas contas dos clientes** — um deploy no meio de uma sequência de `update_rsa` + `apply_change` tiraria a ferramenta com writes parciais aplicados. 🔴 **`paths-ignore` no `on:` seria PIOR, e isso foi verificado, não suposto:** com `test` como required check e `pr_required: true`, PR só de docs nunca reportaria status e travaria sem caminho de merge que não fosse bypass — o que alimenta a pendência 7. A correção certa **condiciona o job `deploy`**, que já é gated por `if:`, mantendo o `test` rodando sempre. Efeito colateral verificado como benigno: nada no pipeline assere que produção == HEAD de `main`.

10. ~~**O gate do Google tem o mesmo buraco que o do Meta acabou de perder.**~~ **FECHADO em 2026-09-05** (branch `feat/gate-google`, commits `d4275ea`/`9eda30d`/`840676c` + a revisão final `00875ee`/`4c1f412`/`f88bfe3`/`8e26c85`). `manager_account_access.can_manager_access` passou a exigir conta ativa **e** grant não revogado — mesmo par de condições que o Meta já tinha (F130 fechado no catálogo). Migration `008` trouxe carência e revogação soft (`revoked_at`/`revoked_reason` em vez de `DELETE`), com restauração restrita ao que saiu por churn (`left_mcc`) — revogação de admin (`admin_revoked`) não volta sozinha. A revisão final ainda fechou quatro reincidências da mesma classe (numerador de `/admin/access/by-manager` em universo diferente do denominador, mensagem de negação recomendando um painel que não resolve quando a conta saiu do MCC, `copy_access` sem guard `origem == destino`, guard AST que passava em 3 das 13 variantes de sabotagem geradas na revisão).
   **O que NÃO foi entregue, e é sprint seguinte:** não há reconciliação automática — nenhum job compara o inventário contra o `customer_client` do MCC, que é a fonte autoritativa no Google (não existe "parceria de BM" pra comparar, por isso o desenho da reconciliação Meta não se transplanta direto); não há fila no painel equivalente às três do Meta (`/admin/accounts/meta`); não há alerta; e `revoke_for_inactive_accounts` — a função que revogaria em massa o grant vivo em conta que já saiu do MCC — **não tem nenhum chamador em produção**, só roda se alguém a invocar manualmente ou via teste. `count_grants_on_inactive_accounts` (leitura, pensada pro dry-run desse job futuro) também está viva e sem consumidor.

11. **O system user está três níveis acima do uso, e o token não expira.** Medido em 20/08: `assigned_users` das contas devolve `["DRAFT","ANALYZE","ADVERTISE","MANAGE"]` — Ad Account Admin —, enquanto as 6 tools Meta são **todas de leitura**. Pela doc da Meta, o papel que corresponde ao uso é `['ANALYZE']`. Hoje, a única coisa que impede o MCP de alterar campanha de cliente é não termos escrito a tool, o que não é um controle. Some-se: o token é **permanente** (a expiração de 60 dias é opcional, com refresh suportado) e o BM tem **um único `business_user`** — você. Se você sair da V4, é o único caminho de administração desse SU. **Nada disso é código:** é configuração no Business Manager. Reduzir para `ANALYZE` deve ser testado em uma conta antes das 25.

13. ~~**F141 — os presets de data resolvem em UTC, e nenhuma das 25 contas está em UTC.**~~ **RESOLVIDA em 03/09** junto com **F143 e F144** (bloco único: mesmo fuso, mesmo enum de resposta). `freshness.status` mudou de contrato: `atrasado` → `nao_coberto`, e entrou `em_curso`. **Verificado em produção em 03/09** (PR [#28](https://github.com/BadWolf1509/v4-ads-mcp/pull/28), merge `324ee08`, deploy `success`, `/health` 200): Camaçari 06/08→02/09 saiu `nao_coberto` com o texto das duas hipóteses (ontem `atrasado`); JP `TODAY` saiu `nao_coberto` — não `em_curso` — porque a fronteira de JP é de ontem e a ordem decidida é *fronteira velha vence dia corrente*; `em_curso` só com evento já indexado hoje. O F144 vale: nenhuma janela até hoje saiu `confiavel`. A diferença de fuso não é observável de dia (UTC e Fortaleza coincidem); a prova do F141 é o teste de integração com DB no CI. **Aberto no lugar: F146 (LOW)** — `import_offline_conversions` assume BRT fixo e 2 contas são UTC−4; erra alto, não em silêncio; o fix muda o payload ao Google e merece probe própria. Registro original: Aberto em 02/09 pelo T7 do smoke: `date_range="TODAY"` na conta `7862230676` (`America/Fortaleza`) resolveu `period` para **2026-09-03** quando na conta ainda era 02/09. `_today()` é `datetime.now(UTC).date()` e o Google lê data no fuso da conta — 23 contas em UTC−3, 2 em UTC−4. **Das 21:00 à meia-noite locais (~12,5% do dia), todo dia:** `TODAY` devolve vazio silencioso, `YESTERDAY` devolve o dia parcial, e a família `LAST_N_DAYS` **desliza um dia** — entra o parcial de hoje, sai o dia inteiro mais antigo. Esta última é a pior por parecer certa. Nenhum teste pega: sob `freezegun` a data UTC e a da conta são a mesma, então a diferença **não é representável** no fixture. **O fix é mais barato do que parece** — o fuso já está em `google_ads_accounts.time_zone`, populado pelo resync; é leitura local, sem chamada extra ao Google. Blast radius mesmo assim é grande (quase toda tool usa preset): merece sprint próprio. Detalhe e as 4 decisões de desenho no F141 do catálogo.

**Ambiente:** o `gcloud` está com credencial expirada (`Reauthentication failed. cannot prompt during non-interactive execution`) — reautentique com `gcloud auth login` antes de qualquer tarefa de infra. Custou duas vezes: impediu de verificar se o `v4-ads-mcp-prod` ainda existe (11/08) e limitou a investigação de infra/CI de 19/08 à configuração declarativa do repo — env real dos jobs, crons do Scheduler, alert policies e IAM não foram verificados. Isso chegou a mudar um desenho: no F114 usei `--update-*` (merge) em vez de `--set-*` (replace) justamente por não conseguir enumerar o estado real dos jobs.

**F76/F77 encerrados:** reabrir só se aparecer `mcp_auth_error` pós-`00026`, `health_deep_db_failed` persistente pós-`00028` ou incidente do uptime check.

**Quando ler outros docs:** [bug suspeito → `findings-catalog.md`] [o que shipou / detalhe sprint → `sprint-history.md`] [última sessão → `session-2026-09-02-03-handoff.md`] [sessão anterior → `session-2026-08-19-infra-ci-handoff.md` + `-backend-` + `-frontend-`] [infra/DB, sessão anterior → `session-2026-07-23-handoff.md`] [migração GCP → `session-2026-06-30-handoff.md`] [executar pendente → spec+plan em `docs/superpowers/`].

