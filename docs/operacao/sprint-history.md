# Sprint history archive — V4 Ads MCP

> **Purpose:** Histórico de entregas Google/Meta e das sessões operacionais que alteraram produção. Mantém o detalhe cronológico fora do contexto principal do agente.

> **Last updated:** 2026-08-19. Estado corrente: 64 MCP tools (58 Google + 6 Meta), produção `v4-ads-mcp-00065-9pl`, F-series até F117. O estado vivo (decision gates, pendências, tokens) fica em [estado-atual.md](estado-atual.md) — este arquivo é **cronologia**, não estado. O catálogo autoritativo de bugs/lições é [findings-catalog.md](findings-catalog.md); o estado executável da sessão corrente está em [session-2026-07-23-handoff.md](session-2026-07-23-handoff.md).

> **For findings cross-reference:** see [findings-catalog.md](findings-catalog.md).

---

## Recent operational sessions (June–August 2026)

| Session | Status | Outcome |
|---|---|---|
| 2026-08-19 (3ª) — investigação de infra e CI | ✅ prod `00065-9pl` | F113-F117: `ci.yml` ensinava a regerar o lockfile sem `--universal` (quebra o build Linux); env dos 3 Cloud Run Jobs passou a ser declarado no deploy; gate local ganhou o check do Tailwind; rollback ancorou na revisão que estava servindo. [handoff](session-2026-08-19-infra-ci-handoff.md) |
| 2026-08-19 (2ª) — investigação de backend | ✅ prod `00064-nvg` | F109-F112: 3 caminhos que servem request ainda bloqueavam o event loop (o F86 fora fechado **sem guard**); `get_my_rate_limit_status` reportava a quota que não bloqueia; audit Meta e política de blast radius amarrados por teste. [handoff](session-2026-08-19-backend-handoff.md) |
| 2026-08-19 (1ª) — investigação de frontend | ✅ prod `00063-6bc` | F101-F108: rótulo acessível da matriz degradava no swap HTMX; logo servido `immutable` sem `?v=`; 8 controles sem nome acessível; isenção de CSRF cobria mutações do painel. Os três mais graves caíam em pontos cegos de guards **verdes**. [handoff](session-2026-08-19-frontend-handoff.md) |
| 2026-08-14/15 — investigação ampla | ✅ | F82-F100 (19 findings) fechados em duas ondas: bookkeeping em `finally`, SDK fora do event loop, escape GAQL, paginação Meta, pool, jobs, backup em snapshot único. [handoff](session-2026-08-14-15-handoff.md) |
| 2026-08-11 — frontend medido em produção | ✅ | Play CDN aposentado (407 KB de JS → 12 KB de CSS), CSP sem nenhum `unsafe-*`, a11y, assets comprimidos e cacheados; F78-F81. [handoff](session-2026-08-11-frontend-handoff.md) |
| 2026-07-23 — alerta health / F76 soak / F77 | ✅ `23fccfe`, prod `00028-lvc` | Dois 503 transitórios do deep health investigados; MCP continuou 200. F76 validado positivamente por `db_dropped_connection_retry` + request 200. F77: `/health?deep=1` passou a usar reconnect com deadline 5s e 3 regressões. CI com DB + deploy + smoke verdes. [handoff](session-2026-07-23-handoff.md) |
| 2026-07-22 — erros de produção / F76 / observabilidade | ✅ prod `00027-kmw` ao encerrar | Auditoria de 3 categorias de erro sem bug de negócio; A7 acento refutado; smoke fail-well; F76 reconnect do auth path; `level`→`severity`. O follow-up D+1 foi encerrado em 23/07. [handoff](session-2026-07-22-handoff.md) |
| 2026-07-04 (2ª) — UI/UX do painel | ✅ | 11 commits: flashes, HTMX HX-aware, toasts, convites, contraste AA, tabelas, PT-BR e fontes; F74/F75. [handoff](session-2026-07-04-ui-ux-handoff.md) |
| 2026-07-04 (1ª) — governança/infra/dívida | ✅ | F73 quota leak + cap por gestor; audit admin; alerting/backup/lockfile; smoke; deduplicações e guards. [handoff](session-2026-07-04-handoff.md) |
| 2026-07-02 — seis ondas pós-migração | ✅ | F68-F72; F64-F66 resolvidos; scheduler; gate Meta; Customer Match audit; deploy gated pelo CI; steering Fase 2B. [handoff](session-2026-07-02-handoff.md) |
| 2026-06-30 — migração GCP | ✅ | Lift-and-shift para `v4-ads-mcp` (`299432068772`) com Wellington owner; F64-F67. [handoff](session-2026-06-30-handoff.md) |
| 2026-06-20 — hardening + M.4 + Fase 2A | ✅ | Observabilidade, guards, `meta_get_performance_breakdown` e `get_performance_breakdown`; 64 tools. [handoff](session-2026-06-20-handoff.md) |
| 2026-06-19 — recuperação + Meta resync | ✅ | Conta admin recuperada, Meta 12→22, F61-F63, resync agendado. [handoff](session-2026-06-19-handoff.md) |

---

## Meta Ads sprint family — M.1 onwards (May 2026 →)

| Sprint | Status | Detail |
|---|---|---|
| Sprint M.1 — Meta Ads Foundation (DB + repos + settings + Meta App) | ✅ 2026-05-24 | 6 commits código ([d4b2485..7fe05c8](https://github.com/BadWolf1509/v4-ads-mcp/compare/48588ca..7fe05c8)) + 2 commits signoff/deploy (M.1.1 + Task 8 deploy.yml binding); ZERO MCP tools (pure infrastructure). Migration `003_meta_schema.sql` cria 4 tabelas (`meta_oauth_connections`, `meta_ad_accounts`, `manager_meta_account_access`, `meta_rate_counters`) + ALTER `audit_log`/`pending_confirmations` (add `platform TEXT DEFAULT 'google'` column). 3 repositories paralelos a Google (asyncpg + dataclasses slots+frozen). 2 settings (`meta_app_id`, `meta_app_secret`, empty defaults — graceful boot sem secrets). 12 integration tests (3+5+4). CI/Deploy verdes em `7fe05c8`, /health 200, 4 Meta tables criadas em prod (0 rows). **1 finding CI:** `test_migrations_are_idempotent` tinha hardcoded list de migrations esperadas que não incluía 003 — descoberto + fixado em CI run 2. **Task 8 Wellington manual completed (mesma sessão 2026-05-24):** Meta App criado em developers.facebook.com (App ID `1522411803012799`, BM V4 Lima Soares & Co, Dev Mode), Privacy + Terms URLs whitelisted, App Roles=Wellington admin (3 colaboradores deferred até time precisar), Use Cases habilitados (`ads_read`+`ads_management`+`business_management`), OAuth Redirect URI configurado (`/oauth/meta/callback`, localhost auto-allowed via Dev Mode), secrets `meta-app-id`/`meta-app-secret` em GCP Secret Manager (Wellington manual via `gcloud secrets versions add`), IAM `secretAccessor` concedido a `v4-ads-mcp-runtime` SA, deploy.yml binding `META_APP_ID`+`META_APP_SECRET` env vars em Cloud Run (commit `cc99320`), deploy verde em 2m47s, smoke /health+/mcp 401 gate 2/2 PASS. **Sprint family completo:** [spec](../superpowers/specs/2026-05-24-meta-ads-incorporation-design.md) (10 sections, M.1-M.25 roadmap, ~3-6 meses, ~45 tools Meta paridade quase total com Google) + [plan M.1](../superpowers/plans/2026-05-24-sprint-m1-meta-foundation.md). Decisões arquiteturais: implementação nativa V4 (Meta MCP oficial tem limitações + não funciona em Codex), naming `meta_*` prefix em tools Meta (Google sem prefix preservado), OAuth Facebook Login for Business per-user, tabelas separadas (não generalizar com `platform` column). **Próximas tasks críticas M.2 descobertas em final review:** (1) `audit_log.record()` precisa de `platform` param antes de qualquer Meta tool; (2) migration `004_audit_log_provider_id.sql` rename (22 callers); (3) `meta_rate_counters` repository CRUD ainda não existe; (4) `conftest.py _TEST_ENV` precisa de `META_APP_ID`/`META_APP_SECRET` quando Meta OAuth tests entrarem; (5) endpoint `/oauth/meta/data-deletion-callback` POST com `signed_request` validation (Meta App Review pré-requisito). |
| Sprint M.1.1 — Legal pages hotfix (`/legal/privacy` + `/legal/terms`) | ✅ 2026-05-24 | 1 commit (`f56ffe6`). 2 jinja2 templates extending `_base.html` em `src/web/templates/legal/`, V4 brand identity (red #e50914, Montserrat, editorial hero pattern de help.html com red accent dot). LGPD-compliant Privacy Policy (8 seções: quem somos, dados coletados, como usamos, onde armazenamos, retenção, direitos LGPD, atualizações, contato) + Terms of Service (11 seções: uso restrito, aceitável, proibido, confirmação mutações, disponibilidade, sem garantias, limitação responsabilidade, privacidade, modificações, jurisdição BR foro João Pessoa, contato). 2 routes públicas em `src/web/routes.py` (`optional_current_manager` — Meta crawlers acessam sem session). Required by Meta App Settings URL whitelist + App Review futuro (M.2). Pre-push 5/5, deploy verde, ambas URLs 200. Unblocks Sprint M.1 Task 8 Meta App configuration. |
| Sprint M.2a — OAuth + SDK + 1st Meta tool (`meta_list_my_ad_accounts`) | ✅ 2026-05-25 | 14 commits ([3d82fbe..e93a05b](https://github.com/BadWolf1509/v4-ads-mcp/compare/3d82fbe..e93a05b)). **Tool count 57→58** (1ª tool Meta MCP). **5 prep tasks foundation:** (1) `audit_log.record()` add `platform: Literal["google","meta"]` kwarg backward-compat (`bca3621`); (2) Migration 004 RENAME `google_request_id` → `provider_request_id` + audit_log.py 4 callsites (`bb606b7`); (3) Big-bang rename 49 caller files src+tests+2 templates (`b37c5ea`); (4) `meta_rate_counters` repository CRUD (`5e45cdd`); (5) conftest `_TEST_ENV` add META env vars (`1dde97c`). **OAuth + SDK + tool:** (6) facebook-business v21 dep + `src/meta_ads/` package + client.py (FacebookAdsApi instance direta, NÃO `.init()` global state) + errors.py PT-BR mapping (`91019b5`); (7) reports.py executor + rate_limit BUC parsing (`4db318d`); (8) `src/auth/meta_oauth.py` (3 routes: start/callback/revoke + state HMAC + granular permission enforcement, 9 testes unit+integration) (`d3d6db7`); (9) tool `meta_list_my_ad_accounts` + `_meta_common.py` helpers (4 integration tests) (`a463751`); (10) admin UI card "Conexões OAuth" Google + Meta (`248198e`); (11) smoke runbook 774 linhas (`fcd85fb`); (12) push deploy verde (+1 CI fix `c0914ef` — `test_migrations_are_idempotent` hardcoded list não pegava 004, mesmo gap do M.1). **+1 hotfix M.2a** (`e93a05b`): removido `is_allowed_email(fb_email)` do callback Meta — design error descoberto em smoke real (fb_email é conta FB PESSOAL gmail/etc, não corporativa). **Smoke real Wellington V4 Lima Soares & Co BM:** T1 OAuth happy path ✅ + T2 `meta_list_my_ad_accounts` retornou 12 ad accounts reais com PT-BR labels ✅ + T4 audit log meta_oauth_connect target_count=12 ✅ + T7 Google regression 25 contas intactas + provider_request_id renamed em prod ✅. T3/T5/T6 skip (edge cases). **2 findings catalogados:** A6 (Meta OAuth domain check design error) + F47 (PowerShell pipe CRLF mangling em secret creation procedural — fix runbook: arquivo binary intermediário). **3 real biz findings dogfood doc** (ML Antiguidades CA pagamento pendente, MDO Cotia duplicação, WJX FECHADO). **Sprint family completo:** [spec](../superpowers/specs/2026-05-24-sprint-m2a-meta-oauth-first-tool-design.md) (786 linhas, 9 sections) + [plan](../superpowers/plans/2026-05-24-sprint-m2a-meta-oauth-first-tool.md) (2961 linhas, 13 tasks bite-sized TDD) + [runbook](../_archive/runbooks/phase-M-2a-bootstrap.md) + [dogfood](../_archive/dogfood/dogfood-2026-05-25-meta-first-tool-real-biz-findings.md). **Próximo:** M.2b polish + Meta App Review submit. |
| Sprint M.2b — `meta_get_account_overview` + App Review prep | ✅ 2026-05-25 (code + smoke 8/8 + F48/F49 fixed + Meta App Review respondido 10:58 GMT-3 — `public_profile` APROVADA + `Marketing API Access Tier` REJEITADA → reanálise pós-docs oficial → Caminho B+ janela observação 30-45 dias) | 18 commits ([b5dad3e..db1d4ac](https://github.com/BadWolf1509/v4-ads-mcp/compare/b5dad3e..db1d4ac)). **Tool count 58→59** (2ª tool Meta MCP — 1ª com Graph API real call). Execução via subagent-driven-development (7 tasks A-G; haiku pra mecânicos A/B/F/G, sonnet pra integration C/D/E; 2-stage review per task). **Task A** A5 fix (`b5dad3e`): `get_my_audit_log.list_for_manager` SQL SELECT adiciona `platform` column + return dict propagation + regression test (A5 OPEN → closed). **Task B** account_overview pure module (`36394a9`): `src/meta_ads/account_overview.py` 5 functions (resolve_meta_date_window, shift_to_previous_period, parse_insights_response, compute_deltas, build_warnings) + CONVERSION_ACTION_TYPES frozenset (6 action_types) + helpers + 33 unit tests. **Task C** tool orchestrator (`9069332` + `9867690` polish): `src/mcp/tools/meta_get_account_overview.py` (218 LOC) com `classify()` + `INPUT_SCHEMA` (ZERO oneOf/allOf/anyOf — 3b.19B.1) + handler async function (rename `_handler→handler` pra match M.2a) + 2 Graph calls via `run_meta_graph_get` (current + previous, audit_this_call=True só no current) + 4 integration tests (happy + 3 edges) + auto-register via `@register_tool` (decorator-based registry, não apply_change.py). **Task D** data-deletion-callback (`ed93deb` + `94e5b7b` polish): `_verify_meta_signed_request()` helper (base64url + HMAC-SHA256 + compare_digest + algorithm whitelist) + endpoint POST `/oauth/meta/data-deletion-callback` (form data signed_request validation, audit_log row `meta_data_deletion_request` platform=meta, retorna `{url, confirmation_code}` via `request.url_for(name="data_deletion_status")` + force HTTPS) + public status page `/legal/data-deletion-status/{code}` Jinja2 template + 8 unit tests + 3 integration tests. **Task E** refresh-accounts (`29d6ea3`): endpoint POST `/oauth/meta/refresh-accounts` (CurrentUser dep + token expiry pre-check 422 PT-BR + Graph /me/adaccounts + upsert + grant idempotent + audit_log + redirect /admin?meta_refreshed=1) + admin_index flash branch (bonus fix: extract meta_connected/revoked/refreshed flags que não estavam sendo passados antes) + 2 integration tests. **Task F** admin UI buttons (`847dfab`): card OAuth Meta extends M.2a Task 10 — botões "Atualizar lista" (form POST refresh) + "Revogar conexão" (modal vanilla `<dialog>`) + warning banner se token <7d + flash success ✓ se ?meta_refreshed=1; usa `button()` + `modal()` macros de `_components.html` + V4 color tokens (v4-gold-soft, v4-green-soft). **Task G** smoke runbook (`c73b48a`): `docs/operacao/phase-M-2b-bootstrap.md` 1084 linhas via `smoke-runbook-generator` subagent (8 tests Wellington manual ~45 min, per-value probe convention 3b.19A.1, 18 V4 invariants table) + `scripts/test_meta_deletion_callback.py` helper (getpass pra evitar shell history exposure F47-aware). **Pre-push 5/5 PASS em cada commit + final**, deploy verde 3m11s, /health 200, /legal/data-deletion-status/* renderiza confirmation_code corretamente. **Smoke real via MCP 5/8 PASS + 1 finding caught:** T1 happy path ICSER (10-key return + LAST_7_DAYS resolved), T2 per-value probe ML Antiguidades (ctr=4.117% ✓ math, cpc=0.528 ✓, frequency=1.421 ✓, fields tipos corretos), T3 PAGAMENTO_PENDENTE warning fire em ML Antiguidades (current=0 + previous=120.40 BRL → spend_pct=-100%, conversions_pct=null per spec), T5 PT-BR error translation (invalid ad_id + partial date), T6 data-deletion-callback synthetic via `scripts/test_meta_deletion_callback.py` (valid sig → 200 + url/confirmation_code; tampered → 400 Invalid signature; empty → 411 GFE pre-block). **F48 caught + fixed** (`a281c00`): `facebook_business v21 FacebookAdsApi.__init__` aceita só `(session, api_version, enable_debug_logger)` — NÃO `access_token`/`app_id`/`app_secret` direto. M.2a code passava esses kwargs → TypeError em smoke real (não pego em CI: integration tests mockam `run_meta_graph_get` nunca exercise real factory). Fix: extract `build_facebook_ads_api()` pure factory + use `FacebookSession(...)` bridge + 13 unit tests `tests/unit/test_meta_client.py` cobrindo factory signature contract (mitigation testing gap). **Insight de negócio confirmado em smoke:** ML Antiguidades spend cresceu +325% Apr→May (70.97→301.97 BRL) mas conversions=0 todo período — Smart Bidding cego cross-platform, vale review com cliente. **Smoke 8/8 COMPLETO same-day:** T4 token expiry warning (mock Supabase 5d → warning fired "Token OAuth Meta expira em 4 dias" + restore 59d → warnings vazio); T7 revoke button browser (modal → POST /revoke → /admin?meta_revoked=1 + DB revoked_at + audit row 285); T8 refresh button browser (após F49 fix — button macro default type="button" não submetia form → fix explicit type="submit") + audit row 287 target_count=12. **F49 caught + fixed** (`fe9a976`): smoke real T8 expôs bug em `_components.html:80` macro default `type="button"` ignorava `<form>` submit. Fix: explicit `type="submit"` no chamado + nova convention CLAUDE.md. **F50/F51 retrospective trace** (commit `db1d4ac`): subagent investigou 2 errors antigos em audit_log (id=140 TypeError + id=149 AttributeError em create_campaign 2026-05-18) — confirmados como traces produção de F33+F37 já fixados em commits `df0f451` + `9488f7c`. Bonus: nova convention "field rename guard" — quando proto field renomeado entre versões SDK, test MUST assertar BOTH `has(new_field) is True` AND `has(old_field) is False` (CapturedOp._SubCapture aceita qualquer atributo silently). **Tool audit completa** (`6d821da`): 242-line doc `docs/operacao/tool-audit-2026-05-25.md` com data-driven analysis dos 58 tools — 8 core (60% volume), 22 zombies (38%), recomendação sweet spot 30-45 tools (vs 102 plano M.25). **GitHub default branch fix** (mesma sessão): default era `chore/initial-ci` (3 semanas stale), trocado pra `main` via `gh repo edit` + branch antigo deletado, protection rules `main` preserved. **Meta App Review RESPONDIDO 2026-05-25 10:58 GMT-3** (mesma sessão, ~6h após submit): `public_profile` ✅ APROVADA, `Marketing API Access Tier` ❌ REJEITADA. Motivo literal Meta: *"Our records do not show a sufficient number of Ads API calls in the last 15 days by this application."* **Critérios exatos Meta (pós-leitura docs oficial):** ≥500 chamadas/15d + <15% error rate. **Nomenclatura atualizada:** "Standard Access" virou Limited Access (default sem App Review), "Advanced Access" virou Full Access (o que rejeitou). **Red flag Limited Access (docs Meta literal):** *"Volumes extremamente limitados por conta de anúncios. NÃO para apps em produção visualizado para um cliente publicitário."* Risk throttle real em uso production V4. **Decisão Wellington Caminho B+ (janela observação 30-45 dias):** acelerar M.3+ pra ship mais tools Meta (`meta_get_campaign_performance` etc) → Wellington usa naturalmente day-to-day → volume cresce → ~30-45 dias atinge 500 calls cumulativas em 12 contas V4 LS&Co → re-submit Full Access com fundamento. Não-Caminho A permanente (risk throttle production), não-Caminho B forçado (3×/dia waste Wellington time). **Monitorar:** `meta_rate_counters` table X-Business-Use-Case-Usage — se throttle real surface antes da janela, priorize re-submit. **Decision gate atualizado:** continuar M.3 = ≥3 calls/semana dogfood Wellington (não-mais "App Review APPROVED" como pré-req); senão pivot Google. **Action quando colab entrarem:** add como App Roles → Administrators no Meta Dev Console (Limited Access permite 25 admins/testers). **Re-submit Full Access ON roadmap** (originalmente OFF na análise inicial pré-leitura docs Meta) agendado pra ~30-45 dias após volume natural atingido. D1 finding decision-not-bug catalogado com critérios exatos + 2 conceitos diferentes (Marketing API tier vs resource access tier) em findings-catalog.md. **2-stage review subagent flags:** Task A test em tests/integration/ vs plan tests/unit/ (convention wins, doc drift acceptable); Task C `_handler` over-engineering renomeado pra `handler`; Task D `(ValueError, Exception)` overly broad + hardcoded base URL — ambos fixados em commit polish `94e5b7b`. **A5 finding closed.** **Decision gate post-Caminho B+:** 30-45 dias janela observação — Wellington usa naturalmente + monitorar volume + acelerar M.3+ ship tools Meta pra gerar uso. Se ≥3 usos/semana sustentados + 500 calls cumulativas atingidas = re-submit Full Access; senão re-evaluate pivot Google backlog 3b.39+. **Sprint family completo:** [spec](../superpowers/specs/2026-05-25-sprint-m2b-meta-get-account-overview-design.md) (892 linhas) + [plan](../superpowers/plans/2026-05-25-sprint-m2b-meta-get-account-overview.md) (2503 linhas, 10 tasks bite-sized TDD) + [runbook](../_archive/runbooks/phase-M-2b-bootstrap.md). |
| Sprint 3b.38 — F52 + F23 fix + B1 refino (dogfood 25/05 zombies-audit ship) | ✅ 2026-05-25 (code + smoke real 5/5 PASS) | 1 commit (`78894cf`). **Tool count stays at 59** (enrich + fix, sem novo tool). Origem: dogfood 2026-05-25 MO-JP+CAB zombies audit — 3 itens shipped same-day em ~3h ciclo dogfood → catalog → plan → implement → CI green → smoke real. **F52 fix (HIGH):** `audit_zombie_keywords` adiciona `ad_group_status` field na response (Opção C dogfood B6, mínimo invasivo, zero breaking change) + description warning ("zumbis incluem keywords em ad_groups REMOVED — filtre `ad_group_status='ENABLED'` pra impacto técnico real OU mantenha tudo pra inventário"). 3 source files + 3 test files (KeywordRow + ZombieKeyword dataclasses + parse_keyword_view_row + dict_to_keyword_row + tool response dict + 3 regression tests novos cobrindo cenário DELL/GPA órfã + propagation). **F23 fix (LOW→MED promoted):** `get_change_history` auto-clamp `start_date` pra `max(start, today-28)` quando preset usado (margem 2-day safety) + `date_range_warning` na response. Non-breaking: custom dates (start_date/end_date OR dict legacy {from,to}) honram intent do usuário sem clamp + RangeTooWideError preservado. 2 regression tests novos. **B1 refino:** docstring caveat + description string editados de "HORAS (>3h já visto)" → "DIAS (>4 dias já visto em produção — dogfood 25/05 MO-JP)" baseado em medição empírica reverts Pedro 21/05 ainda não surfaceavam 4 dias depois. **Pre-push 5/5 PASS em 36.9s** + CI verde 8m12s + Deploy 3m17s. **Smoke real conta MO-JP+CAB 5/5 PASS:** T1 F52 — 280 zumbis retornadas = 170 REMOVED + 110 ENABLED match exato dogfood (DELL JPA 93 órfãs + GPA02 ANDAIME CAB 77 órfãs); T2 F23 LAST_30_DAYS clamp + warning (period.from=2026-04-27=today-28 + date_range_warning field presente); T3 F23 negative LAST_7_DAYS (period.from=2026-05-18=today-7 + sem date_range_warning); T4 F52 description warning visível em tool description; T5 B1 description "DIAS" visível. **WATCH confirmado:** T1 output 97k chars estourou MCP cap (esperado pelo CLAUDE.md WATCH item — `audit_zombie_keywords` default limit=200 estoura em contas 500+ entries; V1 candidate: bump default smaller OR compress payload). **2 findings catalogados:** F52 (audit_zombie_keywords não filtra ad_group.status — órfãs cosméticas em ad_group REMOVED) + Lições V4 48 P4 (audit tools devem expor status do parent resource) catalogada em Lessons reinforced #7. F23 promoted "known limitation" → "fixed". **Loop dogfood comprovado ~3h cycle.** [dogfood doc](../_archive/dogfood/dogfood-2026-05-25-mestre-da-obra-jp-zombies-audit.md). |
| Sprint M.3.1 + M.3.1.1 — Hotfix Meta Insights API field whitelist (F53 + F54) | ✅ 2026-05-27 (smoke real T1-T10 = **8 PASS + 2 DEFERRED V1**) | 2 commits hotfix iterativos (`984a7ae` M.3.1 + `b3ba6b5` M.3.1.1). **Tool count permanece 62** (schema-only fix, sem novo tool). **F53 caught em smoke real M.3 T1:** Meta Insights API rejeita `effective_status` em `fields=` AND `filtering=` params — TODAS 3 tools M.3 broken em produção desde 2026-05-26 deploy. Audit_log id=296+299+300 confirmaram errors PT-BR mapeados corretamente (to_friendly_meta_error subcode 100). **F54 caught em smoke iteration 2 pós-M.3.1:** more invalid fields revelados — `billing_event`, `daily_budget` (adset), `creative_id` (ad). Mesma family F53. **Hotfix execution:** Edit insights.py (3 INSIGHTS_FIELDS_* lists + remove filtering block + signature) + Edit 3 tool files (schema + handler + params_summary) + Update 2 unit tests + 1 integration test. Parser defensive code já handle missing fields (returns None/UNKNOWN). Pre-push 5/5 em 41.6s (M.3.1) + 37.1s (M.3.1.1). Deploy verde 3m28s + 3m48s. **Smoke real validation pós-M.3.1.1 em conta ML Antiguidades (`act_370008662`) LAST_90_DAYS:** T1 campaign-level success (1 row: ENGAJAMENTO WHATSAPP, objective=OUTCOME_ENGAGEMENT, spend_brl=411.83, impressions=20101, ctr=0.0377 = 3.77% normalizado ok, cpc_brl=0.544, reach=10522, frequency=1.91); T2 adset-level success (optimization_goal=REPLIES, ad_set_id snake_case ✓, daily_budget_brl=null pós-F54); T3 ad-level success (creative_id=null pós-F54, parents corretos). T5 custom date range Apr-only spend 70.97 vs Apr+May 411.83 math consistente. T9 BUC tracking ML Antiguidades calls_used=5 throttle_pct=1%. T10 audit_log 5 success entries com provider_request_id Meta x-fb-trace-id populated (BeBREJnkUJk, A4yiXe8X1Vj, AZcBEtMACkf, etc), platform='meta' validado. T4+T6 DEFERRED V1 (effective_status filter feature gone pós-F53/F54 hotfix; V1 enhancement = 2-step query `/campaigns?fields=effective_status` → `/insights?filtering=campaign_id IN [...]`). T7 PASS (account inexistente PT-BR friendly). T8 PASS implicit (T1 attempt #1 com token-expired-server-side reproduziu cenário, msg PT-BR mapeada via subcode 458/467/460/463 em errors.py to_friendly_meta_error). **2 F-findings novos catalogados:** F53 + F54 (family design-gap-via-API-field-whitelist, Meta variant, multi-iteration). **Lição reinforced (F17-F44 family Google + agora F53-F54 Meta):** schema/field whitelists MUST ser empiricamente validadas via per-value probe contra REAL Meta sandbox account ANTES de deploy production. CI integration tests mockam `run_meta_graph_get` — nunca exercise real API. Caminho B+ Meta volume restored em produção same-day. **Próximo natural:** Wellington dogfood naturally usando 3 tools → +3-6 calls/dia → acelera ~30-45d window pra 500 calls/15d threshold Full Access re-submit. Sprint M.4 plan (geo+device+hourly) candidato priority alta. [phase-M-3-bootstrap.md]
|
| Sprint M.3 — 3 tools Meta performance (campaign + ad_set + ad) | ✅ 2026-05-26 (code shipped + CI/Deploy verde + production /health 200; smoke real revelou F53 + F54 → hotfix M.3.1+M.3.1.1 2026-05-27) | 8 commits ([940cb57..fd50348](https://github.com/BadWolf1509/v4-ads-mcp/compare/f7ac4cc..fd50348)): 2 docs (spec `804a689` + plan `a4a3a8f`) + 5 código (T1 META_EFFECTIVE_STATUS_LABELS `940cb57` + T2 insights.py `7bb4484` + T3 campaign tool `6f70231` + T4 ad_set tool `7f67009` + T5 ad tool `e76b7f2`) + 1 runbook (`fd50348`). **Tool count 59→62** (22 always + 40 defer = paridade direta Google get_*_performance). Execução **subagent-driven-development** (haiku T1+T2 pure module / sonnet T3-T5 integration tasks / smoke-runbook-generator T7); 5 implementer + 10 reviewer dispatches (spec compliance + mcp-tool-quality-reviewer paralelos per task). **Architecture (Approach C — shared insights module):** `src/meta_ads/insights.py` (176 LOC) com `build_insights_call(level, ad_account_id, start, end, effective_status, limit) -> (edge, params)` + `parse_insights_row(row, level) -> dict` + `INSIGHTS_FIELDS_CAMPAIGN/ADSET/AD` constants + `_extract_action_value/_extract_purchase_roas` helpers. Pure module zero-IO, 18 unit tests cobrem todos níveis + edge cases (no actions, malformed values, unknown status, CBO sem daily_budget, ctr normalization, multi-match). 3 tool handlers (~150 LOC cada) thin wrappers reusando `run_meta_graph_get` (M.2b) + `resolve_meta_date_window` (M.2b) + `meta_ad_accounts.get_by_id` (M.2a). 14 integration tests (6+4+4) mock `run_meta_graph_get` no namespace do tool (convention 3b.5/3b.8). **Schema convention:** 4 effective_status enum simplificado (ACTIVE/PAUSED/ARCHIVED/ALL, simplificação mental model Google enabled/paused/removed/all — full Meta enum 9 valores fica V1), 6 date_range presets (sem THIS_MONTH/etc), limit max 500 (Meta API page cap), NO oneOf/allOf/anyOf, additionalProperties=false. **Response shape:** flat list ordenada por spend_brl DESC, level-specific fields prepended (campaign: id+name+objective; adset: +optimization_goal+billing_event+daily_budget_brl com CBO=None; ad: +creative_id+parents), common metrics (spend_brl/impressions/clicks/ctr/cpc_brl/reach/frequency), purchases+leads extracted top-level via `_extract_action_value(actions, "purchase"|"lead")`, purchase_roas via primeira entrada da lista. Snake_case convention: parser converte Meta's `adset_id` → `ad_set_id` na response (V4 canonical). **Bucket classification (3b.39 F1 D3 mechanism):** `meta_get_campaign_performance` bucket=always (Pareto Meta top usage — primeira pergunta gestor), `meta_get_ad_set_performance` + `meta_get_ad_performance` bucket=defer (granular, follow-up natural). D3 regression test `test_list_tools_anthropic_alwaysload_count_matches_always_bucket` auto-adjusted 21→22 alwaysLoad pós-T3. **Schema allowlist updates** em `test_tools_schemas.py::test_no_unexpected_tools` (3 entradas novas). **2-stage reviewer flags (não-blockers):** runbook ausente até T7 (esperado per plan), per-value probe T6 effective_status pendente smoke real, friendly error mapping `hasattr(e, "message")` consistente com M.2b pattern (pode robustez via mapeamento explícito MetaAdsFriendlyError/NoMetaConnectionError/MetaTokenExpiredError — non-blocker LOW). **Pre-push 5/5 PASS em cada T1-T5 + final** (792/792 unit tests + 5/5 test_mcp_server_meta), CI verde 8m41s (com DB integration testcontainers) + Deploy verde 3m28s (Cloud Run + smoke test built-in + traffic 100%), /health 200. **Caminho B+ Meta volume contribution:** +3-6 calls/dia naturais Wellington dogfood → acelera ~30-45d window pra atingir 500 calls/15d threshold → Full Access re-submit fundamento (vs forçado 3×/dia waste pré-D1). **Runbook smoke `phase-M-3-bootstrap.md` 821 linhas** via smoke-runbook-generator subagent (10 tests T1-T10: 3 happy paths + status ALL + custom date + per-value probe 4 valores + 2 error paths + BUC tracking + audit_log.platform='meta' validation + 11 V4 invariants table). **Pending:** Wellington restart Claude Code + escolher ad_account com Pixel purchase configurado + executar T1-T10 (~30min). Per-value probe T6 vai validar 4 valores effective_status enum — se Meta rejeitar algum, remover schema + catalog F-finding novo. **Sprint family completo:** [spec](../superpowers/specs/2026-05-26-sprint-m-3-meta-campaign-performance-design.md) (594 linhas) + [plan](../superpowers/plans/2026-05-26-sprint-m-3-meta-campaign-performance.md) (2355 linhas, 9 tasks bite-sized TDD) + [runbook](../_archive/runbooks/phase-M-3-bootstrap.md). **Próximo:** Wellington smoke M.3 + Sprint M.4 plan (`meta_get_geo_performance` + `meta_get_device_performance` + `meta_get_hourly_performance` — multiplica volume per session). |
| Sessão operacional 2026-06-19 — recuperação conta + Meta 12→22 + GAQL error UX + resync Meta | ✅ 2026-06-19 | 4 commits (`17a8145` paginação Meta + `653e724` GAQL error hardening + `ee4dde0` cleanup/resync + commits de docs). **Sem novo MCP tool** (fixes + 1 Cloud Run Job). **Contexto:** repo clonado em `D:\v4-ads-mcp` numa máquina nova (sem gcloud/gh/Python — instalados na sessão: Python 3.13 + venv + deps, gh 2.95, gcloud 573). **Recuperação da conta admin:** a conta antiga `wellinton.ribeiro@v4company.com` (typo) foi EXCLUÍDA no Google → Google OAuth refresh token morto (`invalid_grant: Account has been deleted`) + manager `not_invited` no painel. Fix via Supabase SQL: `UPDATE managers SET email=...` no MESMO `manager_id` (`fc67c099-...` → grants/sessões/audit intactos) + revoke das `google_oauth_connections` mortas + re-login do painel (re-gravou refresh token Google). **Meta 12→22 contas:** (1) F61 paginação `/me/adaccounts`; (2) re-sync `/admin/accounts/meta` → inventário 12→22 (system user `v4-ads-mcp-integracao` id `61590110716028` alcança o portfólio); (3) grants Modelo B `/admin/access/meta` "Selecionar todas" pros 4 gestores (todos com 22; Anderson `invited`). MCP `meta_list_my_ad_accounts` 12→22 + spot-check insights verificados. **GAQL error hardening (F62/F63):** investigação dos erros do Pedro (Codex) via `audit_log` (Supabase MCP) — eram queries inválidas (auction insights inexistentes + change_history >30d), não bug. `to_friendly` QUERY_ERROR auto-corretivo + `run_gaql` desc + `get_change_history` custom clamp. **Cleanup:** `BOOTSTRAP_ADMIN_EMAILS` no `deploy.yml` (vazio em prod) + e-mail LGPD nos templates legais + CLAUDE.md/admin.py. **Resync Meta agendado:** `src/jobs/meta_resync.py` piggyback no `account_resync` + `META_SYSTEM_USER_TOKEN` no job via `deploy.yml --update-secrets`. **IAM GCP:** conta nova com ZERO acesso ao projeto (antiga era owner, excluída) — deploy SA é least-privilege (não auto-concede IAM, verificado via workflow break-glass efêmero, removido) → grant pendente de Org Admin V4. **CI/Deploy verdes** em todos os commits. 3 F-findings (F61/F62/F63). [session-2026-06-19-handoff.md] |
| Sessão 2026-06-20 — Onda 0 (hardening) + Onda 1 (observabilidade) | ✅ 2026-06-20 | 2 unidades, commits `6506cd7`+`f5461e3` (Onda 0) e `3c3767a` (Onda 1). **Sem novo MCP tool** (fixes de segurança + infraestrutura de observabilidade). **Partindo de auditoria proativa de oportunidades de melhoria. Onda 0 — hardening de segurança (`fix(security)`):** (1) `validate_gaql` ganhou `ensure_account_access` — era o ÚNICO dos ~8 call-sites de `build_client_for_manager` sem hard-gate (classe F57; vazava existência/schema de qualquer conta da MCC + bypassa rate-limit; pego proativamente antes de chegar em produção como incidente). (2) `run_meta_graph_get` auditando negação SEMPRE (movido pra fora do `if audit_this_call` — evento de segurança idêntico ao gate Google; protege tools Meta futuras zero-config). (3) 3 builder tests (add_keywords/add_negatives/bulk_pause) migrados de MagicMock cru → `make_capture_client` + novo guard `test_builder_tests_use_capture_client_not_magicmock` (anti-reincidência classes F16/F42/F44). **Onda 1 — observabilidade (`chore(observability)`):** `clear_request_context`+`bind_request_context` plugados em `resolve_session_to_context` (`src/mcp/session.py`, padrão structlog clear-no-início) → logs de cada request MCP carregam `manager_id`/`session_id` automaticamente. `/health?deep=1` faz `SELECT 1` no pool (200 db=ok / 503 degraded); shallow `/health` inalterado. Ambas ondas: CI verde, sem novos F-findings (TDD + review capturou tudo antes do push). |
| Sprint M.4 — `meta_get_performance_breakdown` | ✅ 2026-06-20 | Commits `79263db..050b146`, subagent-driven (implementers haiku/sonnet + 2-stage review). **Tool count 62→63** (6ª tool Meta). 1 tool Meta consolidada com `breakdown` enum `platform|device|geo|hourly` (1 breakdown por chamada). Estende `src/meta_ads/insights.py` com `build_insights_call(breakdowns=)` + `parse_insights_row(breakdown_keys=)`. bucket=defer. **Smoke GREEN** (ML Antiguidades `act_370008662`): platform→`publisher_platform`, device→`impression_device`, geo→`country`, hourly→`hourly_stats_aggregated_by_advertiser_time_zone` — todos 4 breakdowns retornaram dados reais. Lintagem de fields Meta via `ads_get_field_context` do Meta MCP oficial validou fields pré-ship (lição F53/F54/F55 honrada). Spec: `docs/superpowers/specs/2026-05-24-meta-ads-incorporation-design.md` (roadmap M.4). Plan: `docs/superpowers/plans/2026-06-19-meta-performance-breakdown.md`. Sem novos F-findings. |
| Sprint Fase 2A — `get_performance_breakdown` (Google, consolida 8 reports) | ✅ 2026-06-20 | Commits `441962b..69a7f2d`, subagent-driven (implementers haiku/sonnet + 2-stage review). **Tool count 63→64** (23 always + 41 defer = 58 Google + 6 Meta). 1 tool Google consolidada ADITIVA que substitui funcionalmente os 8 reports individuais (campaign/ad_group/ad/keyword/audience + account×device/geo/hourly). Módulo puro `src/google_ads/performance_breakdown.py` (`_validate_combo`+`_common_metrics`+`build_performance_breakdown_query`+`parse_performance_row`). `level` enum (campaign/ad_group/ad/keyword/audience/account) + `breakdown` enum (device/geo/hourly, opcionais; account+null → aponta `get_account_overview`). bucket=always, audit_this_call=True. **Smoke GREEN** (MO-JP `7862230676`): 8 combos + 2 negativos (combo inválido → erro claro) + **parity cross-check bit-a-bit** vs reports antigos (consolidação sem regressão — custo, cliques, conversões idênticos campo a campo). Tombstone dos 8 antigos = Fase 2B (futuro, pós-soak 21d). Spec: `docs/superpowers/specs/2026-06-20-google-performance-breakdown-design.md`. Plan: `docs/superpowers/plans/2026-06-20-google-performance-breakdown.md`. Sem novos F-findings. |

---

## Sprint 3b.1 through 3b.19B (chronological, May 2026)

| Sprint | Status | Detail |
|---|---|---|
| Sprint 3b.1 — `add_negatives_from_search_terms` + `get_change_history` | ✅ 2026-05-11 | 11 commits ([f156d99..3549778](https://github.com/BadWolf1509/v4-ads-mcp/compare/c660c6c..3549778)); smoke runbook signed-off ([`phase-3b-1-bootstrap.md`](../_archive/runbooks/phase-3b-1-bootstrap.md), [9fbfb1c](https://github.com/BadWolf1509/v4-ads-mcp/commit/9fbfb1c)) |
| Sprint 3b.2 — `update_ad_status` + `bulk_pause_by_query` | ✅ 2026-05-11 | 11 commits ([1ee0bda..24854dc](https://github.com/BadWolf1509/v4-ads-mcp/compare/edbb8ba..24854dc)); smoke runbook signed-off em conta real ([`phase-3b-2-bootstrap.md`](../_archive/runbooks/phase-3b-2-bootstrap.md)) — 2 bugs reais pegos pelo pre-smoke (GAQL parens + REMOVED retrofit gap nos 3 status ops existentes) fixados em `24854dc`. Retrofit completo: `blast_radius.classify` + extends `run_report` com `params_summary` + 4 status ops passam `new_status`. |
| Sprint 3b.3 — `add_keywords` | ✅ 2026-05-12 | 5 commits ([220ae25e..657f8f6](https://github.com/BadWolf1509/v4-ads-mcp/compare/4d54878..657f8f6)); smoke runbook signed-off em conta real ([`phase-3b-3-bootstrap.md`](../_archive/runbooks/phase-3b-3-bootstrap.md)) — production revision `v4-ads-mcp-00096-mzm`. **2 achados reais** documentados: (A1) Google faz silent dedupe em vez de `CRITERION_EXISTS` quando KW duplicada, portanto idempotência via Google API behavior (não via nosso `_classify_partial` mapping); (A2) **bug pré-existente Sprint 3a:** `update_*_status(REMOVED)` passa dry-run mas `apply_change` falha — Google rejeita REMOVED em `.status.update`. Spawn-task criado pra fix posterior. Pivot original `update_keyword_match_type` → `add_keywords` por API immutability finding (KeywordInfo.text+match_type são identidade do criterion, não modificáveis). |
| Sprint 3b.4 — `apply_audience` | ✅ 2026-05-12 | 5 commits ([9b2aaf7..528d77c](https://github.com/BadWolf1509/v4-ads-mcp/compare/72e98ed..528d77c)); smoke runbook signed-off em conta real ([`phase-3b-4-bootstrap.md`](../_archive/runbooks/phase-3b-4-bootstrap.md)) — production revision `v4-ads-mcp-00102-6td`. **2 achados graves documentados:** (A3) Google silent-drops user_interest com taxonomy_type incompatível (VERTICAL_GEO em SEARCH ad_group) — retorna applied_count=1 + request_id mas criterion não persiste. Validado in-prod com IN_MARKET (id 80001) persistindo criterion `56976936578`. (A4) Google silently overrides `negative=True` → `false` em CampaignCriterion user_list creates — T3 apply (Customer Match exclusion na Mestre da Obra JP) criou criterion `2480650242694` mas como POSITIVE observation, NÃO exclusion. V4 playbook `-10% CPA via exclusion` requer mecanismo API diferente a investigar. Spawn-tasks A3 + A4 criados. **Lição test coverage:** MagicMock-everywhere em unit tests não verifica atribuição real de proto fields — A4 passou nos 8 builder tests sem detecção. Padrão de teste pra mutate builders precisa capturar field assignments via mock state, não MagicMock default. |
| Sprint 3b.5 — stabilization fixes (A2 + A3 + A4) | ✅ 2026-05-12 | 6 commits ([ffd50fa..3c23fc5](https://github.com/BadWolf1509/v4-ads-mcp/compare/178ab18..3c23fc5)); smoke runbook signed-off em conta real ([`phase-3b-5-bootstrap.md`](../_archive/runbooks/phase-3b-5-bootstrap.md)) — production revision `v4-ads-mcp-00111-ljc`. **Composite stabilization sprint:** A2 schema-restrict `update_*_status` para `["ENABLED", "PAUSED"]` em 4 status tools + remove dead-code Sprint 3b.2 retrofit do `blast_radius.classify`. A4 pre-flight rejection no `apply_audience` pra combo `(campaign + exclusion + user_list)` — direciona gestor pra ad_group level. A3 pre-flight GAQL batch taxonomy validation no `apply_audience` pra `user_interest` (whitelist IN_MARKET + AFFINITY). ProtoFieldCapture fixture em `tests/unit/fixtures/proto_capture.py` + retrofit do `test_builder_campaign_user_list_exclusion` pra exercitar o pattern. 10 de 10 smoke tests PASS first try (primeira sprint sem novos bugs no smoke — resultado esperado de stabilization). **Lição CI:** pre-push gates rodaram só unit tests; 2 integration tests pré-existentes ficaram inválidos pós-Sprint 3b.5 (REMOVED dead test + apply_audience sem run_report mock) e foram fixados em segundo commit `3c23fc5`. Spawn-task pendente: adicionar integration sweep ao pre-push gates. |
| Sprint 3b.6 — `remove_audience` (closes audience CRUD) | ✅ 2026-05-12 | 6 commits ([eb3ed19..0c68bca](https://github.com/BadWolf1509/v4-ads-mcp/compare/825a830..0c68bca)); smoke runbook signed-off em conta real ([`phase-3b-6-bootstrap.md`](../_archive/runbooks/phase-3b-6-bootstrap.md)) — production revision `v4-ads-mcp-00117-frb` (post-A5 fix). **Closes audience CRUD:** create (3b.4) + validation (3b.5) + delete (3b.6). Tool `remove_audience` com schema target_type + target_id singular + criterion_ids array (cap 100), sempre CONFIRM (spec §7.1 remove). 6 ProtoFieldCapture builder tests + 9 tool tests + 1 integration. **Cross-cutting fix descoberto em T4** (`apply_change.py`): propagação de `__params_summary__` via payload key (mesmo pattern que `__partial_failure__` + `__target_count__`) — beneficia TODAS as CONFIRM-path tools com custom audit summaries. Nutry smoke 6/6 PASS + Mestre da Obra JP cleanup do criterion `2480650242694` (Sprint 3b.4 A4 artifact). **Achado A5 (smoke + post-Nutry):** CampaignCriterion resource_name é compound `{campaign_id}~{criterion_id}` (mesmo padrão de AdGroupCriterion), NÃO flat como spec inicial assumia. Builder construía path flat — Google silently aceitou retornando applied_count=1 mas NÃO removia o criterion. 5ª variante da família silent-acceptance (A1 dedupe, A3 drop, A4 override, A5 path-malformed). Fixed em `0c68bca` via SDK path helpers (authoritative source). **Real biz value cumulativo:** 5 orphan criteria limpas em 4 sprints, audience CRUD agora self-contained no MCP. |
| Sprint 3b.7 — UX fixes bundle (P1b dogfood findings) | ✅ 2026-05-12 | 4 commits ([a2d2046..952033a](https://github.com/BadWolf1509/v4-ads-mcp/compare/74af804..952033a)); smoke runbook signed-off em 3 contas reais ([`phase-3b-7-bootstrap.md`](../_archive/runbooks/phase-3b-7-bootstrap.md)) — production revision `v4-ads-mcp-00121-kw7`. **Composite UX-fixes mini-sprint (zero new tools, tool count stays at 39):** UX-1 `tracking_warning` field PT-BR em `get_account_overview` + `get_funnel_metrics` quando `conversions_value == conversions` (1:1 placeholder tracking, ROAS misleading). UX-2/UX-3 (descobertos via brainstorming como **mesmo bug**): proto-plus v20 repr regression — `str(enum).split(".")[-1]` retornava `"2"` em vez de `"ENABLED"`. Fix mecânico para `.name` em 22 call sites across 10 read tools. **Helper validation empírica em 3 shapes:** (a) Mestre da Obra JP — 1:1 placeholder, warning fires ✅; (b) ML Antiguidades (`7455088726`) — real revenue tracking, AOV R$ 515, ROAS 100.79, no warning ✅; (c) Expresso Turismo (`5295988089`) — zero-value tracking gap (conv=95, value=0), no warning ✅ (different gap class, correct). **Bonus catch:** retrofit pegou quality_* enums em `get_keyword_performance` (4 enums adicionais) — coverage maior que spec antecipou. 7 de 7 smoke tests PASS first try (segunda sprint consecutiva sem bugs — stabilization compounding). **Out-of-MCP-scope finding documentado:** V4 accounts default a placeholder tracking; ML Antiguidades é exceção feliz. V4 setup audit fica fora do escopo do MCP. |
| Sprint P2 — pre-push integration sweep (process improvement) | ✅ 2026-05-12 | 8 commits ([88912ab..e6b1a74](https://github.com/BadWolf1509/v4-ads-mcp/compare/8c52a26..e6b1a74)); zero novos MCP tools (tool count stays at 39); zero smoke runbook (internal tooling, sem mudança em production behavior). **Closes Sprint 3b.5 spawn-task:** "adicionar integration sweep ao pre-push gates." Ship 2 scripts Python standalone em `scripts/`: `check_pre_push.py` fast default (ruff + format + mypy + unit + non-DB integration, ~30s, sem Docker) + `check_pre_push_full.py` opt-in full sweep (6º step `pytest -m integration` via testcontainers, exit 2 + PT-BR hint se Docker off). Shared engine `_runner.py` com `Step` frozen dataclass + `run_steps` fail-fast + `check_docker` probe — 7 unit tests cobrem todos os paths. **Marker fix pré-requisito:** `pytestmark = pytest.mark.integration` em `tests/integration/test_update_ad_status.py` (usava testcontainers mas faltava marker — step 5 do fast script teria hung). CLAUDE.md "Verification cadence" agora aponta single entry `python scripts/check_pre_push.py` em vez dos 4 comandos manuais; "Don't do" adiciona bullet sobre não pushar sem rodar o gate. **Achados durante implementação:** (a) deviation no plano: implementer adotou underscore filenames (`check_pre_push.py`) em vez de dash; aceito como mais Pythonic e consistente; (b) Task 3 scope-creep: implementer marcou 5 testes não-Docker como `integration` para suprimir falhas locais quando real root cause era `google-ads` dep não instalada — revertido em `cdb86ae` (5 markers falsos retirados, 3 legit em test_health.py + test_mcp_handshake.py preservados); (c) `tests/unit/test_rate_limit.py` é misplaced (usa testcontainers + tem marker `integration`, mas vive em `tests/unit/`) — fix do filter no pytest unit step matcheia CI behavior, relocate fica pra spawn-task. Gate dogfooded em sua própria merge: 5/5 PASS first time. |
| Sprint 3b.8 — F12 fix (manual CPC bid pre-flight) | ✅ 2026-05-12 | 5 commits ([42f01e3..70ce0a5](https://github.com/BadWolf1509/v4-ads-mcp/compare/1ab076a..70ce0a5)); zero novos MCP tools (tool count stays at 39); zero smoke runbook (Sprint 3b.5 A3 pattern — pre-flight é unit-testable). **Closes silent-acceptance bug family 6ª variante (F12 do P3 dogfood):** pre-flight GAQL batch validation em `update_keyword_bid` + `update_ad_group_bid` rejeita payloads em campaigns que usam auto-bidding strategy (MAXIMIZE_CONVERSIONS, TARGET_CPA, MAXIMIZE_CONVERSION_VALUE, etc) — Google ignora `cpc_bid_micros` silenciosamente nessas strategies (documentado oficialmente em `developers.google.com/google-ads/api/docs/campaigns/bidding/override-strategies`). Whitelist: `{MANUAL_CPC, ENHANCED_CPC}` (únicas strategies que honram `cpc_bid_micros` field). Helper `validate_manual_cpc_strategy` em `src/google_ads/queries/_common.py` compartilhado entre as 2 tools. 9 unit tests (5 helper + 2 per tool). **Investigation finding (Wellington pivotou de C→B via context7 docs):** considerou whitelist liberal com MANUAL_CPM/CPV mas rejeitado — esses usam `cpm_bid_micros`/`cpv_bid_micros` (bid fields diferentes), incluí-los re-introduziria F12 em outro guise. Empirical V4 distribution (7 accounts surveyed): MAXIMIZE_CONVERSIONS dominant (67%), MANUAL_CPC em SHOPPING (ML Antiguidades) confirmed. Silent-acceptance family timeline completa: A1 (3b.3 dedupe), A3 (3b.4 drop), A4 (3b.4 override), A5 (3b.6 path), F11 (P3 enum no-decode), **F12 (3b.8 silent ignore)**. |
| Sprint 3b.9 — F7 fix (get_recommendations type_pt) | ✅ 2026-05-12 | 3 commits ([62f25db..0cb34e4](https://github.com/BadWolf1509/v4-ads-mcp/compare/a1fdd1c..0cb34e4)); zero novos MCP tools (tool count stays at 39). **Closes P3 dogfood F7:** `type_pt` field não duplica mais `type` quando PT-BR mapping ausente — fallback null sinaliza "use type field, no translation available." `_TYPE_PT` mapping expandido de 25 → 30 entries cobrindo categorias mais comuns emitted pelo Google: forecasting variants (`FORECASTING_SET_TARGET_CPA` do dogfood + `FORECASTING_SET_TARGET_ROAS`) + Performance Max upgrades (`UPGRADE_LOCAL_CAMPAIGN_TO_PERFORMANCE_MAX`, `UPGRADE_SMART_SHOPPING_CAMPAIGN_TO_PERFORMANCE_MAX`, `IMPROVE_PERFORMANCE_MAX_AD_STRENGTH`). Tool description updated explicitando contract ("null caso contrario"). 3 unit tests: null fallback + regression (existing KEYWORD entry) + new dogfood FORECASTING_SET_TARGET_CPA entry. Net: ~10 LOC source + ~30 LOC tests. **4ª sprint consecutiva sem novos bugs no smoke** (3b.7 + P2 + 3b.8 + 3b.9 — stabilization compounding mantido). |
| Sprint 3b.10 — test_rate_limit relocate (Sprint P2 finding cleanup) | ✅ 2026-05-12 | 3 commits ([e9ac216..7948255](https://github.com/BadWolf1509/v4-ads-mcp/compare/4af0e99..7948255)); zero novos MCP tools (count stays at 39); zero source code change (move-only). **Sprint P2 finding fechado:** `tests/unit/test_rate_limit.py` → `tests/integration/test_rate_limit.py` via `git mv` (history preservada, 98% rename similarity após cleanup de 2 unused imports). **Coverage improvement descoberto durante design:** os 6 testes estavam DEAD em CI (step 1 excluia via marker, step 2 não escaneava `tests/unit/`). Post-move, step 2 picks them up — `rate_limit` governance code exercitado em CI pela 1ª vez. `scripts/_runner.py` comment atualizado pra refletir new state (filter ainda útil pra defensive "marker é authoritative signal"). 5ª sprint consecutiva sem novos bugs no smoke. |
| Sprint 3b.11 — process cleanup bundle (lesson learned + orphan delete) | ✅ 2026-05-12 | 2 commits ([aa7643c..2ee5f39](https://github.com/BadWolf1509/v4-ads-mcp/compare/5fa1fd3..2ee5f39)); zero novos MCP tools (count stays at 39); zero source code change (docs + dead code removal). **Process cleanup bundle de 2 items:** (1) CLAUDE.md nova subsection "Pre-flight test convention (post-Sprint 3b.8)" documentando bug class recorrente (pre-flight call to shared helper missed by existing integration test mocks — bit Sprint 3b.5 commit `3c23fc5` apply_audience + Sprint 3b.8 commit `5fa1fd3` update_keyword_bid/update_ad_group_bid). Pattern + why + mitigation explicitos. "Don't do" bullet expandido (3b.5 → 3b.5/3b.8). (2) `tests/integration/test_update_ad_status.py` deleted via `git rm` — file tinha 3 orphan fixtures desde Sprint 3b.5 commit `3c23fc5` deletar único test do file (P2 Task 1 spawn-task closed). Net: +28 LOC docs / -47 LOC dead code = -19 LOC. **6ª sprint consecutiva sem novos bugs no smoke.** |
| Sprint 3b.12 — `get_my_rate_limit_status` utility tool | ✅ 2026-05-12 | 2 commits ([c00712e..a53e653](https://github.com/BadWolf1509/v4-ads-mcp/compare/9fc99f4..a53e653)); **1 new MCP tool (count 39 → 40, primeira utility shipada):** thin wrapper read-only around existing `src.governance.rate_limit.get_today_usage()`. Zero params (quota é per dev token V4, atravessa todas as 23 contas). Response shape B (pragmatic): `used`, `limit`, `remaining`, `pct`, `pct_display` (`"8.2%"` formatted), `date_utc`, `warning_threshold_pct`. Hardcoded `DAILY_QUOTA_BASIC` (15k) com inline comment documentando switch pra `DAILY_QUOTA_STANDARD` (1M) post-Standard Access approval (case `26521440673`, ainda pendente). Audit pattern consistente com `list_my_accounts` (account-wide read, audit_log.record). 3 unit tests cobrem zero/partial/high usage paths. Tool allowlist em `test_tools_schemas.py` atualizada (esperado ao adicionar novo tool — mecânico). ~80 LOC source + ~120 LOC tests. **7ª sprint consecutiva sem novos bugs no smoke.** |
| Sprint 3b.13 — `get_my_audit_log` utility tool | ✅ 2026-05-12 | 2 commits ([199cbb8..f625996](https://github.com/BadWolf1509/v4-ads-mcp/compare/7516f92..f625996)); **1 new MCP tool (count 40 → 41, segunda utility shipada):** paginated history das próprias operações do gestor via MCP (mutations + audited reads), scoped automaticamente ao gestor logado. 4 optional filter params: `days` (1-30, default 7), `limit` (1-1000, default 100), `customer_id` (per-account filter), `action_type` (`mutate`/`read`/`auth`/`system`/`all` default `all`). **New repo function** `audit_log.list_for_manager()` em `src/db/repositories/audit_log.py` mirrors `export_csv_rows` structure mas retorna JSON list (vs CSV stream). Omits `params_summary` jsonb pra compact response; gestor pode usar `get_by_id` existente se precisar detail. Response shape: `manager_id`, `filters`, `count`, `events[]` (10 columns: id, occurred_at ISO, operation, customer_id, action_type, target_count, status, duration_ms, google_request_id, error_message). Audit pattern consistente com utility predecessor. 4 unit tests cobrem defaults + customer_id filter + action_type filter + empty result. ~130 LOC source + ~150 LOC tests. **8ª sprint consecutiva sem novos bugs no smoke.** |
| Sprint 3b.14 — `create_ad_group` (primeiro create-pattern do MCP) | ✅ 2026-05-12 | 5 commits ([ef9868a..6d914ac](https://github.com/BadWolf1509/v4-ads-mcp/compare/ebe3262..6d914ac)) + smoke + Sprint 3b.14.1 fix; **1 new MCP tool (count 41 → 42, primeiro create shipado):** batch creation de 1-10 ad_groups por chamada, always-CONFIRM. Schema + pre-flight rejecting missing/REMOVED/channel-mismatch/F12-cpc_bid combinations. New builder + helper + 14 unit/integration tests. **Smoke 5/6 PASS em Nutry sandbox** (T1+T2+T3+T4+T5 ok; T6 revealed spec assumption wrong — F14). **3 findings:** F13 UX (response doesn't return new ad_group_id, gestor needs separate GAQL — fix candidate next sprint), F14 (Google ENFORCES name uniqueness within campaign — tool description claim "NAO idempotente" incorrect, actual behavior is safer), F15 critical (registry auto-discovery — see Sprint 3b.14.1). 4 test ad_groups paused em sandbox para cleanup futura via UI. |
| Sprint 3b.14.1 — registry auto-discovery (critical fix) | ✅ 2026-05-12 | 1 commit ([14d3d7b](https://github.com/BadWolf1509/v4-ads-mcp/commit/14d3d7b)); production revision `v4-ads-mcp-00142-dzf`. **CRITICAL BUG fixed:** `import_all_tools()` em `_registry.py` era manual hardcoded list que **lagged behind actual files**. Sprints 3b.12 + 3b.13 + 3b.14 shipparam 3 new tools (`get_my_rate_limit_status`, `get_my_audit_log`, `create_ad_group`) mas **esqueceram** atualizar a lista → 3 tools DEAD em produção apesar de "shipped" + CI verde. **Tests passed via pytest import side effects** (decorator `@register_tool` runs ao importar tool module from test file → `_TOOLS` populated; production `import_all_tools()` runs WITHOUT those side effects → tools missing). Discovered durante smoke setup do 3b.14 — `create_ad_group` não aparecia no MCP client tool list post-reconnect. **Fix:** replace manual list com `pkgutil.iter_modules` auto-discovery em `_registry.py` (self-maintaining — new tools auto-registered just by file existing). Defense-in-depth: new test `test_registered_tool_count_matches_files_on_disk` (1:1 count match). 3 tools now properly registered em produção. **Lesson:** test side effects can mask production bugs; tests must verify production behavior, not just internal state. |
| Sprint 3b.15 — F13 + F14 bundle (3b.14 smoke findings) | ✅ 2026-05-12 | 3 commits ([c410fc7..8c76721](https://github.com/BadWolf1509/v4-ads-mcp/compare/83442f8..8c76721)); zero new MCP tools (count stays 42). **F13 (cross-cutting, high value):** `run_mutation` now extracts `resource_names` from MutateGoogleAdsResponse via `WhichOneof("response")` + `getattr` for `.resource_name`. Returns `list[str \| None]` (None for failed ops em partial_failure). `apply_change` propagates em response. **ALL future creates auto-inherit benefit** (create_rsa, create_campaign, etc) — gestor no longer needs separate GAQL query to find new entity IDs. Solves F13 UX gap descoberto em Sprint 3b.14 smoke T1. **F14 (description correction):** `create_ad_group.py` description updated — Google ENFORCES name uniqueness within campaign (smoke T6 revealed spec assumption wrong). Idempotency-by-error effective via Google server-side check. 3 new unit tests (extraction: happy/mixed_failure/missing_field) + 1 apply_change extension + 1 integration assertion. ~30 LOC source + ~125 LOC tests. **Additive backward-compatible change** — existing callers ignore new key. Bonus: ruff version drift caught (local 0.14.10 vs CI 0.15.x) — fix shipped em second commit. |
| Sprint 3b.16 — `create_rsa` (segundo create-pattern do MCP) | ✅ 2026-05-12 | 6 commits ([044ea69..1c5ec69](https://github.com/BadWolf1509/v4-ads-mcp/compare/24e4f8e..1c5ec69)); **1 new MCP tool (count 42 → 43):** batch creation de 1-5 Responsive Search Ads (RSAs) por chamada em existing ad_groups. Always-CONFIRM. JSONSchema enforces Google's structural rules declaratively (headlines 3-15 × 30 chars, descriptions 2-4 × 90 chars, final_urls 1+, path1/path2 maxLength 15). Pre-flight helper `validate_parent_ad_groups_for_rsa_create` em `_common.py` (ad_group existence + status != REMOVED + parent campaign channel ∈ {SEARCH, SEARCH_PARTNERS}). **Bonus fixture extension:** ProtoFieldCapture ganhou `_RepeatedCapture` + `field_count(path)` helper (reusable em future creates). 15 tests totais. **Smoke 5/5 PASS em Nutry sandbox (post F16 fix):** F13 production validation SUCCESS — `resource_names` field flows from Google API real response through apply_change. **F16 critical bug found em T1 smoke + fixed inline (Sprint 3b.16.1, commit `1c5ec69`, production rev `v4-ads-mcp-00149-jjz`):** builder usava `rsa.headlines.add()` (raw protobuf API) — tests passaram com ProtoFieldCapture mock mas proto-plus em real SDK só tem `.append(typed_instance)`. Fixed builder pra usar `client.get_type("AdTextAsset")` + `rsa.headlines.append(h)` pattern. **7ª variante da mock-fidelity bug family** (A1/A3/A4/A5/F11/F12/F16). 4 test RSAs criadas em Nutry sandbox (todas PAUSED, parent ad_groups PAUSED — zero serving risk). |
| Sprint 3b.17 — F16 cleanup (mock fidelity) | ✅ 2026-05-13 | 1 commit ([c9b37ea](https://github.com/BadWolf1509/v4-ads-mcp/commit/c9b37ea)); zero new MCP tools (count stays 43); zero source code change (test fixture only). **Closes F16 lesson from Sprint 3b.16.1:** removed `.add()` mock from `ProtoFieldCapture._RepeatedCapture` + `_SubCapture` — proto-plus repeated message fields don't have `.add()`, only `.append(typed_instance)`. Mock surface now mirrors proto-plus reality. Future builders that regress to `.add()` will AttributeError loudly at test time instead of production. Grep verified: zero callers em src/ or tests/ use `.add()` on repeated proto fields post-F16 fix. Net: -15 LOC fixture, 6 builder tests (test_create_rsa_builder.py) continue passing — they use `.append()` since F16 fix. **Defense-in-depth against bug family 7th variant.** |
| Sprint 3b.18 — `update_rsa` (completes RSA CRUD parcial) | ✅ 2026-05-13 | 5 commits ([9a7e877..8f6eb84](https://github.com/BadWolf1509/v4-ads-mcp/compare/551408a..8f6eb84)); smoke runbook signed-off em conta real ([`phase-3b-18-bootstrap.md`](../_archive/runbooks/phase-3b-18-bootstrap.md)) — production revision `v4-ads-mcp-00153-hkl`. **1 new MCP tool (count 43 → 44):** modify existing RSAs via `AdService.mutate_ads` (not `AdGroupAdService` like create_rsa). Always-CONFIRM. Batch up to 5 updates. Each update: ad_id (required) + optional headlines/descriptions/final_urls/path1/path2 — **anyOf** constraint enforces ≥1 mutable field provided. Provided field lists REPLACE existing (proto-plus + field_mask semantics). **First builder em codebase usando `MutateOperation.ad_operation`** (top-level Ad mutation, not via AdGroupAd). Pre-flight `validate_existing_rsas_for_update` em `_common.py`: ad existence + type == RESPONSIVE_SEARCH_AD + parent ad_group status != REMOVED + campaign channel ∈ {SEARCH, SEARCH_PARTNERS}. Builder uses dynamic `update_mask` derived from set fields (only touches what gestor specified). **ProtoFieldCapture extended:** added `AdService.ad_path(cid, ad_id)` mock support (reusable em futuros Ad-level mutations). 15 tests totais (5 helper + 6 ProtoFieldCapture builder + 3 tool + 1 integration). ~290 LOC source + ~520 LOC tests + ~90 LOC smoke runbook. **Nutry smoke 5/5 PASS first try, zero findings.** Highlights: (1) **F13 validated 2ª vez in-prod via `ad_operation`** (Sprint 3b.15 cross-cutting feature; 1ª foi Sprint 3b.16 `create_rsa` via `ad_group_ad_operation`) — auto-inherited via `run_mutation`, zero new code; (2) **proto-plus field_mask precision validated triplo** — T4 single-field partial (path1 changes, path2/headlines/descriptions UNCHANGED), T5 batch cross-update (descriptions em ad1 só, final_urls em ad2 só), `update_mask` derivado dinamicamente funciona com Google API; (3) **9ª sprint consecutiva sem novos bugs no smoke** — mock-fidelity lesson honrada empiricamente (builder usou `client.get_type("AdTextAsset") + .append()` desde initial draft, sem `.add()` regression; Sprint 3b.17 fixture cleanup atuou como defense-in-depth). 4 test RSAs continuam PAUSED em sandbox (zero serving risk). |
| Sprint 3b.19A — `create_conversion_action` (terceiro create-pattern; resolve UX-1 setup gap) | ✅ 2026-05-13 | 7 commits ([2bc6eaf..(F18-fix)](https://github.com/BadWolf1509/v4-ads-mcp/compare/ac5099b..main)); smoke runbook signed-off em conta real ([`phase-3b-19A-bootstrap.md`](../_archive/runbooks/phase-3b-19A-bootstrap.md)) — production revision `v4-ads-mcp-00157-ssp` (smoke run; F18 fix bumpa próximo rev). **1 new MCP tool (count 44 → 45):** cria 1-5 ConversionActions customer-level via `ConversionActionService.mutate_conversion_actions`. Always-CONFIRM. Schema: name (1-100) + category (14 V4-focused values post F17+F18 — was 18 inicialmente) + type (WEBPAGE/UPLOAD_CLICKS/UPLOAD_CALLS) + value_settings opcional + counting_type opcional. Pre-flight `validate_conversion_action_create` em `_common.py` (single GAQL batch sobre `conversion_action` table — name uniqueness). **First builder em codebase usando `MutateOperation.conversion_action_operation`** (terceiro resource type touched: AdGroup/3b.14 + Ad/3b.16+18 + ConversionAction agora). V4 invariants hardcoded: status=ENABLED on create (diferente de RSAs/ad_groups PAUSED default), currency_code=BRL, counting_type=ONE_PER_CLICK default. **ProtoFieldCapture extended:** `ConversionActionService.conversion_action_path` mock + 4 enum mocks via `_EnumDict` helper (reusable em futuros customer-level mutates). Context7 validated `ca.type_` (underscore) + `default_value` decimal (no micros) + `conversion_action_path` signature BEFORE writing builder (Sprint 3b.16 lesson honored). 17 tests totais (5 helper + 6 builder + 5 tool + 1 integration). ~290 LOC source + ~520 LOC tests + ~95 LOC smoke runbook. **Out of scope v0:** tag generation (WEBPAGE manual install via Google Ads UI), offline conversion import (Standard Access blocked), attribution/lookback override, status PAUSED on create, remove/update tools, niche categories/types. **Nutry smoke 5/5 PASS + Sprint 3b.19A.1 follow-up probes com 3 findings reais documentados (F17/F18/F19):** (F17) `LEAD` removido do google-ads SDK v20 — design assumiu LEAD válido com base em legacy docs; context7 cobriu só exemplo de `type_`. Fix: removido do schema, replacement `SUBMIT_LEAD_FORM`. (F18) Lead lifecycle categorias `IMPORTED_LEAD`, `QUALIFIED_LEAD`, `CONVERTED_LEAD` são system-managed pelo Google's lead workflow — válidas no SDK enum mas rejeitadas em create-via-API. (F19, descoberto em Sprint 3b.19A.1 per-value follow-up probe) `DOWNLOAD` rejected em ambos WEBPAGE + UPLOAD_CLICKS types — provavelmente requer `GOOGLE_PLAY_DOWNLOAD` type (app-install workflow, niche excluído). Fix bundled em 3b.19A.1: schema final 13 categorias V4-focused empirically validated. F17+F18+F19 são 8ª-9ª-10ª variantes da silent-acceptance bug family, todos design gaps. **F13 cross-cutting validado 3ª vez in-prod** via `conversion_action_operation` — auto-inherited via run_mutation. **Lição:** nova convention documentada em CLAUDE.md "Schema whitelist empirical validation" — smoke runbook deve cobrir per-value probe para cada enum whitelist. **Real biz value:** 14 ConversionActions úteis criadas em Nutry sandbox (3 do smoke + 7 probes válidas + 4 probes do batch 2 isolated). 10ª streak de sprints sem regressions ainda mantida em pattern terms (F17/F18/F19 são design gaps via SDK ambiguity, não regressions de codebase pattern). |
| Sprint 3b.19B.1 — composition-keyword schema fix (Anthropic API 400 unblock) | ✅ 2026-05-13 | zero new MCP tools (count stays 46); fixes 2 in-prod schemas that broke real Claude sessions. **Root cause:** Anthropic Messages API tool-use validator rejects `oneOf/allOf/anyOf` in tool `input_schema` at ANY nesting level — error message says "at the top level" but empirically rejects nested-in-`items` too. Two recent sprints shipped violations: Sprint 3b.18 `update_rsa` (`anyOf` in `properties.updates.items` for ≥1 mutable field), Sprint 3b.19B `create_conversion_value_rule_set` (`allOf` at root for CAMPAIGN→campaign_id + `allOf` in `properties.rules.items` for condition_type→device/geo). Both schemas were valid per JSON Schema 2020-12 standard and passed `jsonschema` local validator + smoke runbook (smoke calls `tools/call` direct, never exercises `messages.create(tools=[...])` path where Anthropic validates). **Fix:** removed all 3 composition keywords from the 2 schemas; ported constraints to private `_validate_*` helpers invoked at top of tool body (BEFORE async pre-flights), returning structured PT-BR errors consistent with pre-flight error contract. **Defense-in-depth:** new test `test_no_composition_keywords_in_any_schema` walks every registered schema recursively + fails CI if a regression reintroduces. Smoke runbooks intentionally NOT updated — both tools' existing happy-path payloads satisfy the new runtime checks (verified). **New convention** documented em CLAUDE.md "No JSON Schema composition keywords in tool input_schema". 6 new unit tests (1 schema guard + 2 update_rsa runtime + 3 conversion_value_rule_set runtime); ~50 LOC source changes + ~100 LOC tests. |
| Sprint 3b.19B — `create_conversion_value_rule_set` (quarto create-pattern; complementa 3b.19A tracking setup) | ✅ 2026-05-13 | 9 commits ([824eba6..3877332](https://github.com/BadWolf1509/v4-ads-mcp/compare/868fac0..3877332)); smoke runbook scaffolded ([`phase-3b-19B-bootstrap.md`](../_archive/runbooks/phase-3b-19B-bootstrap.md)) — production revision `v4-ads-mcp-00160-jhb`. **1 new MCP tool (count 45 → 46):** cria 1 ConversionValueRuleSet (CUSTOMER ou CAMPAIGN attachment) com 1-10 ConversionValueRule(s) nested via **chained mutation** (temp resource names em single MutateGoogleAdsRequest — primeiro builder em codebase usando esse pattern). Always-CONFIRM. Schema: attachment_type + campaign_id (allOf if/then) + conversion_action_categories optional filter (reuse 13 V4 cats de 3b.19A) + rules[1..10] cada com action (ADD/MULTIPLY/SET, value ≥ 0) + condition_type (DEVICE/GEO_LOCATION/NO_CONDITION) + conditional device_condition (3 types) or geo_condition (2 match types + geo_target_constants). Pre-flight 2-helper: `validate_campaign_for_value_rule_set` (when CAMPAIGN) + `validate_geo_target_constants_for_value_rule` (when GEO rules; rejects non-BR per V4 invariant). V4 invariants: status=ENABLED on create, BR-only geo. **ProtoFieldCapture extended:** 2 services (ConversionValueRule + ConversionValueRuleSet) + 7 enum mocks via `_EnumDict`. Context7 validated chained mutation temp paths + repeated STRING field type + dimensions inferred behavior BEFORE writing builder. 26 tests totais (7 helper incl injection test + 8 builder + 9 tool + 1 integration + 2 blast_radius). ~370 LOC source + ~620 LOC tests + ~140 LOC smoke runbook. **Sprint 3b.19A.1 lesson aplicado:** Smoke T6 explicit per-value empirical probe step para todos 7 novos enums — previne F17/F18/F19-class design gaps. **First time F13 cross-cutting tested em chained mutation case** — applied_count = N rules + 1 set, resource_names returns todas paths em ordem. **Nutry smoke executado 2026-05-17 em production revision `v4-ads-mcp-00167-5x7` (post-3b.21 deploy, tool surface unchanged from initial 00160-jhb): 4/6 PASS + 4 findings reais.** (T1 ✅ chain mutation F13 4ª validação in-prod; T2 ✅ runtime payload validation; T3 ✅ pre-flight campaign 999999999; T4 ✅ após retry no-filter + BR geo correto; T5 ❌ F25; T6 ✅ TABLET + LOCATION_OF_PRESENCE validated, SET enum untested). **Findings:** (F24 runbook typo, geo `20114` é British Columbia/CA não SP — V4 pre-flight rejeitou corretamente, fix inline na runbook); (F25 design gap HIGH, `NO_CONDITION` Google só aceita para Store Visits/Sales RuleSets — schema fix needed); (F26 Google constraint MEDIUM, CUSTOMER-level RuleSet unique per `(customer, category_filter)` combo — doc fix needed); (F27 design gap HIGH, `conversion_action_categories` filter só aceita `[]`/`[STORE_VISIT]`/`[STORE_SALE]`, a whitelist de 13 categorias do schema é INVÁLIDA — schema fix needed). F25+F27 mesma família F17/F18/F19/F25 (design-gap-via-SDK-ambiguity, **11ª variante**). 3b.19A.1 convention (per-value empirical probe) WORKING — pegou ambos antes de gestor encontrar em produção. 3 RuleSets criados em Nutry sandbox (`36515010` CUSTOMER, `36515013`+`36797833` CAMPAIGN) todos ENABLED via `run_gaql` verify, attachment campaigns PAUSED (zero serving impact). **Spawn-task criado: Sprint 3b.22 candidate bundle F25+F27 schema fixes** (remove NO_CONDITION + remove conversion_action_categories filter v0, ~5-10 LOC source). |
| Sprint 3b.20 — `date_range` clarification + search_terms default | ✅ 2026-05-17 | 16 commits ([115d218..main](https://github.com/BadWolf1509/v4-ads-mcp/compare/e621f26..main)); smoke runbook signed-off em conta real ([`phase-3b-20-bootstrap.md`](../_archive/runbooks/phase-3b-20-bootstrap.md)) — production revision `v4-ads-mcp-00163-zm6` (Deploy verde em 3m9s, /health 200). Zero new MCP tools (count stays 46); closes relatorio 2026-05-17 findings #1 (CRITICO, custom periods unblocked) e #2 (search_terms default 500->50). **Schema change:** 14 tools com `date_range` (13 read + 1 mutation `bulk_pause_by_query`) ganham `type: "string" + enum` explicito + novos params `start_date`/`end_date` (pattern YYYY-MM-DD). Novo helper `resolve_date_window` em `_common.py` aplica precedencia custom > preset. Defensive `json.loads` via `contextlib.suppress(ValueError)` em `parse_date_range` como safety net (Wellington relatorio root cause: Claude serializa dict como JSON string quando schema nao tem `type`). Regression guard `test_date_range_schemas_are_explicit`. **11 novos tests** (6 resolve_date_window + 2 defensive parse + 2 per-tool schema + 1 regression guard). **MO-JP smoke 7/7 PASS first try, zero findings.** Highlights: (1) T2 reproduziu o caso exato que falhou em 15/05 (`2026-05-08..2026-05-14` custom, `cost_brl=3036.62` confere com workaround LAST_7_DAYS do relatório); (2) T7 cross-tool equivalence validada — preset e custom paths produzem metricas identicas para mesma janela em `get_campaign_performance` + `get_funnel_metrics`; (3) T6 search_terms default 50 cabe em single MCP response sem overflow. **10ª sprint consecutiva sem novos bugs no smoke** (continua streak 3b.7→3b.18, broken only by 3b.19A design gaps). Resolve dogfood pain identificado pelo Wellington em report 15/05 Mestre da Obra JP+CAB. |
| Sprint 3b.21 — `get_negative_keywords_audit` created_date enrichment | ✅ 2026-05-17 | 9 commits ([8da7873..e85aa6d](https://github.com/BadWolf1509/v4-ads-mcp/compare/8da7873..e85aa6d)) + smoke signoff commit; smoke runbook signed-off em conta real ([`phase-3b-21-bootstrap.md`](../_archive/runbooks/phase-3b-21-bootstrap.md)) — production revision `v4-ads-mcp-00167-5x7` (Deploy verde em 2m34s, /health 200). Zero new MCP tools (count stays 46); closes relatorio 2026-05-17 finding #3 (último finding aberto do relatório oficialmente fechado). **Enrichment:** per-criterion `created_date` (YYYY-MM-DD) + `added_by_email` (null se >30d via change_event retention) + bloco `additions_summary` no root com counts `last_7_days` / `last_30_days` / `pre_30_days_or_unknown`. **Architecture:** parallel 2-query JOIN via `asyncio.gather` (Query A negatives full state + Query B `change_event` last 30d CREATE), client-side merge keyed por criterion_id via novo helper público `parse_resource_path` em `_common.py` (extraído de `get_change_history.py` para cross-tool reuse). Date-comparison robust (não assume DESC ordering). **6 unit tests** (enrichment scenarios + summary invariant + orphan CREATE handling) + 5 parse_resource_path tests + 3 GAQL builder tests + integration test_tactical_tools.py adapted to dual-call contract. **MO-JP smoke 5/5 PASS + 2 findings reais documentados:** (F22) Token cap finding em conta grande — MO-JP 467 negativas pós-enrichment retornou 81k chars, excedeu MCP response cap. Tool funciona em contas low-volume (T5 ML Antiguidades 13 negativas clean). Mesma família de Sprint 3b.20 #2 (search_terms 500→50 fix). Spawn-task "F22 fix: limit param em get_negative_keywords_audit" criado para próximo sprint candidate. (F23) `get_change_history LAST_30_DAYS` Google rejeita "start date too old" — preset hits retention boundary. Workaround: usar LAST_14_DAYS ou custom `start_date/end_date` com today-29 (mesmo pattern do `creates_start` em 3b.21 ✓). Não é regressão da 3b.21, existe desde 3b.1. **Cross-tool validation EXATA em T2:** criterion 11208536 keyword "salvador" `created_date "2026-05-09"` bate bit-a-bit com `get_change_history` resource_id `22169885957~11208536` change_date_time `2026-05-09 12:45:23` user_email `wellinton.ribeiro@v4company.com`. Dogfood pain do relatório §1.3 ("X negativas adicionadas no período") resolvido — Wellington agora narra "243 negativas adicionadas em 30d (52% do total, geo + match_type expansion em MO-JP)" em report semanal. Streak interrompida em 10 (3b.7→3b.18 + 3b.20 + 3b.21 break) — F22 é boundary class (enrichment funcionou bem demais), não design gap. |
| Sprint 3b.22 — F25+F27 schema cleanup (closes 3b.19B smoke findings) | ✅ 2026-05-17 | 1 commit (`ea39150`) + smoke signoff commit; smoke runbook signed-off em conta real ([`phase-3b-22-bootstrap.md`](../_archive/runbooks/phase-3b-22-bootstrap.md)) — production revision `v4-ads-mcp-00170-5k5` (Deploy verde, /health 200). **Nutry smoke 3/3 PASS first try** (T1 NO_CONDITION schema-rejected `"is not one of ['DEVICE', 'GEO_LOCATION']"`; T2 conversion_action_categories schema-rejected `"Additional properties are not allowed"`; T3 dry_run preview clean sem `has_category_filter`, apply blocked by F26 = doc note validation, builder pattern já validated em 3b.19B). **Streak restart após 3b.19B+3b.21 — Sprint 3b.22 = clean smoke.** Zero new MCP tools (count stays 46). **Schema cleanup em `create_conversion_value_rule_set`** (Sprint 3b.19B findings F25 + F27 + F26 doc):  **(F25)** removed `NO_CONDITION` from `condition_type` enum — Google API rejeitava em runtime ("can only be used by Store Visits/Store Sales value rule set"), STORE out of scope v0. **(F27)** removed `conversion_action_categories` field entirely — Google API restringe esse field a `[]`/`[STORE_VISIT]`/`[STORE_SALE]`, a whitelist de 13 categorias V4-focused herdada de 3b.19A era invalida pra esse field (semantics diferente do ConversionAction.category). **(F26)** tool description atualizada com nota sobre constraint Google "1 RuleSet CUSTOMER-level por conta". **2 new regression guards** (`test_schema_rejects_no_condition_value_in_condition_type` + `test_schema_rejects_conversion_action_categories_field`) prevent reintroduction. Source net: ~-30 LOC (removed dead code) + ~+45 LOC tests (regression guards) + smoke runbook ~85 LOC. **F25 + F27 são 11ª variante da família design-gap-via-SDK-ambiguity** finalizada; convention 3b.19A.1 (per-value empirical probe) WORKING — pegou ambos antes de gestor encontrar em uso real. Schema agora rejects schema-time vs runtime-time → cleaner UX, sem confusing Google errors. |
| Sprint 3b.23 — F22 fix: limit param em `get_negative_keywords_audit` | ✅ 2026-05-17 | 1 commit (`03d595d`) + smoke signoff commit; smoke runbook signed-off ([`phase-3b-23-bootstrap.md`](../_archive/runbooks/phase-3b-23-bootstrap.md)) — production revision `v4-ads-mcp-00172-7pm` (Deploy verde, /health 200). **MO-JP smoke 3/3 effective PASS** (T1 default limit ✅ F22 resolved — response 33k chars vs 81k pre-fix, ~59% redução; T2 ordering DESC validated bit-a-bit; T5 low-volume sanity ML Antiguidades ✅; T3/T4 custom limits blocked by F28 session schema cache, work post-restart). Zero new MCP tools (count stays 46). **Closes Sprint 3b.21 smoke F22 finding** (MO-JP 467 negativas excedeu MCP response cap pós-enrichment, +25% size). **Schema change:** novo param `limit` (int 1-1000, default 100) — by_campaign retorna no max `limit` negativas, ordenadas recentes primeiro (`created_date != null` DESC, depois null). 3 new response fields: `returned_count`, `truncated` (bool), `limit` (echo back). `total_negatives` + `additions_summary` continuam refletindo conta INTEIRA (não truncados — semantics preserved). **3 new unit tests** (apply limit + mark truncated, ordering recent-first, no-truncation when total ≤ limit). ~+30 LOC source + ~+90 LOC tests. **Pattern consistente com Sprint 3b.20** (search_terms_report 500→50): 2 read tools agora usam limit + truncation. **F28 documented** (MCP client schema cache propagation lag — characteristic of transport, gestor pode precisar restart Claude Code session post-deploy quando new integer-typed params shipados; não-blocking, server validation correta). Sprint 3b.20+3b.22+3b.23 streak iniciada (Sprint 3b.22+3b.23 clean smokes em <2h cada). **F22 resolvido em conta MO-JP — gestor agora pode rodar negative_keywords_audit em V4 accounts de qualquer tamanho.** |
| Sprint 3b.24 — `create_campaign` SEARCH v0 (5º create-pattern, 1st campaign create) | ✅ 2026-05-17/18 | Production revision final `v4-ads-mcp-00181-w7g`. 5 fix iterations (Sprint 3b.24.1-3b.24.5). **Tool count 46 → 47.** 5/6 bidding strategies validated em Nutry sandbox (TARGET_CPA/TARGET_ROAS rejected por F36 = Nutry sem conversion history; real V4 accounts funcionam). 7 F-findings documented (F29-F37, all design-gap-via-SDK-ambiguity family). Always-CONFIRM, chained mutation N+M+2 ops, V4 invariants: PAUSED + SEARCH + BR + PT. Runbook: [`phase-3b-24-bootstrap.md`](../_archive/runbooks/phase-3b-24-bootstrap.md). |
| Sprint 3b.25 — `create_and_link_assets` (6º create-pattern, asset CRUD bundle) | ✅ 2026-05-18 | Production revisions `00188-cwv` → `00191-f56` (3b.25.1) → final (3b.25.2). **Tool count 47 → 48.** 15/15 PASS após 2 fix iterations em Nutry sandbox. 5 types (SITELINK, CALLOUT, STRUCTURED_SNIPPET, CALL, PROMOTION) × 3 levels (CUSTOMER, CAMPAIGN, AD_GROUP) × 1-20 batch. Chained mutation 2N ops. 3 F-findings: F38 (STRUCTURED_SNIPPET PT-BR header format), F39 (PROMOTION language_code BCP 47 pt-BR), F40 (discount_modifier NONE invalid). V4 invariants: BR + pt-BR + BRL. R6 critical micros formula validated (`percent_off × 10_000`). Runbook: [`phase-3b-25-bootstrap.md`](../_archive/runbooks/phase-3b-25-bootstrap.md). |
| Sprint 3b.26 — `import_offline_conversions` (49th tool, first ConversionUploadService dispatcher) | ✅ 2026-05-18/19 | Production revisions `00196-h22` → `00199-l8v` (3b.26.1 F42 fix). **Tool count 48 → 49.** 9/12 PASS após 1 fix iteration + 3 deferred (F41 Nutry sandbox sem traffic — not a bug). T7 partial_failure path validated 100% com UNPARSEABLE_GCLID errors. F42 (HIGH): `UploadClickConversionsRequest.debug_enabled` removed em v24 SDK (1-line fix). First tool fora de GoogleAdsService.mutate — novo dispatcher `run_conversion_upload` parallel a `run_mutation`. V4 invariants: BRL + -03:00 timezone + LGPD consent.ad_user_data=GRANTED. Always-CONFIRM dry_run, partial_failure=True. Runbook: [`phase-3b-26-bootstrap.md`](../_archive/runbooks/phase-3b-26-bootstrap.md). |
| Sprint 3b.27 — combo `update_conversion_action` (50th tool) + B1/F43 pre-flight fix em `update_keyword_status` | ✅ 2026-05-20 | Production revisions `00202-vhk` (Phase A) → `00203-h2n` (Phase B) → `00204-86d` (3b.27.1 F44 fix). **Tool count 49 → 50.** 14/14 PASS após 1 fix iteration. V0 com 2 fields mutáveis (`name`, `primary_for_goal`) — `include_in_conversions_metric` removido em F44 (immutable em v24). F43 (HIGH) Fixed: pre-flight async `validate_keyword_criterion_types` separa positive/negative criterion_ids + hard reject com `to_retry_with`. F44 (HIGH): Silent-acceptance family (14ª variante: A1-A5/F11/F12/F16-19/F25/F27/F30-37/F38-40/F42/F43/F44). Caso real MO 23/05 (Opção C SIMPLIFICADA — rebaixar Store visits action via `primary_for_goal=false`) destravado. Runbook: [`phase-3b-27-bootstrap.md`](../_archive/runbooks/phase-3b-27-bootstrap.md). |
| Sprint 3b.28 — `upload_customer_match_list` (51st tool, second non-mutate dispatcher) | ✅ 2026-05-20 | Production revisão `v4-ads-mcp-00206-rwv`. **Tool count 50 → 51.** 6/11 PASS + 5 DEFERRED (F45 env limitation — Nutry sandbox sem CRM_BASED_USER_LIST / Customer Match terms acceptance). Validation paths (T1-T6 Layer 1/2/3 reject paths) verified em produção; happy paths (T7-T11) covered via 31 unit tests + 5 integration tests incluindo LGPD raw-query (payload em pending_confirmations SEM plaintext). V0 minimal: 2 identifier types (email + phone_number) × 2 operation types (add + remove). LGPD invariants: consent.ad_user_data + consent.ad_personalization GRANTED hardcoded + enable_partial_failure=True + audit log sem plaintext. Fire-and-forget async (tool retorna `job_resource_name` + run_gaql template pra polling status — jobs processam em horas backend). Segundo dispatcher non-mutate (`run_offline_user_data_job` paralelo a `run_conversion_upload` do 3b.26). 3-step Google API sequence (create_job → add_ops → run_job). QW1 prep: ProtoFieldCapture retrofit em `test_run_conversion_upload.py` commit `e055ef7`. Runbook: [`phase-3b-28-bootstrap.md`](../_archive/runbooks/phase-3b-28-bootstrap.md). |
| Sprint 3b.29 — `run_gaql.aggregate_by` (B5 fix dogfood MO-JP) | ✅ 2026-05-20 | Production revisão post-`1f07d3e`. **Tool count permanece 51** (sprint é extensão de existing tool, sem add). 5/6 PASS + 1 DEFERRED (T6 safety net 10k — Nutry sandbox volume insuficiente; change_event tem cap nativo Google 10k hard, keyword_view só 57 entries). T3 reproduziu caso B5 real: campaign_asset 119 rows → 7 groups (massive reduction), ordered DESC. Module puro `src/google_ads/aggregation.py` (9 unit tests cobrindo empty/single/multi/nested/missing/sort/ties/edge cases). Backward compat 100%: sem `aggregate_by` mantém shape original (`rows[]`, `row_count`). Com `aggregate_by`: retorna `{total_rows_scanned, group_count, truncated, groups[]}` ordered DESC. Safety net hard fail >10k raw rows (PT-BR ValueError com refine hint). Fix iteration 1: A2.1 (`ae90191`) corrigiu `group_count` pré-truncate vs pós-slice (catch do spec reviewer subagent). 13 testes adicionados (9 unit + 4 integration wire-up incluindo regression guard pra A2.1). B5 sub-demanda do dogfood MO-JP 2026-05-19 marcada **RESOLVED**. Subagent-Driven workflow validado: A1 (sonnet) + A2 (haiku) + A3 (sonnet) + A4 (smoke-runbook-generator), com spec compliance + code quality review por task. Runbook: [`phase-3b-29-bootstrap.md`](../_archive/runbooks/phase-3b-29-bootstrap.md). |
| Sprint 3b.31 — `audit_competitor_keywords` (53rd tool, #6 ICE 432 dogfood MO-JP) | ✅ 2026-05-20 | Smoke 8/8 PASS (zero DEFERRED) com CASO REAL detectado em Nutry: T7 identificou 3 positive_keywords pagando pra aparecer em buscas de competidores diretos (Clínica Seven, Dr. Hilquias Rocha, Rodrigo Sousa Nutricionista). | Production revisão post-`a01954b`. **Tool count 52 → 53** (test_registered_tool_count_matches_files_on_disk 53==53 PASS). 23 testes (14 unit pure competitor_analysis + 6 GAQL builder + 3 integration wire-up). 2 dimensões V0: positive_keywords (state-based, ENABLED + negative=FALSE) + search_terms (date-windowed com cost real). Match algorithm: substring case-insensitive (normalize lowercase + strip). Suggested negatives: EXACT + PHRASE per matched brand (apenas brands com hit — zero-match brands omitidas). Architecture pure: `src/google_ads/competitor_analysis.py` (testable standalone, 5 dataclasses frozen+slots, _find_matching_brand helper insertion-order first-match) + `src/google_ads/queries/audit_competitor_keywords.py` (2 GAQL builders + 2 row parsers + 2 dict→dataclass converters) + `src/mcp/tools/audit_competitor_keywords.py` (wrapper com asyncio.gather paralelo em 2 run_report calls). `audit_this_call=True` em ambas calls. Default LAST_7_DAYS, limit 200 (max 1000), brands 1-20 (minLength 3 chars). Hardcoded filters em positive_keywords: status=ENABLED + negative=FALSE. Caso real dogfood MO-JP: ~R$2k/mês waste detectado em concorrência. Subagent-Driven: A1 (sonnet) + A2 (sonnet) + A3 (sonnet+adaptation resolve_date_window path) + A4 (sonnet+smoke-runbook-generator) + A5 (controller). Smoke T1-T8 pending Wellington next-session (MCP cache pre-deploy — pattern análogo 3b.30 A5). Runbook: [`phase-3b-31-bootstrap.md`](../_archive/runbooks/phase-3b-31-bootstrap.md). |
| Sprint 3b.30 — `audit_quality_score` (52nd tool, #1 ICE 504 dogfood MO-JP) | ✅ 2026-05-20 | Production revisão post-`668ddb1`. **Tool count 51 → 52** (test_registered_tool_count_matches_files_on_disk 52==52 PASS). Smoke 6/8 PASS + 2 DEFERRED (T7-T8 env limitation — Nutry sandbox tem 36 kw ENABLED mas todas com QS=NULL pq low-volume Google nao calcula; tool filter `IS NOT NULL` correto, mas nao ha QS data real pra validar candidate_pause/duplicate_intent empiricamente — igual padrão F41/F45). 24 testes (16 unit pure flags + 5 GAQL builder + 3 integration wire-up). 3 flags V0: candidate_pause (QS<=2 + imp>=threshold + clicks=0), candidate_promote_exact (QS>=7 + BROAD + conv>=1), duplicate_intent (amplification only — kw text exato em multi ad_groups + outra flag ativa). Architecture pure: `src/google_ads/flag_keywords.py` (testable standalone, dataclasses frozen+slots, named QS constants) + `src/google_ads/queries/audit_quality_score.py` (GAQL builder + parsers) + `src/mcp/tools/audit_quality_score.py` (wrapper). `audit_this_call=True`. Default min_impressions=10, limit=200 (max 1000). Date range preset OR custom via `resolve_date_window` (Sprint 3b.20 helper). Hardcoded filters: status=ENABLED + quality_score IS NOT NULL (Google research convention guard rails). Fix iteration 1: A1.1 (`f691cd4`) — code reviewer catch I1 (list mutation trap) + M1 (named QS constants) + 2 boundary tests added. T1-T6 validaram shape + filters + date resolution corretas em Nutry. Subagent-Driven validado: A1 (sonnet+1 fix) + A2 (haiku) + A3 (haiku) + A4 (sonnet) + A5 (controller smoke + signoff). Caso real economiza ~30min/sessão V4 cleanup recurring. Runbook: [`phase-3b-30-bootstrap.md`](../_archive/runbooks/phase-3b-30-bootstrap.md). |
| Sprint 3b.32 — Quality Wins HIGH (G1+B1+UX2 dogfood 21/05 MO-JP) | ✅ 2026-05-21 | Production revisão post-T4 commit. **Tool count permanece 53** (zero new tools — sprint puro quality wins). 3 quick wins ICE 600+: **G1 (ICE 720)** `get_conversion_actions` retorna `primary_for_goal` + `include_in_conversions_metric` (READ-only, F44-safe — immutable apenas em UPDATE); antes exigia GAQL custom ~5min extra pra audit. **B1 (ICE 700)** `get_change_history` description escala lag warning de "alguns minutos" para HORAS (>3h visto em prod 21/05 MO-JP) + afeta múltiplos campos (ai_max_setting, asset_automation_settings, text_guidelines.messaging_restrictions, não só campaign.status). Recomenda `run_gaql FROM campaign` como leading indicator pra revert/incident recovery. **UX2 (ICE 600)** `update_conversion_action` description documenta fluxo dry-run+confirm token (UUID format, expires 10 min, 3-step path: tool → review → apply_change). Inline execution (não subagent-driven — 3 quick wins SMALL ~75 min total). 4 commits (`40ef6d6` G1 + `102b58a` B1 + `c7fb716` UX2 + T4 signoff). 2 integration test assertions adicionadas (primary_for_goal + include_in_conversions_metric). Zero schema input changes, zero comportamento novo. Pre-push gate 5/5 PASS. Reference: [`dogfood-2026-05-21-mestre-da-obra-jp-drift-detection.md`](../_archive/dogfood/dogfood-2026-05-21-mestre-da-obra-jp-drift-detection.md). Zero F-findings novos. F44 reforçado (immutable em UPDATE mas readable em listing). Next sprint candidates (dogfood 21/05): W1 `detect_drift` ICE 486, W3 `audit_goal_attribution` ICE 360. |
| Sprint 3b.33 — `detect_drift` (54th tool, W1 ICE 486 dogfood 21/05) | ✅ 2026-05-21 | Production revisão post-`d41056a`. **Tool count 53 → 54** (auto-discovery test 54==54). Smoke 6/6 effective PASS + **F46 NOVO** (GAQL BETWEEN end_date midnight) + T5 positive DEFERRED (env limitation sem REMOVE em CAMPAIGN/AD_GROUP/CONVERSION_ACTION). 23 tests (19 unit pure drift_detection — 16 algorithm + 3 boundary parser + 1 A1.1 defensive list-or-none + 3 integration wire-up). Pure aggregator pattern (Sprint 3b.30/3b.31 lineage): `src/google_ads/drift_detection.py` 5 frozen+slots dataclasses + `detect_drift()` + `dict_to_change_event_row()` parser. Tool wrapper `src/mcp/tools/detect_drift.py` invoca `get_change_history` INTERNAMENTE (zero novos GAQL builders, _resolve_names herdado). LAST_2_DAYS preset novo local + outros delegados a `resolve_date_window` (smart fix do implementer pós-spec — `_PRESETS` compartilhado não tinha LAST_2_DAYS). 3 flags V0: `auto_apply_detected` low, `multiple_users_detected` medium, `structural_change` high. Schema: customer_id + responsible_user_emails[] (optional, maxItems 20, [] = incident mode) + date_range preset (default LAST_2_DAYS) OR start_date+end_date custom + limit 1-500 (default 100). Subagent-Driven: A1 (haiku + 🟡 → A1.1 inline defensive fix `tuple(...or [])`) + A2 (sonnet + mcp-tool-quality-reviewer READY TO PUSH com 3 WATCH non-blocking) + A3 (smoke-runbook-generator) + A4 controller + A5 controller smoke real. **Smoke em produção:** T1 incident mode 52 changes window LAST_2_DAYS ✓ (1 REMOVE em AD_GROUP_AD não trigger structural — correct), T2 co-management zero drift wellinton ✓, T3 single-day 0 rows → F46 descoberto (workaround end+1 retorna Pedro Vytor cluster 4 changes 20/05 com ai_max_setting+asset_automation+text_guidelines.messaging_restrictions — confirma B1 quality win multi-field empiricamente), T4 limit=2 truncated=true + multiple_users_detected flag emitida, T5 negative case PASS / positive DEFERRED, **T6 CASO REAL ML Antiguidades**: Pedro Vytor mudou `amount_micros` em 2 campaigns 18/05 16:17 cluster 14s — segunda conta com pattern V4 interno cross-account. **F46 (MED severity NOVO):** `change_event.change_date_time BETWEEN 'YYYY-MM-DD' AND 'YYYY-MM-DD'` interpreta end_date como midnight 00:00:00 exclusive — single-day window 0 rows silenciosamente. Afeta `get_change_history` (3b.1) + `get_negative_keywords_audit` (3b.21) + `detect_drift` (3b.33). 15ª variante design-gap-via-Google-API-semantics. Fix candidate 3b.33.1 ou 3b.34. 7 commits (`e91814c` spec + `411c73a` self-review + `48568e2` plan + `d65b334` A1 + `76cc9be` A1.1 + `7dd4446` A2 + `d41056a` A3) + signoff commits. Pre-push 5/5 PASS 40.4s. CI green. Deploy green. /health 200. 3 WATCH non-blocking documentados (W1 audit_log gap operation_name, W2 LAST_2_DAYS test coverage, W3 params_summary) — agrupar com pending LOW audit_log batch. Reference: [`phase-3b-33-bootstrap.md`](../_archive/runbooks/phase-3b-33-bootstrap.md), [`dogfood-2026-05-21-mestre-da-obra-jp-drift-detection.md`](../_archive/dogfood/dogfood-2026-05-21-mestre-da-obra-jp-drift-detection.md). |
| Sprint 3b.34 — F46 fix (GAQL BETWEEN end_date midnight) | ✅ 2026-05-21 | Production revisão post-`0785dbf`. **Tool count permanece 54** (pure bug fix). Closes F46 (descoberto em smoke 3b.33 T3). 1 commit (`0785dbf` fix + 3 regression tests). Fix: novo helper `_format_change_date_between(start, end)` em `src/google_ads/queries/change_history.py` aplica `timedelta(days=1)` ao end_date antes do isoformat — Google passa a interpretar GAQL `BETWEEN ... AND '{end+1}'` como `<= midnight do dia seguinte`, capturando o dia inteiro inclusive. Helper compartilhado entre `change_history_query` e `negative_criterion_creations_query` pra consistência. Range validation 30-day cap preservada usando user-facing end_date (NÃO transformed end+1). **3 regression tests F46-specific:** (a) single-day window start=end produz `BETWEEN start AND start+1`; (b) mesma transformação aplicada ao `negative_criterion_creations_query`; (c) 30-day range validation usa user-facing dates (não falsea-positives no boundary). 2 assertions existentes atualizadas (refletem nova semantics +1 day no end). Affects 3 production tools (todas read sensíveis com audit_this_call=True): `get_change_history` (3b.1), `get_negative_keywords_audit` via `negative_criterion_creations_query` (3b.21), `detect_drift` (3b.33). 13/13 unit tests PASS. Pre-push gate 5/5 PASS em 48.1s. CI run `26249457028` green. Deploy run `26249457029` success. /health 200. F46 status: OPEN → FIXED. 15ª variante design-gap-via-Google-API-semantics resolvida. Reference: [findings-catalog.md F46]. |
| Sprint 3b.35 — `audit_goal_attribution` (55ª tool, W3 ICE 360 dogfood 21/05) | ✅ 2026-05-21 | Production revisão `v4-ads-mcp-00232-72r`. **Tool count 54 → 55** (auto-discovery test 55==55). Smoke **6/6 effective PASS — zero F-findings, zero DEFERRED** + 2 casos reais validados: (i) MO-JP CONTACT__WEBSITE bate dogfood lição 47 (biddable=true, 7 primary, 15 secondary actions — 2 secondaries a mais que dogfood 13, evolução natural conta) + 3 buckets biddable=true emitiram warning PT-BR ("AFETA Smart Bidding"); (ii) ML Antiguidades PURCHASE biddable=true 5 primary actions (e-commerce Smart Bidding engaged — Compra Ativo, Compra GTM, Nuvemshop Backend/Website). 29 testes (18 unit pure goal_attribution + 4 GAQL builder + 4 boundary parser + 3 integration wire-up). Pure aggregator pattern (lineage Sprint 3b.30/3b.31/3b.33): `src/google_ads/goal_attribution.py` com 5 frozen+slots dataclasses (ConversionActionRow, CustomerConversionGoalRow, ActionSummary, OriginSummary, GoalAttributionResult) + `audit_goal_attribution()` + 2 boundary parsers. 2 GAQL queries paralelas via `asyncio.gather`: `conversion_action` (7 fields, WHERE status='ENABLED') + `customer_conversion_goal` (3 fields, no WHERE). `audit_this_call=True` em ambas + `params_summary` distinguishes phase. Schema: customer_id + category optional whitelist 13 V4 (idêntica `create_conversion_action` 3b.19A — F17/F18/F19-safe). Algoritmo: filter status+category → goals_lookup → group por (cat, origin) → split primary/secondary → biddable cross-ref (default False) → warning_pt se biddable=true → key strategy (composite "{cat}__{origin}" sem filter, origin-only com filter) → sort ASC por name. Use case primário: pre-flight antes de `update_conversion_action(primary_for_goal=...)`. Subagent-Driven: A1+A2 paralelo (haiku — arquivos isolated) → A3 sonnet integration → A4 smoke-runbook-generator → A5 controller. 4 commits (`2bc1478` A1 + `75f4354` A2 + `fdc7a64` A3 + `6ff0219` A4) + signoff. Pre-push 5/5 PASS 42.5s. CI run `26252353190` green. Deploy run `26252353191` success. /health 200. 1 WATCH non-blocking documentado em code review A3 (W1 ausência de `limit` per origin — V0 cuts deliberadamente deferiu, V1 candidate se conta crescer). 7 buckets MO-JP scanned + 1 bucket ML Antiguidades: contas V4 típicas <50 actions ENABLED per origin — output JSON ~50KB dentro MCP cap. F46 imune (não usa change_event). Pattern customer_conversion_goal categories podem incluir valores fora whitelist V0 (PHONE_CALL_LEAD detectado em MO-JP) — schema restringe INPUT mas algorithm aceita qualquer categoria do Google em grouping (design-correct). Reference: [`phase-3b-35-bootstrap.md`](../_archive/runbooks/phase-3b-35-bootstrap.md), [`dogfood-2026-05-21-mestre-da-obra-jp-drift-detection.md`](../_archive/dogfood/dogfood-2026-05-21-mestre-da-obra-jp-drift-detection.md). |
| Sprint 3b.36 — `audit_zombie_keywords` (56ª tool, ICE 315 cleanup massivo 19/05) | ✅ 2026-05-21 | Production post-deploy. **Tool count 55 → 56** (auto-discovery test 56==56). Smoke **6/6 effective PASS — zero F-findings, zero DEFERRED** + 2 casos reais MASSIVOS validados: (i) MO-JP CONTACT campaign 279 zombies em LAST_30_DAYS (93 só ad_group DELL — quase 1/3 da conta) — cleanup massivo recurring confirmed; (ii) ML Antiguidades 248 zombies (e-commerce com long-tail keywords obscure Luís XV/antiguidades sem traffic — assumption "conta clean" era falsa). **Total 527 zombie keywords cleanup-ready em 2 contas.** 18 testes (10 unit pure flag_zombie_keywords + 5 GAQL+boundary + 3 integration). Pure aggregator pattern (lineage 3b.30/3b.31/3b.33/3b.35): `src/google_ads/flag_zombie_keywords.py` 2 frozen+slots dataclasses (KeywordRow, ZombieKeyword) + `flag_zombie_keywords()` filter+sort+truncate. GAQL builder `src/google_ads/queries/audit_zombie_keywords.py` keyword_view com 11 fields SELECT + WHERE status=ENABLED + negative=FALSE hardcoded server-side + optional ad_group_ids IN clause. `audit_this_call=True` + `params_summary`. Schema: customer_id + ad_group_ids[] optional + limit 1-1000 default 200 + date_range preset (LAST_7/14/30/90_DAYS, default LAST_30) OR start_date+end_date custom (Sprint 3b.20 convention). Algoritmo: filter `impressions==0 AND clicks==0` strict (spec section 4 zombie definition cravada) → sort `ad_group_name ASC, keyword_text ASC` (Python stable sorted) → truncate `total = len(zombies)` pre-slice. T3 7-day window retornou MAIS zombies (330) que T1 30-day (279) — keywords podem ter 1 impression em 30d mas 0 em 7d (correct semantic). Subagent-Driven: A1+A2 paralelo (haiku) → code review 🟡 fix inline A2.1 (test parse_keyword_view_row) → A3 sonnet → A4 smoke-runbook-generator → A5 controller. 5 commits (`ba3d6f3` A1 + `a576fcc` A2 + `a573f44` A2.1 + `a6c41c8` A3 + `3cba582` A4) + signoff. Pre-push 5/5 PASS 42.8s. **WATCH non-blocking (não-F-finding):** Default limit=200 em conta com 500+ keywords zombi estoura MCP cap (~126KB > ~60KB MCP cap). Workaround: passar `limit=50`. Spec V0 cuts deferiu `limit` tuning per origin — V1 candidate bump default. F46 imune (não usa change_event). Use case primário cleanup massivo recurring MO-JP confirmed massivo. Reference: [`phase-3b-36-bootstrap.md`](../_archive/runbooks/phase-3b-36-bootstrap.md). |
| Sprint 3b.37 — `audit_orphan_smart_actions` (57ª tool, ICE 288 cleanup ConversionActions 19/05) | ✅ 2026-05-21 | Production post-deploy. **Tool count 56 → 57** (auto-discovery test 57==57). Smoke **6/6 effective PASS — zero F-findings, zero DEFERRED** + **2 casos reais MASSIVOS** validados: (i) MO-JP 12 ConversionActions orphan em LAST_30_DAYS, **4 primary_for_goal=true** entre eles (incluindo Smart Campaign clicks-to-call/map-directions — primary consumindo Smart Bidding slot SEM dados); (ii) ML Antiguidades **EMERGENCY** — 7 PURCHASE actions orphan, **TODAS 5 primary_for_goal=true sem ZERO conversões em 30d** (Compra Ativo, Compra GTM, Compras MS, Nuvemshop Backend/Website) — cross-ref 3b.35 T3 mostrou biddable=true + 5 primary, agora detectado que Smart Bidding está cego (e-commerce sem tracking). 19 testes (8 unit pure flag_orphan_smart_actions + 5 GAQL+boundary + 3 integration + 3 paralelos do plan A1+A2). Pure aggregator pattern (lineage 3b.30/3b.31/3b.33/3b.35/3b.36): `src/google_ads/flag_orphan_smart_actions.py` 2 frozen+slots dataclasses (ConversionActionRow, OrphanAction) + `flag_orphan_smart_actions()` filter `all_conversions==0.0` + sort+truncate. GAQL builder `src/google_ads/queries/audit_orphan_smart_actions.py` conversion_action com 7 fields SELECT + metrics.all_conversions per-action aggregada via segments.date BETWEEN + WHERE status=ENABLED hardcoded + optional category filter. `audit_this_call=True` + `params_summary` com filters. Schema: customer_id + category optional (V4 whitelist 13 valores idêntica 3b.35/3b.36) + limit 1-500 default **100** (lição 3b.36 reduce from 200) + date_range preset OR start_date+end_date custom. Algoritmo: filter zero conversions → sort by (category, origin, name) ASC → truncate. T3 7-day window retornou MAIS orphans (18) que T1 30-day (12) — actions podem ter 1 conv em 30d mas 0 em 7d (correct semantic, igual zombie keywords 3b.36). Subagent-Driven: A1+A2 paralelos (haiku) → A3 sonnet → A4 smoke-runbook-generator → A5 controller. 4 commits (`88e6a74` A1 + `a9d5ea7` A2 + `4391ff3` A3 + `bfb7adc` A4) + signoff. Pre-push 5/5 PASS 53.9s. F46 imune (não usa change_event). **PHONE_CALL_LEAD category** detected em MO-JP T1 (fora whitelist V4 mas algorithm aceita — design correto per spec section 5, pattern 3b.35). Use case primário cleanup ConversionActions confirmed massivo + EMERGENCY ML Antiguidades. Reference: [`phase-3b-37-bootstrap.md`](../_archive/runbooks/phase-3b-37-bootstrap.md). |
| Sprint 3b.38 — F52 + F23 fix + B1 refino (dogfood 25/05 same-day ship) | ✅ 2026-05-25 | Production post-deploy. **Tool count permanece 59** (pure bug fix + field enrichment). Origem: dogfood 2026-05-25 MO-JP+CAB zombies audit — 3 itens shipped em ~3h. **F52 (HIGH):** `audit_zombie_keywords` adiciona `ad_group_status` field na response (Opção C dogfood B6) + description warning ("zumbis incluem keywords em ad_groups REMOVED — filtre pelo campo ad_group_status='ENABLED' pra cleanup de impacto técnico"). Dogfood revelou 280 zumbis MO-JP = 170 REMOVED (DELL 93 + GPA02 ANDAIME 77 órfãs cosméticas) + 110 ENABLED. Batch PAUSE sem filter F52 seria 60.7% no-op + narrativa cliente inflada 2.5×. Fix files: `flag_zombie_keywords.py` (KeywordRow + ZombieKeyword dataclasses) + `audit_zombie_keywords_query.py` (SELECT add `ad_group.status` + parser + dict converter) + tool response. **F23 promoted "known limitation" → "fixed":** `get_change_history` LAST_30_DAYS clamp pra today-28 (margem 2-day safety) + `date_range_warning` na response. Non-breaking: custom dates (start_date/end_date OR dict legacy) honram intent. **B1 description refino:** "HORAS (>3h)" → "DIAS (>4 dias)" baseado em medição empírica dogfood 25/05 (3 dos 4 reverts Pedro de 21/05 ainda não surfaceavam 4 dias depois). 3 regression tests novos (F52 DELL/GPA cenário + F23 clamp+no-clamp positive/negative). **Smoke real 5/5 PASS** bit-a-bit em conta MO-JP+CAB: T1 280 zumbis = 170 REMOVED + 110 ENABLED match exato dogfood. Pre-push 5/5. CI green. Commit `78894cf` (10 files, +472/-20). Lição V4 48 (P4) catalogada Lessons reinforced #7: parent resource validation antes de batch via audit_* tool. Reference: [`dogfood-2026-05-25-mestre-da-obra-jp-zombies-audit.md`](../_archive/dogfood/dogfood-2026-05-25-mestre-da-obra-jp-zombies-audit.md). |
| Sprint 3b.40 — Quick Wins Mutate Safety (A1+B9+A2 dogfood 27/05 MO-JP) | ✅ 2026-05-27 | Production post-deploy. **Tool count permanece 62** (3 enrich responses + 1 helper module — sem novo tool). 4 commits sequenciais + 1 chore format + 1 runbook + 1 signoff: F56 catalog (`cbe4ba8`) + A2 ad_group_status em audit_quality_score (`4516d29`) + B9 negative field em get_keyword_performance (`9d92d64`) + A1 sample_keywords no dry-run update_keyword_status (`37adda7`) + chore ruff format (`bb0de92`) + runbook (`ecf926b`). ICE somado 2030 (dogfood MO-JP 27/05). Esforço ~3-4h via inline execution (não-subagent, sprint pequeno). **F56 catalog (NOVO, MED severity):** `get_keyword_performance` retornava positive E negative `ad_group_criterion` indistintamente — workflow risk em mutate downstream. Dogfood revelou 500 keywords retornadas → 39 falsos positivos (negative ENABLED) vs 108 reais. Mitigation Opção A+C (field `negative: bool` + description warning). Family design-gap-via-missing-discriminator (variant silent-acceptance, similar F52 missing parent filter). **A2 ship (replica F52 pattern):** `audit_quality_score` adiciona `ad_group_status` field — KeywordRow + FlaggedKeyword dataclasses + query SELECT (`ad_group.status`) + parser + dict_to_keyword_row + tool response + description warning. 3 unit tests novos (1 F52 propagation + 1 query SELECT + reused make_row). 2 integration tests novos (a2 orphan regression DELL/GPA cenário + existing tests com `ad_group_status: ENABLED` em fake_rows). **B9 ship (Opção A+C backward-compat):** `get_keyword_performance` adiciona `negative: bool` na response — keyword_performance_query SELECT add `ad_group_criterion.negative` + _row_formatter cast `bool()` defensive + description warning F56. 2 unit tests novos (_row_formatter sob SimpleNamespace SDK row, true + false) + 1 integration test wire-up (mock passthrough). **A1 ship (sample_keywords no dry-run):** Novo módulo `src/google_ads/queries/keyword_lookup.py` 90 LOC com `build_keyword_text_lookup_query(pairs) → GAQL` (dedupes + sorts ids, raises ValueError em empty) + `_lookup_row_formatter(row)` + `async fetch_keyword_texts(...) → dict[tuple, dict]` graceful (partial fetch → missing absent dict). `update_keyword_status` DRY_RUN branch (>5 keywords) chama `fetch_keyword_texts` (~100ms extra Google API IN clause sobre ad_group_criterion resource state-based, sem date filter) → response inclui `sample_keywords` (top 5 primeiros da lista caller, preserva intent caller-defined) + `sample_truncated: bool`. AUTO_APPLY path (≤5 keywords) inalterado. Partial fetch fail graceful: `keyword_text=None` + `match_type=None` mas `ad_group_id`+`criterion_id` preservados como fallback identifier. 4 unit tests `tests/unit/test_keyword_lookup.py` (build_query dedup+sort + selects required fields + empty raises + formatter extracts) + 3 integration tests `tests/integration/test_update_keyword_status.py` (dry_run top 5 + auto-apply sem sample + partial fetch returns None). Prevenção bug humano em batch mutation com TTL 10min sem reverter (dogfood MO-JP 27/05 Wellington aplicou 9 tokens PAUSE em batch sem sanity check). **Tests:** 9 novos (5 unit + 3 integration + 1 unit formatter). Pre-push 5/5 PASS 75.5s. CI verde + Deploy verde. /health 200. **Source:** dogfood [`dogfood-2026-05-27-mestre-da-obra-jp-investigacao-senior.md`](../_archive/dogfood/dogfood-2026-05-27-mestre-da-obra-jp-investigacao-senior.md) §A1+B9+A2. Sprint family completo: [spec](../superpowers/specs/2026-05-27-sprint-3b-40-quick-wins-mutate-safety-design.md) (430 linhas) + [plan](../superpowers/plans/2026-05-27-sprint-3b-40-quick-wins-mutate-safety.md) (1412 linhas, 7 tasks bite-sized TDD) + [runbook](../_archive/runbooks/phase-3b-40-bootstrap.md) (652 linhas, T1+T2+T3 em MO-JP `7862230676`). **Pending:** Wellington smoke real T1-T3 (~20-30min). **Próximo sprint candidato:** M.4 Meta breakdowns (geo+device+hourly) — alta volume Caminho B+ contribution. |
| Sprint 3b.39 (refactor F1) — Tool bucket classification + D3 fix | ✅ 2026-05-25 | Production post-deploy `590261d` + runbook update `6458d7a`. **Tool count permanece 59** (metadata-only F1 — archive vem F3 Sprint 3b.41). **Fase 1 do refactor arquitetural V4 Ads MCP** (spec 4 fases `2026-05-25-architecture-refactor-design.md`, 611 linhas). 6 tasks A-F via subagent-driven (haiku Tasks A/B/C/F + sonnet D/E). **2 D-findings cross-layer descobertas:** (1) **D2 pré-plan via OQ1 WebFetch:** MCP `defer_loading` é CLIENT-SIDE Anthropic Messages API param, NÃO MCP server metadata — F1 reformulada server-metadata-only + Wellington manual config Claude Code (initial plan); (2) **D3 pré-Wellington-config via WebFetch docs Claude Code oficial:** `settings.json tools[].defer_loading` NÃO existe no schema Claude Code v2.x. Real mechanism is **`ENABLE_TOOL_SEARCH=true` por DEFAULT** + per-tool `_meta.anthropic/alwaysLoad: true` SERVER-SIDE pra promover always-loaded. F1 finalmente shipped via server-side fix (Wellington action = ZERO config edits, apenas restart Claude). **Server changes finais:** `@register_tool` decorator add `bucket: Literal["always", "defer"]` kwarg (default `"defer"` conservative) + `_build_tool_meta(bucket)` helper retorna `{"com.v4company/bucket": ..., "anthropic/alwaysLoad": True (only if always)}` em `list_tools()` (MCP SDK 1.22.0 `Tool._meta` field) + mass-tag 59 tool files (`# bucket: always|defer` line 1 grepability + `bucket="..."` kwarg + description prefix `[CORE]`/`[DEFER]`). **Final bucket state:** 21 always (8 core ≥10 uses + 10 warm 5-9 uses + 3 exceções semânticas: detect_drift recém-shipped + meta_list_my_ad_accounts cache + meta_get_account_overview entry point) + 38 defer = 59 total. Data-driven via audit_log uses_30d query, classification doc `tool-buckets-2026-05-25.md`. **Token rotation procedure documentado** pós-Bearer exposure incident: UI `/sessions/new` (Google OAuth login → flash cookie 60s pra token plaintext → cole `.claude.json` `.headers.Authorization` → restart Claude → revoga antigo `/sessions`). NUNCA inventar tokens (backend rejeita 401, hash em `mcp_sessions.token_hash`). **Smoke real validado bit-a-bit em produção** pós-restart: `list_my_accounts` retornou 25 V4 MCC contas via always-loaded path + system reminder confirmou "38 deferred tools available" (exatamente o esperado D3 fix). 11 commits (Tasks A-F + D3 fix + runbook update + meta_list_my_ad_accounts bucket="always" kwarg fix + test isolation fix). Pre-push 5/5 + 792/792 unit tests + 5/5 test_mcp_server_meta + CI verde. **Decision gate F1 → F2 outcome-based timeout 14d:** Wellington 7d feedback D+7 = 2026-06-01 (5 perguntas estruturadas). Revert path: mass-set `bucket="always"` em 59 tools (1-line PR) + restore `.claude.json.bak` (~5 min). **Lição reinforced 3× consecutiva (D1+D2+D3):** sempre verificar docs oficial cliente ANTES de design refactor cross-layer. Cada research salvou 2-3 dias wasted work. Próximo F2 Sprint 3b.40: Caminho C consolidação `get_performance_breakdown(level, dimension)` substitui 9 reports = -9 tools permanente. Reference: [`phase-3b-39-bootstrap.md`](../_archive/runbooks/phase-3b-39-bootstrap.md), [`tool-buckets-2026-05-25.md`](tool-buckets-2026-05-25.md), spec [`2026-05-25-architecture-refactor-design.md`](../superpowers/specs/2026-05-25-architecture-refactor-design.md). |
| Sessão 2026-05-28/29 — Meta Access Matrix (Modelo B) + segurança + UX gestor + 2 auditorias | ✅ 2026-05-29 | **Não-sprint-numerada; trabalho de acesso/segurança/UX, sem novas MCP tools.** Commits `648a43b`→`82d1060` + docs. **(1) Meta Access Matrix (Modelo B):** system-user token compartilhado (secret `meta-system-user-token`) substitui token pessoal nas tools Meta; `build_meta_api()` (sem manager_id) + `run_meta_graph_get` usa system user. Matriz `manager_meta_account_access` vira gate real. UI admin: abas Google\|Meta em `/admin/accounts`+`/admin/access` (grid + por-gestor + detalhe + inventário + status do token). `copy_access` Meta. `meta_oauth_refresh_accounts` via system user (sem auto-grant). Viabilidade validada empiricamente (ICSER → system user opera conta client-owned no tier atual). Spec/plan: `2026-05-28-meta-access-matrix-{design,}.md`. **(2) Hard-gate Google MCP (Plano A):** `ensure_account_access(conn, manager_id, customer_id, session_id, operation_name, level)` em `src/google_ads/access.py` (raise `AccountAccessDeniedError` + audit `status="denied"`) chamado em `run_report`, `run_mutation`, `run_conversion_upload`, `run_offline_user_data_job`, `create_pending` **e `run_recommendation_action`** (este faltou no plano inicial — pego na 2ª auditoria). Matriz `manager_account_access` agora autoritativa na camada MCP (antes só token MCC, alcançava tudo). Sem bypass por role (admin idem; já tem 25 grants). Spec/plan: `2026-05-28-google-mcp-account-gate-{design,}.md`. **(3) Endurecimento de segurança (Fase 1 auditoria):** `src/web/middleware.py` — `CSRFOriginMiddleware` (Origin/Referer vs Host em métodos unsafe; isenta /oauth+/mcp; ausência permitida — SameSite=Lax é a defesa primária) + `SecurityHeadersMiddleware` (XFO/XCTO/Referrer/HSTS + **CSP enforcing**, allowlist tailwind/unpkg/fonts.bunny). SRI no HTMX. XSS escaping em `_error_page`/`_success_page` (html.escape) + fragmentos de toggle (`_toggle_checkbox_fragment`). Logout via POST. is_active check em `resolve_session_to_context` (sessão de gestor desativado para de funcionar). **(4) Funcional+a11y+DS (auditoria):** UUID inválido→404; paginação preserva filtros; revoke via HX-Redirect+toast; aria-labels matriz, th scope, aria-current nav, drawer ESC; label status Meta via filtro Jinja `meta_status_label`; error.html no DS+h1; dead code removido (`build_meta_api_for_manager`, macros). **(5) UX gestor (Plano B):** card "Contas Meta" no `/accounts` (escopado), aviso da janela de 60s do token, histórico de sessões revogadas (`?include_revoked=1`), access_denied amigável, help sem paths admin. **(6) /help + README reescritos** ao estado atual (Google+Meta). **3 sustos de CI corrigidos forward** (todos no CSV export, expostos pelo 1º teste que iterou de fato): `status` ambíguo em JOIN, cursor sem transação (bug pré-existente — export nunca testado), e o teste apply_change sem mock do gate. Detalhe completo: `session-2026-05-29-handoff.md` + specs/plans `2026-05-28-*`. |
