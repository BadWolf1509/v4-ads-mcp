# PR 6 — Cauda da varredura: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar a cauda da varredura de 8 revisores — o gêmeo Meta do F141, a
família de throttle do Graph que chega ao gestor como falha permanente, a
cláusula que o `bulk_grant` esqueceu, dois índices ausentes, o desempate que
falta em quatro `ORDER BY`, e quatro guards que não conferem o que enunciam.

**Architecture:** Nenhum mecanismo novo. Cada tarefa **estende um primitivo que
já existe** — `src/clock.py::account_today` (da PR 2) ganha um resolvedor Meta
espelhando o Google; `to_friendly_meta_error` ganha os códigos que faltam no
conjunto que ele já classifica; o guard do F141 tem sua isenção `meta_*`
invertida no mesmo commit que torna a isenção desnecessária. O padrão que
governa é **fonte única de verdade**: o fix do F141 tem UM corpo
(`account_today`), e o lado Meta passa a chamá-lo em vez de reimplementar.

**Tech Stack:** Python 3.13 · asyncpg (SQL cru) · `facebook_business` ·
pytest + testcontainers · AST-based guards sobre `tests/unit/_guard_harness.py`

**Spec:** [`docs/superpowers/specs/2026-09-06-correcoes-varredura-design.md`](../specs/2026-09-06-correcoes-varredura-design.md), seção "PR 6 — Cauda"

## Global Constraints

- **Nunca contorne o hook de migration.** Se ele bloquear, PARE e reporte — não
  tente outra ferramenta (`sed`, `python`, shell) para aplicar a mesma edição.
  Rotear por fora de um guard é sempre defeito, mesmo com a conclusão certa.
- **`CLAUDE.md` está em 23.987 bytes contra um teto de 24.000** (guard
  `tests/unit/test_docs_links.py::_ORCAMENTO_CLAUDE_MD`). **Não acrescente linha
  nenhuma a ele nesta PR** — o gate fica vermelho em 13 bytes.
- **Não escreva `uv pip compile` sem `--universal`** em lugar nenhum: doc,
  workflow, comentário ou mensagem de commit (F113).
- **Nenhuma chamada de SDK de ads fora de um closure passado a `run_blocking`**
  em caminho que atende request (F109).
- **`hoje` nunca vem do relógio do servidor** em caminho de conta (F141).
- **Todo guard novo ou apertado precisa da mutação que o derruba, provada**:
  sabote o código de produção, veja o guard vermelho, desfaça, veja verde. E um
  **controle positivo** — sabotagem sozinha não prova guard, um `assert False`
  também ficaria vermelho. Para guard *apertado*, prove também que o **antigo
  passava verde** onde o novo acusa; sem isso não se demonstra aperto, só
  reescrita.
- **Gate antes de todo commit:** `python scripts/check_pre_push.py` — rode mudo
  e leia `$?`. **Nunca** ponha pipe entre o gate e o `&&`: o exit code de um
  pipeline é o do último comando, e isso já deixou passar commit com gate
  vermelho. Nas tarefas que mexem em migration, repository ou query com JOIN, o
  **full sweep é obrigatório**: `python scripts/check_pre_push_full.py` (Docker).
- **Mensagem de commit com crase, `$` ou `!` vai por heredoc `<<'MSG'`**, nunca
  por `-m "..."` — crase dentro de aspas duplas é substituição de comando, e o
  shell executa o que era para ser texto.
- Commits: `fix(scope): …` / `chore(scope): …` / `test(scope): …`. Escopos
  válidos: `web`, `admin`, `auth`, `db`, `mcp`, `meta_ads`, `ci`,
  `design-system`, `security`. Trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

## O que foi deliberadamente deixado de fora

**Migrations `002` e `004` idempotentes** — a spec pede, e este plano **não
inclui**. Elas já rodaram em produção, e o tracker `_migrations` não re-aplica
arquivo editado: mexer nelas não muda nada em produção e muda o que um restore
faz. Isso colide de frente com a convenção append-only do `CLAUDE.md` **e** com
o hook `guard-migrations.ps1`, que bloqueia a edição. Editar migration commitada
é decisão do Wellington, não ruling de executor. **Fica registrado como pendência
e sai desta PR.**

**Reescrita para keyset em `src/web/routes.py`** — os dois `OFFSET` vivem no
arquivo de 1839 linhas que a **frente 5 vai partir em dez módulos**. Fazer o
keyset aqui garante conflito no maior arquivo do repo. Esta PR entrega o
**desempate estável** (que é o lado de *corretude*: sem ele o `OFFSET` pula e
duplica linha quando dois `occurred_at` empatam) e os **índices**; a troca de
`OFFSET` por keyset (que é o lado de *desempenho*) vai para a frente 5, onde o
arquivo está sendo reestruturado de qualquer forma e a mudança de lógica entra
em commit separado do split.

---

## File Structure

| Arquivo | Responsabilidade | Tarefa |
|---|---|---|
| `src/meta_ads/account_clock.py` **(criar)** | Único ponto de I/O do fix Meta: lê `meta_ad_accounts.timezone_name` e delega o cálculo puro a `src.clock.account_today`. Espelha `src/google_ads/account_clock.py`. | 1 |
| `src/mcp/tools/_meta_performance.py` | Núcleo do trio (ad/ad_set/campaign) — 1 sítio de relógio servindo 3 tools. | 2 |
| `src/mcp/tools/meta_get_account_overview.py` | 2 sítios: janela e `build_warnings`. | 2 |
| `src/mcp/tools/meta_get_performance_breakdown.py` | 1 sítio de janela. | 2 |
| `tests/unit/test_no_server_clock_in_google_tools.py` | Inverte a isenção `meta_*`/`_meta_*`. | 2 |
| `src/meta_ads/errors.py` | Classificação de erro do Graph — família de throttle. | 3 |
| `src/db/repositories/manager_account_access.py` | `bulk_grant` — cláusula do `ON CONFLICT`. | 4 |
| `src/db/migrations/010_indices_audit_e_acesso.sql` **(criar)** | Dois índices ausentes. | 5 |
| `src/db/repositories/audit_log.py` · `src/web/routes.py` | Desempate estável em 4 `ORDER BY`. | 6 |
| `tests/unit/test_ci_local_parity.py` · `test_change_freshness.py` · `test_tools_schemas.py` · `.gitignore` | Guards que não conferem o que enunciam. | 7 |

---

## Task 1: Resolvedor de fuso do lado Meta

**Files:**
- Create: `src/meta_ads/account_clock.py`
- Test: `tests/unit/test_meta_account_clock.py`

**Interfaces:**
- Consumes: `src.clock.account_today(time_zone: str | None, *, now: datetime) -> date`;
  `src.db.repositories.meta_ad_accounts.get_by_id(conn, ad_account_id) -> MetaAdAccount | None`
  (o campo do fuso chama-se `timezone_name`, **não** `time_zone` — a coluna Meta
  tem outro nome que a Google, e a dataclass espelha a tabela);
  `src.db.connection.run_with_reconnect(op)`
- Produces: `resolve_meta_account_today(ad_account_id: str, *, now: datetime | None = None) -> date`

- [ ] **Step 1: Escreva o teste que falha**

Crie `tests/unit/test_meta_account_clock.py`:

```python
"""F141, gêmeo Meta: `hoje` é da CONTA, não do servidor.

O instante escolhido é o que separa os dois: 2026-03-10T02:30Z é dia 10 em UTC
e ainda dia 9 em `America/Sao_Paulo` (UTC-3). Um teste que use meio-dia UTC
passa com a implementação errada — é a armadilha do "teste que não distingue
código bom de quebrado".
"""

from __future__ import annotations

from datetime import UTC, date, datetime
from types import SimpleNamespace
from unittest.mock import AsyncMock, patch

import pytest

from src.meta_ads.account_clock import resolve_meta_account_today

# 02:30Z de 10/03 = 23:30 de 09/03 em Sao_Paulo. UTC e conta discordam.
INSTANTE = datetime(2026, 3, 10, 2, 30, tzinfo=UTC)


@pytest.mark.asyncio
async def test_usa_o_fuso_da_conta_e_nao_o_do_servidor() -> None:
    conta = SimpleNamespace(timezone_name="America/Sao_Paulo")
    with patch(
        "src.meta_ads.account_clock.connection.run_with_reconnect",
        AsyncMock(return_value=conta),
    ):
        assert await resolve_meta_account_today("act_1", now=INSTANTE) == date(2026, 3, 9)


@pytest.mark.asyncio
async def test_conta_ausente_cai_em_utc_sem_estourar() -> None:
    with patch(
        "src.meta_ads.account_clock.connection.run_with_reconnect",
        AsyncMock(return_value=None),
    ):
        assert await resolve_meta_account_today("act_x", now=INSTANTE) == date(2026, 3, 10)


@pytest.mark.asyncio
async def test_fuso_invalido_cai_em_utc_sem_estourar() -> None:
    conta = SimpleNamespace(timezone_name="Nao/Existe")
    with patch(
        "src.meta_ads.account_clock.connection.run_with_reconnect",
        AsyncMock(return_value=conta),
    ):
        assert await resolve_meta_account_today("act_2", now=INSTANTE) == date(2026, 3, 10)


@pytest.mark.asyncio
async def test_le_o_campo_timezone_name_e_nao_time_zone() -> None:
    """A coluna Meta chama-se `timezone_name`; a Google, `time_zone`.

    Sem esta asserção, um resolvedor copiado do gêmeo Google leria `time_zone`,
    receberia `None` em toda conta, e cairia em UTC SEMPRE — verde nos outros
    testes que usam `SimpleNamespace`, porque um objeto sem o atributo levanta
    `AttributeError`, mas um com AMBOS não distingue. Aqui só existe o certo.
    """
    conta = SimpleNamespace(timezone_name="America/Manaus")  # UTC-4
    with patch(
        "src.meta_ads.account_clock.connection.run_with_reconnect",
        AsyncMock(return_value=conta),
    ):
        assert await resolve_meta_account_today("act_3", now=INSTANTE) == date(2026, 3, 9)
```

- [ ] **Step 2: Rode e confirme que falha**

Run: `python -m pytest tests/unit/test_meta_account_clock.py -q`
Expected: FAIL com `ModuleNotFoundError: No module named 'src.meta_ads.account_clock'`

- [ ] **Step 3: Implemente**

Crie `src/meta_ads/account_clock.py`:

```python
"""F141, gêmeo Meta: `hoje` e uma propriedade da CONTA, nao do servidor.

O lado Google fechou isto em 2026-09-06 e o guard
(`tests/unit/test_no_server_clock_in_google_tools.py`) isentou `meta_*` de
proposito, com a isencao escrita como divida: "estes SAO a mesma classe". Este
modulo paga a divida.

Espelha `src/google_ads/account_clock.py` deliberadamente — mesmo formato, mesmo
fallback, mesma delegacao ao corpo unico em `src.clock.account_today`. O que
muda e SO a fonte do fuso: `meta_ad_accounts.timezone_name` (nome IANA) em vez
de `google_ads_accounts.time_zone`. Nomes de coluna diferentes porque as duas
tabelas os batizaram diferente; a dataclass de cada lado espelha a sua tabela, e
renomear aqui esconderia de onde o dado vem.

Sonda empirica ja feita (2026-09-06, registrada em `src/clock.py`): as 25 contas
vivas do cache de producao trazem `America/Sao_Paulo` (22), `America/Noronha`
(2) e `America/Manaus` (1), todas resolvidas por `ZoneInfo` com o `tzdata`
pinado no lockfile. A doc do Graph so promete "string" — por isso o fallback
cobre valor fora do formato com warning + UTC, nunca excecao.

Leitura idempotente em hot path -> `run_with_reconnect` (F76/F77).
"""

from __future__ import annotations

from datetime import UTC, date, datetime

from src.clock import account_today
from src.db import connection
from src.db.repositories import meta_ad_accounts


async def resolve_meta_account_today(
    ad_account_id: str, *, now: datetime | None = None
) -> date:
    """Dia corrente no fuso da conta `ad_account_id`. `now` injetavel para teste."""
    account = await connection.run_with_reconnect(
        lambda conn: meta_ad_accounts.get_by_id(conn, ad_account_id)
    )
    time_zone = account.timezone_name if account is not None else None
    return account_today(time_zone, now=now if now is not None else datetime.now(UTC))
```

- [ ] **Step 4: Rode e confirme que passa**

Run: `python -m pytest tests/unit/test_meta_account_clock.py -q`
Expected: 4 passed

- [ ] **Step 5: Prove que o teste distingue**

Troque `account.timezone_name` por `account.time_zone` no módulo (a cópia
ingênua do gêmeo Google) e rode de novo. Esperado: `AttributeError`, não um
verde. Depois troque por `getattr(account, "time_zone", None)` — a forma que
**passaria em silêncio** se o teste fosse fraco. Esperado: os dois testes de
fuso ficam **vermelhos** (devolvem 10/03 em vez de 09/03). Desfaça as duas
sabotagens e confirme verde.

Registre no relatório qual das duas sabotagens cada teste pegou.

- [ ] **Step 6: Gate e commit**

```bash
python scripts/check_pre_push.py > /dev/null 2>&1 && git add src/meta_ads/account_clock.py tests/unit/test_meta_account_clock.py && git commit -F - <<'MSG'
feat(meta_ads): resolvedor de `hoje` no fuso da conta Meta

Gemeo do `src/google_ads/account_clock.py`. Delega ao corpo unico
`src.clock.account_today` — o fix do F141 continua com UMA fonte de verdade,
que e a razao de `clock.py` morar na raiz de `src/`.

Le `meta_ad_accounts.timezone_name`, nao `time_zone`: as duas tabelas batizaram
a coluna diferente, e a dataclass de cada lado espelha a sua. O teste
`test_le_o_campo_timezone_name_e_nao_time_zone` existe porque a copia ingenua
do gemeo Google leria o campo errado, receberia None em toda conta e cairia em
UTC sempre — verde em qualquer teste que nao force a distincao.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 2: Ligar os 3 sítios e inverter a isenção do guard

**Files:**
- Modify: `src/mcp/tools/_meta_performance.py:81`
- Modify: `src/mcp/tools/meta_get_account_overview.py:102` e `:180`
- Modify: `src/mcp/tools/meta_get_performance_breakdown.py:108`
- Modify: `tests/unit/test_no_server_clock_in_google_tools.py`
- Test: `tests/unit/test_meta_tools_usam_fuso_da_conta.py`

**Interfaces:**
- Consumes: `resolve_meta_account_today(ad_account_id: str, *, now: datetime | None = None) -> date` (Task 1)
- Produces: nada — é a última camada.

**Contexto que o brief não pode saber:** `_meta_performance.py` é o núcleo
compartilhado de **três** tools (`meta_get_ad_performance`,
`meta_get_ad_set_performance`, `meta_get_campaign_performance`) — um sítio, três
tools. Os outros dois arquivos são tools próprias. Total: **3 sítios, 5 tools**.

- [ ] **Step 1: Escreva o teste que falha**

Crie `tests/unit/test_meta_tools_usam_fuso_da_conta.py`:

```python
"""As 5 tools Meta resolvem `hoje` pela conta, nao pelo relogio do servidor.

Guard de comportamento, complementando o guard estrutural (AST) do F141. O
estrutural diz "nao chame `datetime.now`"; este diz "o dia que sai da janela e
o da conta". Os dois juntos e que fecham: sem o AST, alguem reintroduz a
chamada num arquivo novo; sem este, alguem chama o resolvedor e joga fora o
resultado (a familia F112).
"""

from __future__ import annotations

import ast
from pathlib import Path

import tests.unit._guard_harness as h

SITIOS = [
    h.SRC / "mcp" / "tools" / "_meta_performance.py",
    h.SRC / "mcp" / "tools" / "meta_get_account_overview.py",
    h.SRC / "mcp" / "tools" / "meta_get_performance_breakdown.py",
]


def _chama_o_resolvedor(arv: ast.Module) -> bool:
    return any(
        isinstance(n, ast.Call)
        and (
            (isinstance(n.func, ast.Name) and n.func.id == "resolve_meta_account_today")
            or (isinstance(n.func, ast.Attribute) and n.func.attr == "resolve_meta_account_today")
        )
        for n in ast.walk(arv)
    )


def _usa_o_retorno(arv: ast.Module) -> bool:
    """O resultado tem que ser AMARRADO a um nome — chamar e descartar e F112."""
    for n in ast.walk(arv):
        if not isinstance(n, ast.Assign):
            continue
        v = n.value
        if isinstance(v, ast.Await):
            v = v.value
        if isinstance(v, ast.Call) and (
            (isinstance(v.func, ast.Name) and v.func.id == "resolve_meta_account_today")
            or (isinstance(v.func, ast.Attribute) and v.func.attr == "resolve_meta_account_today")
        ):
            return True
    return False


def test_os_tres_sitios_resolvem_o_dia_pela_conta() -> None:
    faltando: list[str] = []
    for p in SITIOS:
        assert p.exists(), f"sitio sumiu do repo: {h.rel(p)} — atualize SITIOS"
        arv = h.arvore(p)
        if not _chama_o_resolvedor(arv):
            faltando.append(f"{h.rel(p)}: nao chama resolve_meta_account_today")
        elif not _usa_o_retorno(arv):
            faltando.append(f"{h.rel(p)}: chama e descarta o retorno (familia F112)")
    assert not faltando, (
        f"tools Meta ainda resolvem `hoje` sem a conta: {faltando}. "
        "Use `await resolve_meta_account_today(ad_account_id)` UMA vez por request "
        "e passe o mesmo `today` a tudo que precisa dele."
    )
```

- [ ] **Step 2: Rode e confirme que falha**

Run: `python -m pytest tests/unit/test_meta_tools_usam_fuso_da_conta.py -q`
Expected: FAIL, listando os 3 sítios com "nao chama resolve_meta_account_today"

- [ ] **Step 3: Ligue os 3 sítios**

Em `src/mcp/tools/_meta_performance.py`, troque a linha 81:

```python
    today = datetime.now(UTC).date()
```

por:

```python
    today = await resolve_meta_account_today(ad_account_id)
```

e acrescente o import no topo:

```python
from src.meta_ads.account_clock import resolve_meta_account_today
```

Faça o mesmo em `src/mcp/tools/meta_get_performance_breakdown.py:108` e em
`src/mcp/tools/meta_get_account_overview.py:102`.

**`meta_get_account_overview.py:180` é caso diferente e NÃO muda.** Ali o
`datetime.now(UTC)` alimenta `build_warnings`, que carimba o **instante em que o
aviso foi gerado** — timestamp de registro, não data de conta. Deixe como está e
acrescente o comentário que impede a próxima pessoa de "consertar":

```python
    # NAO e data de conta: e o instante em que o aviso foi gerado (timestamp de
    # registro). O fuso da conta nao se aplica — trocar por
    # `resolve_meta_account_today` aqui seria carimbar um evento do servidor com
    # o calendario do anunciante. Contraste com a linha 102, que resolve a
    # JANELA e por isso e da conta (F141).
    warnings = build_warnings(account_status_label, None, datetime.now(UTC))
```

Se depois disso o `datetime` ou o `UTC` ficarem sem uso em algum dos arquivos, o
`ruff` acusa — remova o import morto.

- [ ] **Step 4: Rode e confirme que passa**

Run: `python -m pytest tests/unit/test_meta_tools_usam_fuso_da_conta.py -q`
Expected: 1 passed

- [ ] **Step 5: Inverta a isenção do guard estrutural**

Em `tests/unit/test_no_server_clock_in_google_tools.py`, a função
`_arquivos_sem_relogio()` filtra assim:

```python
        if not p.name.startswith(("meta_", "_meta_")) and p.name not in FORA_COM_MOTIVO
```

Remova o filtro por nome, deixando só o `FORA_COM_MOTIVO`:

```python
        if p.name not in FORA_COM_MOTIVO
```

E acrescente `meta_get_account_overview.py` a `FORA_COM_MOTIVO`, com o motivo
escrito (é o sítio da linha 180, que é legítimo):

```python
    "meta_get_account_overview.py": (
        "le o relogio UMA vez, na linha do `build_warnings`, para carimbar o "
        "INSTANTE em que o aviso foi gerado — timestamp de registro, nao data "
        "de conta. A janela do mesmo arquivo passou a usar "
        "`resolve_meta_account_today` (PR 6). Contraste com o F146: em MUTATE "
        "que grava timestamp no provedor, fuso chutado e corrupcao; aqui o "
        "carimbo e do servidor por definicao."
    ),
```

Atualize também a prosa do topo do arquivo: o bloco que hoje diz que
`_meta_performance.py`, `meta_get_account_overview.py` e
`meta_get_performance_breakdown.py` "SAO a mesma classe" e ficam de fora deve
passar a dizer que a dívida foi paga na PR 6 e que a isenção por nome **caiu**.
Uma enumeração que não acompanha o código é a próxima a mentir.

- [ ] **Step 6: Prove que o guard estrutural aperta de verdade**

Esta é a prova de **aperto**, e ela tem duas metades. Rode e registre as duas:

1. **A mutação de produção:** reponha `today = datetime.now(UTC).date()` em
   `src/mcp/tools/_meta_performance.py`. Rode
   `python -m pytest tests/unit/test_no_server_clock_in_google_tools.py -q`.
   Esperado: **vermelho**, nomeando o arquivo.
2. **O antigo passava:** com a mutação ainda no lugar, restaure temporariamente
   o filtro `not p.name.startswith(("meta_", "_meta_"))` e rode de novo.
   Esperado: **verde** — é isto que demonstra que o guard era cego aqui, e que a
   mudança é aperto e não reescrita.

Desfaça as duas e confirme verde. **Registre os dois resultados no relatório**;
sem a segunda metade não há prova de aperto.

- [ ] **Step 7: Full sweep, gate e commit**

```bash
python scripts/check_pre_push.py > /dev/null 2>&1 && git add -A && git commit -F - <<'MSG'
fix(meta_ads): as 5 tools Meta resolvem `hoje` pela conta (gemeo do F141)

Tres sitios liam `datetime.now(UTC).date()` e serviam CINCO tools —
`_meta_performance.py` sozinho e o nucleo de ad/ad_set/campaign. Em UTC, todo
preset deslizava um dia das 21h a meia-noite locais, todo dia, em silencio: a
mesma classe que o F141 fechou do lado Google em 06/09.

O dado ja estava no banco. `meta_ad_accounts.timezone_name` e populado pelo
resync desde sempre e nenhuma tool o lia.

A linha 180 do `meta_get_account_overview` NAO muda: ali o relogio carimba o
instante em que o aviso foi gerado, que e evento do servidor por definicao. Ela
entra em FORA_COM_MOTIVO com o motivo escrito, e o comentario no codigo diz por
que trocar seria errado — a proxima pessoa a "consertar" isso e o risco real.

O guard do F141 isentava `meta_*` de proposito, com a isencao escrita como
divida. A isencao por NOME cai neste commit: o que sobra e isencao por ARQUIVO,
com motivo. Provado nas duas direcoes — a mutacao derruba o guard novo, e o
guard antigo ficava verde com a mesma mutacao.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 3: A família de throttle do Graph que faltava

**Files:**
- Modify: `src/meta_ads/errors.py`
- Test: `tests/unit/test_meta_errors.py`

**Interfaces:**
- Consumes: `MetaAdsFriendlyError(message: str, retryable: bool)` — já existe no módulo.
- Produces: nada de novo; muda a classificação de 3 códigos.

**Contexto:** hoje o módulo trata `code == 4` e `subcode == 2635` como throttle
retryable. Os códigos **17** (limite por usuário), **613** (limite de chamadas da
API) e **80004** (limite de ads management) caem no ramo genérico com
`retryable=False`. Efeito: throttle transitório chega ao gestor como falha
permanente, e nada tenta de novo.

- [ ] **Step 1: Escreva o teste que falha**

Crie ou estenda `tests/unit/test_meta_errors.py`:

```python
"""A familia de throttle do Graph e maior que o code 4.

Antes: 4 e subcode 2635 eram retryable; 17, 613 e 80004 caiam no ramo generico
com retryable=False. Throttle transitorio virava falha permanente na cara do
gestor, e nenhum caminho de retry disparava.
"""

from __future__ import annotations

import pytest

from src.meta_ads.errors import to_friendly_meta_error

facebook_business = pytest.importorskip("facebook_business")
from facebook_business.exceptions import FacebookRequestError  # noqa: E402


def _erro(code: int, subcode: int | None = None, msg: str = "limite") -> FacebookRequestError:
    corpo = {"error": {"code": code, "message": msg}}
    if subcode is not None:
        corpo["error"]["error_subcode"] = subcode
    return FacebookRequestError(
        message=msg,
        request_context={},
        http_status=400,
        http_headers={},
        body=corpo,
    )


@pytest.mark.parametrize("code", [4, 17, 613, 80004])
def test_toda_a_familia_de_throttle_e_retryable(code: int) -> None:
    r = to_friendly_meta_error(_erro(code))
    assert r.retryable is True, f"code {code} deveria ser retryable"
    assert "limite" in r.message.lower(), f"code {code}: mensagem nao fala de limite"


def test_subcode_2635_continua_retryable() -> None:
    assert to_friendly_meta_error(_erro(1, subcode=2635)).retryable is True


@pytest.mark.parametrize("code", [190, 100, 200, 3018])
def test_o_que_nao_e_throttle_continua_nao_retryable(code: int) -> None:
    """Contraprova: sem ela, `retryable=True` incondicional passaria verde.

    Um guard que so afirma o lado positivo nao distingue "classifica throttle"
    de "diz sim para tudo".
    """
    assert to_friendly_meta_error(_erro(code)).retryable is False
```

- [ ] **Step 2: Rode e confirme que falha**

Run: `python -m pytest tests/unit/test_meta_errors.py -q`
Expected: FAIL nos códigos 17, 613 e 80004 (`assert False is True`)

- [ ] **Step 3: Implemente**

Em `src/meta_ads/errors.py`, acrescente a constante logo abaixo dos imports:

```python
# Familia de throttle do Graph. Cada um e um limite DIFERENTE, e todos passam:
#   4     — limite da aplicacao (app-level rate limit)
#   17    — limite do usuario (user-level rate limit)
#   613   — limite de chamadas do endpoint ("calls to this api have exceeded")
#   80004 — limite especifico de ads management
# Ate a PR 6 so o 4 (e o subcode 2635) eram reconhecidos; os outros tres caiam
# no ramo generico com retryable=False, e um throttle de minutos chegava ao
# gestor como falha permanente.
CODIGOS_DE_THROTTLE = frozenset({4, 17, 613, 80004})
```

e troque a condição:

```python
        if subcode == 2635 or code == 4:
```

por:

```python
        if subcode == 2635 or code in CODIGOS_DE_THROTTLE:
```

- [ ] **Step 4: Rode e confirme que passa**

Run: `python -m pytest tests/unit/test_meta_errors.py -q`
Expected: 9 passed

- [ ] **Step 5: Prove que o teste distingue**

Troque `retryable=True` por `retryable=False` no ramo de throttle. Esperado: os
4 casos de throttle ficam vermelhos. Depois troque a condição por `if True:` —
esperado: os 4 de contraprova (190/100/200/3018) ficam vermelhos. Desfaça as
duas e confirme verde. As duas metades juntas é que provam que o teste
classifica, em vez de só afirmar.

- [ ] **Step 6: Gate e commit**

```bash
python scripts/check_pre_push.py > /dev/null 2>&1 && git add src/meta_ads/errors.py tests/unit/test_meta_errors.py && git commit -F - <<'MSG'
fix(meta_ads): 17, 613 e 80004 sao throttle, nao falha permanente

`to_friendly_meta_error` reconhecia como limite so o code 4 e o subcode 2635.
Os codes 17 (limite por usuario), 613 (limite de chamadas do endpoint) e 80004
(limite de ads management) caiam no ramo generico com retryable=False — um
throttle de minutos chegava ao gestor como erro definitivo, e nenhum caminho de
retry disparava.

Os quatro viram uma constante nomeada com o que CADA um significa, em vez de
uma condicao com numeros soltos. A contraprova (190/100/200/3018 seguem
nao-retryable) existe porque um guard que so afirma o lado positivo nao
distingue "classifica throttle" de "diz sim para tudo".

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 4: A cláusula que o `bulk_grant` esqueceu (F128)

**Files:**
- Modify: `src/db/repositories/manager_account_access.py:81-84`
- Test: `tests/integration/test_manager_account_access.py`

**Interfaces:**
- Consumes: nada de tarefas anteriores.
- Produces: nada — muda uma cláusula SQL.

**Contexto:** `grant` (linha ~46) faz `access_level = EXCLUDED.access_level` no
`ON CONFLICT`. `bulk_grant` (linha ~81) insere `'write'` mas o `DO UPDATE SET` só
limpa `revoked_at`/`revoked_reason` — **não seta `access_level`**. Quem já tinha
`read` continua com `read` depois de um "conceder tudo", sem erro nenhum. É o
F128 exato: cláusula presente num gêmeo, ausente no outro.

- [ ] **Step 1: Escreva o teste que falha**

Acrescente a `tests/integration/test_manager_account_access.py` (é teste de DB —
usa testcontainers, marcado `@pytest.mark.integration`):

```python
@pytest.mark.integration
@pytest.mark.asyncio
async def test_bulk_grant_promove_read_para_write(pool) -> None:
    """F128: o `bulk_grant` concede 'write' — inclusive a quem ja tinha 'read'.

    Sem `access_level = EXCLUDED.access_level` no ON CONFLICT, a linha existente
    e preservada com o nivel antigo. O gestor pede "conceder tudo", a tool
    responde sucesso, e o acesso continua somente-leitura — em silencio, que e
    a parte cara.
    """
    async with pool.acquire() as conn:
        manager_id = await _cria_manager(conn, "bulk@v4company.com")
        await _cria_conta(conn, "1111111111")
        await manager_account_access.grant(
            conn, manager_id=manager_id, customer_id="1111111111",
            access_level="read", granted_by=manager_id,
        )

        await manager_account_access.bulk_grant(
            conn, manager_id=manager_id, granted_by=manager_id
        )

        nivel = await conn.fetchval(
            "SELECT access_level FROM manager_account_access "
            " WHERE manager_id = $1 AND customer_id = $2",
            manager_id, "1111111111",
        )
        assert nivel == "write", (
            "bulk_grant concede 'write', mas a linha que ja existia ficou em "
            f"{nivel!r} — o ON CONFLICT nao seta access_level (F128)"
        )


@pytest.mark.integration
@pytest.mark.asyncio
async def test_bulk_grant_continua_limpando_a_revogacao(pool) -> None:
    """Contraprova: o comportamento que o ON CONFLICT ja tinha nao regride."""
    async with pool.acquire() as conn:
        manager_id = await _cria_manager(conn, "bulk2@v4company.com")
        await _cria_conta(conn, "2222222222")
        await manager_account_access.grant(
            conn, manager_id=manager_id, customer_id="2222222222",
            access_level="write", granted_by=manager_id,
        )
        await manager_account_access.revoke(
            conn, manager_id=manager_id, customer_id="2222222222", reason="teste"
        )

        await manager_account_access.bulk_grant(
            conn, manager_id=manager_id, granted_by=manager_id
        )

        linha = await conn.fetchrow(
            "SELECT access_level, revoked_at FROM manager_account_access "
            " WHERE manager_id = $1 AND customer_id = $2",
            manager_id, "2222222222",
        )
        assert linha["revoked_at"] is None, "a revogacao deveria ter sido limpa"
        assert linha["access_level"] == "write"
```

Se os helpers `_cria_manager` / `_cria_conta` não existirem no arquivo, use os
que já estiverem lá para o mesmo fim — **leia o arquivo antes de escrever** e
siga o padrão de fixture existente. Se a assinatura de `grant`/`revoke` divergir
do que está acima, a do código vence: ajuste a chamada, não a invariante.

- [ ] **Step 2: Rode e confirme que falha**

Run: `python -m pytest tests/integration/test_manager_account_access.py -q -m integration`
Expected: `test_bulk_grant_promove_read_para_write` FAIL com `nivel == 'read'`

Se o Docker não estiver de pé, **pare e reporte** — este teste não tem valor sem
o Postgres real, e um `skip` silencioso é o modo de falha que o repo já
catalogou.

- [ ] **Step 3: Implemente**

Em `src/db/repositories/manager_account_access.py`, no `ON CONFLICT` do
`bulk_grant`:

```sql
        ON CONFLICT (manager_id, customer_id) DO UPDATE SET
            access_level = EXCLUDED.access_level,
            revoked_at = NULL,
            revoked_reason = NULL
```

E atualize a docstring da função: ela hoje explica por que o conflito limpa a
revogação, e passa a explicar também por que seta o nível — a docstring que
enumera metade das cláusulas é a que envelheceu.

- [ ] **Step 4: Rode e confirme que passa**

Run: `python -m pytest tests/integration/test_manager_account_access.py -q -m integration`
Expected: ambos passam

- [ ] **Step 5: Full sweep e commit**

```bash
python scripts/check_pre_push_full.py > /dev/null 2>&1 && git add -A && git commit -F - <<'MSG'
fix(db): bulk_grant seta access_level no ON CONFLICT (F128)

O `grant` de linha unica ja fazia `access_level = EXCLUDED.access_level`. O
`bulk_grant`, que insere 'write', so limpava revoked_at/revoked_reason — entao
quem ja tinha 'read' continuava com 'read' depois de um "conceder tudo". Sem
erro, sem aviso: a tool responde sucesso e o acesso segue somente-leitura.

E o F128 na forma canonica: clausula presente num gemeo e ausente no outro. A
docstring explicava por que o conflito limpa a revogacao e nao mencionava o
nivel — docstring que enumera metade das clausulas e a que envelhece primeiro.

Verificado com Postgres real (testcontainers), nao com mock: o comportamento em
disputa e do ON CONFLICT, que mock nenhum expressa.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 5: Os dois índices ausentes

**Files:**
- Create: `src/db/migrations/010_indices_audit_e_acesso.sql`
- Test: `tests/integration/test_migrations.py`

**Interfaces:**
- Consumes: nada.
- Produces: os índices `idx_audit_occurred_at` e `idx_mac_customer`.

**Contexto medido:**
- `audit_log` tem `idx_audit_manager_time (manager_id, occurred_at DESC)`,
  `idx_audit_account_time (customer_id, occurred_at DESC)` e
  `idx_audit_platform_time`. **Não tem índice só por `occurred_at`** — e a
  listagem do painel (`routes.py:678` e `:1761`) ordena por `occurred_at DESC`
  **sem filtrar por gestor nem por conta**, então nenhum dos três serve: é scan
  + sort da tabela inteira, que só cresce.
- `manager_account_access` tem `PRIMARY KEY (manager_id, customer_id)`. Busca
  por gestor usa a coluna líder; **busca por `customer_id` sozinho varre tudo** —
  e é exatamente a pergunta "quem tem acesso a esta conta?", que o painel de
  admin faz.

- [ ] **Step 1: Escreva a migration**

Crie `src/db/migrations/010_indices_audit_e_acesso.sql`:

```sql
-- 010: dois indices que faltavam, ambos para consultas que o painel ja faz.
--
-- 1. audit_log(occurred_at DESC)
--    Existem tres indices compostos com occurred_at na SEGUNDA posicao
--    (manager_id, customer_id, platform). Nenhum serve a listagem geral do
--    audit, que ordena por occurred_at DESC sem filtrar por nenhuma das tres —
--    o planner cai em scan + sort da tabela inteira. audit_log so cresce.
--
-- 2. manager_account_access(customer_id)
--    A PK e (manager_id, customer_id): busca por gestor usa a coluna lider,
--    busca por conta varre. "Quem tem acesso a esta conta?" e pergunta do
--    painel de admin e da revogacao — as duas varrem hoje.
--
-- IF NOT EXISTS nos dois: a convencao do repo e append-only, e migration que
-- falha por objeto ja existente trava o Job de migration inteiro no deploy.

CREATE INDEX IF NOT EXISTS idx_audit_occurred_at
    ON audit_log (occurred_at DESC);

CREATE INDEX IF NOT EXISTS idx_mac_customer
    ON manager_account_access (customer_id);
```

- [ ] **Step 2: Escreva o teste que falha**

Acrescente a `tests/integration/test_migrations.py`:

```python
@pytest.mark.integration
@pytest.mark.asyncio
async def test_indices_da_010_existem_apos_migrar(pool) -> None:
    """Os dois indices da 010 estao no schema depois de aplicar as migrations."""
    async with pool.acquire() as conn:
        nomes = {
            r["indexname"]
            for r in await conn.fetch(
                "SELECT indexname FROM pg_indexes "
                " WHERE tablename IN ('audit_log', 'manager_account_access')"
            )
        }
    assert "idx_audit_occurred_at" in nomes, (
        f"idx_audit_occurred_at ausente. Indices vistos: {sorted(nomes)}"
    )
    assert "idx_mac_customer" in nomes, (
        f"idx_mac_customer ausente. Indices vistos: {sorted(nomes)}"
    )
```

Se o arquivo não existir, crie-o seguindo o padrão de fixture das outras suítes
de integração do repo — **leia uma delas antes**.

- [ ] **Step 3: Rode e confirme que falha**

Run: `python -m pytest tests/integration/test_migrations.py -q -m integration`
Expected: FAIL antes de a 010 existir; PASS depois.

Como a migration e o teste entram no mesmo commit, rode o teste **com a
migration removida temporariamente** para ver o vermelho, e reponha. Registre
os dois resultados.

- [ ] **Step 4: Full sweep e commit**

```bash
python scripts/check_pre_push_full.py > /dev/null 2>&1 && git add src/db/migrations/010_indices_audit_e_acesso.sql tests/integration/test_migrations.py && git commit -F - <<'MSG'
feat(db): migration 010 — indices de audit_log(occurred_at) e mac(customer_id)

Dois indices que faltavam para consultas que o painel JA faz.

audit_log tem tres indices compostos com occurred_at na segunda posicao
(manager_id, customer_id, platform) e nenhum com occurred_at sozinho. A
listagem geral do audit ordena por occurred_at DESC sem filtrar por nenhuma das
tres colunas lideres, entao nenhum dos tres e utilizavel: scan + sort da tabela
inteira, que so cresce.

manager_account_access tem PK (manager_id, customer_id). Busca por gestor usa a
coluna lider; "quem tem acesso a esta conta?" varre. E pergunta do painel de
admin e do caminho de revogacao.

Aditiva e idempotente (IF NOT EXISTS nos dois): migration que falha por objeto
existente trava o Job de migration inteiro no deploy.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 6: O desempate que falta em quatro `ORDER BY`

**Files:**
- Modify: `src/db/repositories/audit_log.py:157`
- Modify: `src/web/routes.py:342`, `:678`, `:1761`
- Test: `tests/unit/test_order_by_tem_desempate.py`

**Interfaces:**
- Consumes: nada.
- Produces: nada — muda cláusulas SQL.

**Contexto:** `audit_log.py:242` **já** tem `ORDER BY occurred_at DESC, id DESC`
e a docstring explica por quê. Os outros quatro sítios ordenam por
`occurred_at DESC` sozinho. Com `LIMIT`/`OFFSET` e dois `occurred_at` iguais, a
ordem entre eles é indefinida entre uma consulta e a seguinte: a paginação
**pula e duplica linha**. É o F98/F88 na forma de empate, e `occurred_at` empata
de verdade — auditoria de lote grava várias linhas no mesmo instante.

**Fora de escopo aqui:** trocar `OFFSET` por keyset. Vai para a frente 5, onde
`routes.py` está sendo partido em dez módulos. Esta tarefa entrega **só** o
desempate, que é o lado de corretude e cabe em quatro linhas.

- [ ] **Step 1: Escreva o guard que falha**

Crie `tests/unit/test_order_by_tem_desempate.py`:

```python
"""`ORDER BY occurred_at DESC` sem desempate + LIMIT/OFFSET pula e duplica linha.

`occurred_at` empata de verdade: auditoria de lote grava varias linhas no mesmo
instante. Quando duas linhas empatam, o Postgres nao promete ordem estavel entre
duas execucoes — a pagina 2 pode repetir uma linha da pagina 1 e omitir outra.

Por que regex e nao AST: sao literais SQL dentro de strings Python, invisiveis
para o AST. O casador exige que TODA ocorrencia de `ORDER BY ... occurred_at
DESC` seja seguida de uma virgula e outra coluna, em vez de enumerar os sitios
conhecidos — enumerar e o modo de falha 1 do catalogo (o que ficou fora da
lista passa).
"""

from __future__ import annotations

import re

import tests.unit._guard_harness as h

# `occurred_at DESC` que NAO e seguido de `, <coluna>` antes do fim da clausula.
_SEM_DESEMPATE = re.compile(
    r"ORDER\s+BY\s+(?:\w+\.)?occurred_at\s+DESC(?!\s*,\s*(?:\w+\.)?\w+)",
    re.IGNORECASE,
)


def test_todo_order_by_por_occurred_at_tem_desempate() -> None:
    ofensores: list[str] = []
    arquivos = 0
    for p in h.fontes_py(h.SRC):
        texto = p.read_text(encoding="utf-8")
        if "occurred_at" not in texto:
            continue
        arquivos += 1
        for m in _SEM_DESEMPATE.finditer(texto):
            linha = texto[: m.start()].count("\n") + 1
            ofensores.append(f"{h.rel(p)}:{linha}")

    assert arquivos, (
        "o scanner nao viu nenhum arquivo com `occurred_at` — guard que varre "
        "zero arquivos passa por vacuidade."
    )
    assert not ofensores, (
        f"ORDER BY por occurred_at sem desempate: {ofensores}. Acrescente uma "
        "coluna unica e estavel (`, id DESC`): sem ela, LIMIT/OFFSET pula e "
        "duplica linha quando dois occurred_at empatam."
    )
```

- [ ] **Step 2: Rode e confirme que falha**

Run: `python -m pytest tests/unit/test_order_by_tem_desempate.py -q`
Expected: FAIL listando os 4 sítios (`audit_log.py:157`, `routes.py:342`,
`routes.py:678`, `routes.py:1761`)

Se o guard listar **mais** de 4, ótimo — significa que a varredura achou o que a
enumeração da spec não tinha. Corrija todos e diga quantos no relatório.

- [ ] **Step 3: Implemente**

Em cada sítio, acrescente o desempate. Use o **mesmo alias** que a query já usa:

- `src/db/repositories/audit_log.py:157` → `ORDER BY al.occurred_at DESC, al.id DESC`
- `src/web/routes.py:342` → `ORDER BY occurred_at DESC, id DESC`
- `src/web/routes.py:678` → `ORDER BY al.occurred_at DESC, al.id DESC`
- `src/web/routes.py:1761` → `ORDER BY al.occurred_at DESC, al.id DESC`

**Confirme que a coluna `id` existe em `audit_log`** antes de escrever — leia
`src/db/migrations/001_initial_schema.sql`. Se o nome for outro, use o real; a
invariante é "uma coluna única e estável", não a letra `id`.

- [ ] **Step 4: Rode e confirme que passa**

Run: `python -m pytest tests/unit/test_order_by_tem_desempate.py -q`
Expected: 1 passed

- [ ] **Step 5: Prove que o guard distingue**

Remova o `, al.id DESC` de **um** dos quatro sítios. Esperado: vermelho,
nomeando exatamente aquele arquivo e linha. Reponha e confirme verde. Depois,
prove que o casador não é vácuo: troque `occurred_at` por `xxxx` no regex e rode
— esperado **verde**, o que mostra que o vermelho anterior veio do padrão certo
e não de qualquer coisa. Desfaça.

- [ ] **Step 6: Full sweep e commit**

```bash
python scripts/check_pre_push_full.py > /dev/null 2>&1 && git add -A && git commit -F - <<'MSG'
fix(db): desempate estavel nos quatro ORDER BY por occurred_at

`audit_log.py:242` ja tinha `ORDER BY occurred_at DESC, id DESC` com a
docstring explicando por que. Outros quatro sitios ordenavam por occurred_at
sozinho — e tres deles paginam com LIMIT/OFFSET.

`occurred_at` empata de verdade: auditoria de lote grava varias linhas no mesmo
instante. Com empate, o Postgres nao promete ordem estavel entre duas
execucoes, entao a pagina 2 podia repetir uma linha da pagina 1 e omitir outra.
E o F98/F88 na forma de empate, e o sintoma nao aparece em teste com dados
esparsos.

O guard varre `occurred_at` no repo inteiro e exige o desempate, em vez de
enumerar os sitios conhecidos — enumeracao deixa passar o que ficou de fora da
lista, que e o modo de falha 1 do catalogo.

A troca de OFFSET por keyset NAO entra aqui: vive nas duas rotas do
`routes.py`, que a frente 5 vai partir em dez modulos. Fazer aqui garante
conflito no maior arquivo do repo, e este commit ja fecha o lado de corretude.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Task 7: Os guards que não conferem o que enunciam

**Files:**
- Modify: `tests/unit/test_ci_local_parity.py:56`
- Modify: `tests/unit/test_change_freshness.py:235`
- Modify: `tests/unit/test_tools_schemas.py`
- Modify: `.gitignore`
- Delete: o diretório espúrio `D:v4-ads-mcp.superpowerssdd/`

**Interfaces:**
- Consumes: `tests/unit/_guard_harness.py` — em especial `h.markdown(raiz)`, que
  já varre recursivo e já ignora `.superpowers`.
- Produces: nada.

**Contexto de cada um:**

1. ~~**F113 recursivo + 3 violações vivas.**~~ **CAIU — o item do escopo estava
   errado.** Medido em 08/09, depois que o guard reprovou ESTE plano: ele já
   varre recursivo (`h.markdown()` a partir da raiz), já exige `pyproject.toml`
   na linha para não casar prosa, e já tem isenção **por linha** via marcador
   `guard-f113:` na linha seguinte. As "três violações vivas" que um `grep` cru
   encontrou são: duas citações históricas **com marcador escrito**, e uma que
   diz "do pyproject" (prosa, que o casador exclui de propósito). **Zero
   violação viva.** O `grep` era mais grosseiro que o guard — e a prova do
   contrário veio do guard pegando o arquivo que descrevia como consertá-lo.
2. **`test_ci_local_parity.py:56`** afirma que o *nome da ferramenta* aparece no
   workflow. Isso é verdadeiro mesmo se o step tiver sido removido e o nome
   sobreviver num comentário. Passe a afirmar que o **step existe**.
3. **`test_change_freshness.py:235`** é tautologia — a asserção é verdadeira
   independente da implementação. Leia o teste, descubra qual invariante ele
   *pretendia* afirmar, e afirme essa. Se não houver invariante recuperável,
   **remova o teste** e diga por quê no relatório: teste que não pode falhar é
   pior que teste ausente, porque conta como cobertura.
4. **`test_tools_schemas.py`** varre só `test_*_builder.py`, então schema de tool
   cujo teste tenha outro nome nunca é validado. Passe a varrer o registro de
   tools, não o nome do arquivo de teste.

- [ ] **Step 1: Conserte os três guards fracos**

Um por vez, cada um com sua própria prova de sabotagem. O guard do F113 NÃO
entra: a verificação de 08/09 mostrou que ele já faz o que o escopo pedia.

- `test_ci_local_parity.py:56` — afirme que o **step existe** no workflow (parse
  do YAML, procure em `jobs.*.steps[].name` ou `.run`), não que o nome da
  ferramenta aparece em algum lugar do arquivo. Prova: remova o step do
  `ci.yml`, veja vermelho, reponha.
- `test_change_freshness.py:235` — recupere a invariante ou remova o teste.
  Prova: se manteve, sabote a implementação e veja vermelho; se removeu,
  registre no relatório por que não havia invariante recuperável.
- `test_tools_schemas.py` — varra o registro de tools
  (`src/mcp/tools/_registry.py`), não `test_*_builder.py`. Prova: adicione uma
  tool com schema inválido ao registro (temporariamente), veja vermelho,
  remova.

- [ ] **Step 2: Limpe o diretório espúrio e feche o `.gitignore`**

O diretório `D:v4-ads-mcp.superpowerssdd/` está na raiz do repo — nasceu de um
caminho Windows mal montado (`D:\v4-ads-mcp\.superpowers\sdd` virou literal).
Confirme que está fora do git e que nada dentro dele é único antes de apagar:

```bash
git status --porcelain -uall | grep -i superpowerssdd
ls -R "D:v4-ads-mcp.superpowerssdd" 2>/dev/null | head -20
```

Se houver conteúdo não rastreado que pareça trabalho real, **pare e reporte** em
vez de apagar. Se for só scratch de agente, remova e acrescente ao `.gitignore`
um padrão que impeça a recriação.

- [ ] **Step 3: Gate e commit**

```bash
python scripts/check_pre_push.py > /dev/null 2>&1 && git add -A && git commit -F - <<'MSG'
test: tres guards passam a conferir o que enunciam

O F113 NAO entra: o escopo previa aperta-lo, e a verificacao mostrou que ele ja
varre recursivo, ja exige `pyproject.toml` na linha e ja tem isencao por linha.
Zero violacao viva. O item nasceu de um `grep` mais grosseiro que o proprio
guard.

test_ci_local_parity afirmava que o NOME da ferramenta aparecia no workflow —
verdadeiro mesmo com o step removido e o nome sobrevivendo num comentario.
Passa a afirmar que o step existe.

test_change_freshness:235 era tautologia: verdadeira independente da
implementacao, e portanto contando como cobertura sem cobrir nada.

test_tools_schemas varria `test_*_builder.py`, entao schema de tool cujo teste
tivesse outro nome nunca era validado. Passa a varrer o registro de tools.

Cada um com a mutacao que o derruba provada, e — nos apertados — a prova de que
o guard ANTIGO ficava verde com a mesma mutacao. Sem essa segunda metade nao se
demonstra aperto, so reescrita.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
MSG
```

---

## Fecho da PR

- [ ] **Abrir os findings no catálogo.** `docs/operacao/findings-catalog.md` está
  em F164 (a frente 4 fechou F159–F164). Esta PR abre a partir de **F165**. Um
  finding por família, não por arquivo: o gêmeo Meta do F141 é UM; o throttle do
  Graph é UM; o `bulk_grant` é UM; os quatro guards fracos são UM; o desempate é
  UM. Cada entrada diz o que foi feito **e o que ficou deliberadamente de fora**.
- [ ] **Atualizar `docs/operacao/estado-atual.md`:** frente 6 de ⬜ para ✅, e as
  duas pendências que esta PR deixa nomeadas — migrations 002/004 (decisão do
  Wellington) e o keyset (frente 5).
- [ ] **NÃO tocar em `CLAUDE.md`** — 13 bytes de folga.
- [ ] **Full sweep final** na árvore inteira: `python scripts/check_pre_push_full.py`.

---

## Self-Review

**1. Cobertura da spec.** Nove itens no escopo verificado; oito têm tarefa:
(1) gêmeo Meta → Tasks 1–2; (2) throttle do Graph → Task 3; (3) `bulk_grant` →
Task 4; (4) guard do F113 → **CAIU, o item estava errado** (ver Task 7);
(5) três guards fracos → Task 7;
(6) índices → Task 5; (7) desempate → Task 6 (keyset explicitamente diferido);
(8) migrations 002/004 → **fora, com razão escrita**; (9) diretório espúrio →
Task 7 Step 8. O ignore de major do `mcp` saiu na PR #66 e não aparece aqui.

**2. Placeholders.** Nenhum "TBD"/"adicione tratamento adequado". As duas
tarefas que dependem de código que não li linha a linha (Task 4, helpers de
fixture; Task 6, nome da coluna de desempate) trazem instrução explícita de
**ler o arquivo e deixar o código vencer** em vez de assumir.

**3. Consistência de tipos.** `resolve_meta_account_today(ad_account_id: str, *,
now: datetime | None = None) -> date` é definida na Task 1 e consumida na Task 2
com a mesma assinatura. O campo é `timezone_name` nas duas. `CODIGOS_DE_THROTTLE`
é `frozenset[int]`, consumida só na Task 3.

**Ordem de execução:** Task 1 antes da 2 (a 2 consome o resolvedor). As demais
são independentes entre si e podem ir em qualquer ordem. Nenhum par de tarefas
toca o mesmo arquivo, **exceto** Task 6 e Task 7, que ambas mexem em `tests/` —
mas em arquivos distintos, sem sobreposição.
