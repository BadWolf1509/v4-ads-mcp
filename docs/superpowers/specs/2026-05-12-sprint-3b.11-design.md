# Sprint 3b.11 — Process cleanup bundle Design

**Date:** 2026-05-12
**Author:** Claude (Sonnet 4.7) em sessão dirigida por wellinton.ribeiro@v4company.com
**Status:** APPROVED — pronto para writing-plans
**Origem:** P2 Task 1 spawn-task (orphan fixtures) + investigação CI failure 2026-05-12 (lesson learned)

## 1. Goal

Bundle de 2 cleanups independentes em 1 sprint:

1. **CLAUDE.md update** — adicionar lesson learned do bug pattern "pre-flight + integration tests sem mock" (apareceu Sprint 3b.5 + Sprint 3b.8)
2. **`tests/integration/test_update_ad_status.py` delete** — file só tem 3 fixtures órfãs (Sprint 3b.5 deletou único test)

Zero source code change. Zero new MCP tools (count stays 39).

## 2. Scope decisions

| # | Decisão | Resolução | Justificativa |
|---|---|---|---|
| 1 | Bundle vs separate sprints | **Bundle (single sprint)** | Ambos são <30min total + same nature (process/cleanup). Splitting adds ceremony sem valor. |
| 2 | Onde colocar lesson learned em CLAUDE.md | **Nova subsection "Pre-flight test convention (post-Sprint 3b.8)"** | Match existing pattern de "Mutate builder test convention (post-Sprint 3b.5)" — style-coherent, discoverable. |
| 3 | Atualizar bullet existente em "Don't do" | **Sim** | Expand reference de "Sprint 3b.5" para "Sprint 3b.5/3b.8" — preserva narrative continuity. |
| 4 | Cleanup approach pro test file | **`git rm`** (delete entire file) | Fixtures órfãs sem referências; CLAUDE.md convention é "local fixtures per file" então extrair não ajuda. Simpler. |
| 5 | Smoke runbook | **Não** | Docs + dead code removal — zero production behavior change. |

## 3. Component specs

### 3.1 CLAUDE.md — new "Pre-flight test convention" subsection

Insert AFTER existing "Mutate builder test convention (post-Sprint 3b.5)" block (after the YAGNI paragraph mention).

```markdown
### Pre-flight test convention (post-Sprint 3b.8)

When adding a pre-flight call to an existing mutate tool — especially one that
invokes `run_report` via a shared helper in `src/google_ads/queries/_common.py`
— the existing integration tests MUST mock the new helper. Pattern:

```python
with patch(
    "src.mcp.tools.<your_tool>.<helper_name>",
    AsyncMock(return_value=None),  # pre-flight passes
):
    ...
```

Why this matters: the helper's `run_report` import lives in `_common.py`
namespace, NOT the tool's namespace. Existing test patches targeting
`src.mcp.tools.<tool>.run_report` do NOT cover the pre-flight call site.

Bug class recorrente — appeared in Sprint 3b.5 (commit `3c23fc5`,
`apply_audience` pre-flight) and Sprint 3b.8 (commit `5fa1fd3`,
`update_keyword_bid` + `update_ad_group_bid` pre-flight). Both shipped
to main + caught by CI (DB integration step) but missed by local
`check_pre_push.py` (fast script doesn't run DB integration tests).

Mitigation: before pushing a pre-flight change, run
`python scripts/check_pre_push_full.py` (Docker required) to catch this
class locally. Alternative: rely on CI as catch-all + plan for 1
follow-up fix commit if CI red.
```

### 3.2 CLAUDE.md — extend existing "Don't do" bullet

Find existing bullet (line 259):

```markdown
- Don't push to main without running `python scripts/check_pre_push.py` first.
  CI catches lint/type/test failures but it wastes a deploy cycle and may
  trigger rollback if integration tests reveal a bug. Lesson Sprint 3b.5:
  gate gap (apenas unit pre-push) deixou 2 integration tests quebrados
  escaparem para CI.
```

Replace with:

```markdown
- Don't push to main without running `python scripts/check_pre_push.py` first.
  CI catches lint/type/test failures but it wastes a deploy cycle and may
  trigger rollback if integration tests reveal a bug. Lesson Sprint 3b.5/3b.8:
  pre-flight additions to existing mutate tools require the OPT-IN full sweep
  (`check_pre_push_full.py`, Docker required) — fast script doesn't run DB
  integration tests where pre-flight mock gaps surface.
```

### 3.3 `tests/integration/test_update_ad_status.py` delete

State (verified Sprint 3b.10):
- 47 lines
- 3 fixtures (pg, db, session_ctx)
- 0 test functions
- Zero external references (grep cleared)
- File added in Sprint 3b.2 with 1 test, Sprint 3b.5 deleted that test (commit `3c23fc5`)

Action: `git rm tests/integration/test_update_ad_status.py`

Verification post-delete:
- `python -m pytest tests/integration --collect-only -m integration` → counts unchanged from prior (file contributed 0 tests)
- Other test files still importable (no shared fixture imports from this file — verified)

## 4. Files affected

```
CLAUDE.md                                          # MODIFY: +new section (~25 LOC) + bullet update (~3 LOC delta)
tests/integration/test_update_ad_status.py        # DELETE (47 LOC removed)
```

**Net delta:** +28 LOC docs / -47 LOC dead code = -19 net LOC. Zero source code change. Zero new MCP tools.

## 5. Risks & mitigations

| Risk | Likelihood | Mitigation |
|---|---|---|
| Other test files import fixtures from deleted file | Very Low | Grep'd earlier — zero references. Re-verify before delete. |
| Lesson learned section dilutes CLAUDE.md | Very Low | Match existing pattern (Mutate builder test convention). Future Claude sessions benefit from explicit documentation. |
| CI fails post-delete | Very Low | File contributed 0 tests — collection counts identical. |

## 6. Out of scope

- **Refactor `check_pre_push_full.py` to detect "pre-flight added" automatically** — out of scope for cleanup bundle. Would require static analysis or git hook intelligence. YAGNI; manual mitigation in convention is sufficient.
- **Sprint 3b.5 commit `3c23fc5` retroactive cleanup** — the orphan fixtures were leftover from that commit. Now resolved via delete; no further action.
- **Other orphan files audit** — grep'd briefly during P3; no other dead test files identified. Out of scope for systematic sweep.

## 7. Open questions — resolved

| # | Question | Resolution |
|---|---|---|
| 1 | Bundle 2 cleanups vs separate sprints | Bundle (both <30min, same nature) |
| 2 | Lesson learned location | New subsection "Pre-flight test convention" |
| 3 | Update existing "Don't do" bullet | Yes (expand Sprint 3b.5 → 3b.5/3b.8 reference) |
| 4 | Cleanup approach for orphan file | `git rm` entire file |
| 5 | Smoke runbook needed | No (docs + dead code only) |

## 8. References

- P2 Task 1 spawn-task ("orphan fixtures cleanup pendente") — discovered during P2 Task 1
- CI failures investigation 2026-05-12 — 3 sign-off CIs red, fixed via `5fa1fd3`
- Bug pattern occurrences:
  - Sprint 3b.5 commit `3c23fc5` — `apply_audience` pre-flight test mock gap
  - Sprint 3b.8 commit `5fa1fd3` — `update_keyword_bid` + `update_ad_group_bid` pre-flight test mock gap
- CLAUDE.md "Mutate builder test convention (post-Sprint 3b.5)" — pattern reference for new convention block style
